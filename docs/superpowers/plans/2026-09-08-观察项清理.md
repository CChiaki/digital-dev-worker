# 观察项清理批次 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消化 5 条已留痕的观察级 Minor（task.role 注入 trim / handlers 回退分支退役字段 / seedFrom 死字段 / 前端文案常量化 / router.back 兜底），不引入任何行为面扩张。

**Architecture:** 全部为收口式小改——API 侧三处在既有单一咽喉点（server.ts 装配 / handlers 回退视图 / employee-store seedFrom）删改；前端两处提常量到 `status.ts` 与新增 `router-utils.ts` 工具。

**Tech Stack:** TypeScript / vitest / @vue/test-utils

## Global Constraints

- 不改 `packages/console-api/data/` 运行数据（只读）。
- yaml 回退路径（无 employeeStore 时）GET /api/employees 的 `roles: [p.role]` 包装**保留**（yaml 仍是单值 role，不属本次范围）。
- TaskDag 列标题「依赖环（需人工处理）」文案语义独立，**保留原样**；只统一「依赖阻断」字面量。
- 全程简体中文注释/文案；conventional commits 中文。

---

### Task 1: skill 注入链 task.role trim

**Files:**
- Modify: `packages/console-api/src/http/server.ts:141-148`（skillsFor 装配）
- Test: `packages/console-api/test/plan-runner-skills.test.ts`（或就近注入测试文件）

**Interfaces:**
- Consumes: `skillsFor: (employeeId: string, taskRole?: string) => Promise<SkillRecord[]>`（executor.ts:142 / process-executor.ts:68 调用，传 `task.role` 原值）
- Produces: 装配层对 taskRole 做 trim 后再判断 `rec.roles.includes(role)`——分派（scheduler.tickInner 已 trim）与注入口径一致。

- [ ] **Step 1: 写失败测试**——任务包 role 带首尾空格（如 `' backend '`）时，员工 roles 含 `backend`，注入应走**精准分支**（只取该岗位 skills）而非并集兜底。断言注入的 skill 集合 = backend 岗位分类的 approved skills，且不含其它岗位分类的 skill。
- [ ] **Step 2: 跑测试确认失败**（现状并集兜底 → 多岗位员工会混入其它岗位 skill）。
- [ ] **Step 3: 改装配**——`server.ts` skillsFor 闭包首行加：

```ts
skillsFor: async (id: string, taskRole?: string) => {
  const rec = await employeeStore.get(id);
  if (!rec) return [];
  const cats = await skillStore.listCategories();
  const byName = (name: string) => cats.find((c) => c.name === name);
  const role = taskRole?.trim(); // 注入口径与调度 trim 收口对齐（2026-09-08 观察项清理）
  const targetRoles = role && rec.roles.includes(role) ? [role] : rec.roles;
  const catIds = targetRoles.map(byName).filter((c): c is NonNullable<typeof c> => !!c).map((c) => c.id);
  return skillStore.skillsForCategories(catIds);
},
```

- [ ] **Step 4: 跑测试确认通过** + `pnpm --filter @ddw/console-api test` 全绿。
- [ ] **Step 5: Commit** `fix(console-api): skill 注入链 taskRole trim 收口——与调度分派口径一致`

### Task 2: handlers yaml 回退分支退役 skills 下线

**Files:**
- Modify: `packages/console-api/src/http/handlers.ts:456-478`（无 employeeStore 回退 GET 视图）

**Interfaces:**
- Consumes: yaml profiles 只读回退视图（`deps.employees`）
- Produces: 回退视图不再下发 `skills` 字段（前端已 roles 化不消费该字段），注释口径与 store 分支一致。

- [ ] **Step 1: 写失败测试**——`handlers.test.ts`（或就近）构造无 employeeStore、仅 `deps.employees` profiles 的 Handlers，GET /api/employees 断言响应项**不含** `skills` 键。
- [ ] **Step 2: 跑测试确认失败**（现状下发 skills）。
- [ ] **Step 3: 改实现**——回退视图删除 `skills: p.skills,` 行，注释改为：

```ts
// 现有 GET profiles 只读视图（回退路径，零回归）；skills 已随多岗位改造退役不再下发（2026-09-08）
```

- [ ] **Step 4: 跑测试确认通过** + console-api 全绿。
- [ ] **Step 5: Commit** `fix(console-api): yaml 回退员工视图下线退役 skills 字段——注释口径对齐`

### Task 3: seedFrom 死字段清理

**Files:**
- Modify: `packages/console-api/src/team/employee-store.ts:236-239`（seedFrom flush）
- Test: `packages/console-api/test/employee-store.test.ts`（seedFrom 用例补断言）

**Interfaces:**
- Consumes: `EmployeeProfile`（yaml 单值 role）
- Produces: 种子档案落盘不再带退役 `skills` / `skillCategories` 键（新档干净，与「字段原样忽略」读入兼容并存）。

- [ ] **Step 1: 写失败测试**——employee-store.test.ts seedFrom 用例断言落盘 JSON 文本不含 `"skillCategories"` 与 `"skills"`（读 flush 后的文件原文 indexOf 断言）。
- [ ] **Step 2: 跑测试确认失败**。
- [ ] **Step 3: 改实现**——seedFrom flush 映射改为：

```ts
await this.flush(profiles.map((p) => ({
  id: p.id, name: p.name, roles: [p.role], capabilities: [],
  ...(p.supervision ? { supervision: p.supervision } : {}),
  enabled: true, createdAt: Date.now(),
})));
```

（同函数上方注释「skills / skillCategories 已退役」处补一句种子侧同步说明。）
- [ ] **Step 4: 跑测试确认通过** + console-api 全绿。
- [ ] **Step 5: Commit** `fix(console-api): seedFrom 种子档案清理退役 skills/skillCategories 死字段`

### Task 4: 前端文案常量化 + router.back 兜底

**Files:**
- Modify: `apps/console/src/status.ts`（新增 DEPS_STATE_ZH + depsLabel 导出）
- Modify: `apps/console/src/views/TaskCenter.vue:103`（depsLabel 改引 status.ts）
- Modify: `apps/console/src/components/TaskDag.vue:80`（依赖阻断 tag 改引常量）
- Create: `apps/console/src/router-utils.ts`
- Modify: `apps/console/src/views/TaskDagView.vue:39`、`EmployeeDetail.vue:78`、`EmployeeLive.vue:241`、`TaskDetail.vue:126`（router.back → goBackOrHome）
- Test: `apps/console/src/__tests__/`（就近状态/视图测试目录）

**Interfaces:**
- Produces: `status.ts` 导出 `DEPS_STATE_ZH: Record<string,string>`（ready: '依赖就绪', blocked: '依赖阻断'）与 `depsLabel(s: string): string`（缺省回「等待依赖」）；`router-utils.ts` 导出 `goBackOrHome(router: Router): void`——`window.history.state?.back` 存在则 `router.back()`，否则 `router.push('/')`。

- [ ] **Step 1: 写失败测试**——① status.ts 的 depsLabel：'ready'→'依赖就绪'、'blocked'→'依赖阻断'、其它→'等待依赖'；② goBackOrHome：mock router + history.state（back 有值 → 调 back；无值 → 调 push('/')）。
- [ ] **Step 2: 跑测试确认失败**。
- [ ] **Step 3: 改实现**——status.ts：

```ts
/** 任务依赖状态 → 中文（TaskCenter 标签 / TaskDag 徽标共用，字面量单点） */
export const DEPS_STATE_ZH: Record<string, string> = {
  ready: '依赖就绪',
  blocked: '依赖阻断',
};
export const depsLabel = (s: string): string => DEPS_STATE_ZH[s] ?? '等待依赖';
```

router-utils.ts：

```ts
import type { Router } from 'vue-router';
/** 返回按钮兜底：直链/刷新进入时 history 无本站上一页，back 会退出站点 → 回首页 */
export function goBackOrHome(router: Router): void {
  if (window.history.state?.back) router.back();
  else void router.push('/');
}
```

四个视图的 `@click="router.back()"` 改 `@click="goBackOrHome(router)"`（import 相应调整）。TaskCenter 本地 depsLabel 定义删除改 import；TaskDag.vue:80 的 `>依赖阻断<` 改 `{{ depsLabel('blocked') }}`。
- [ ] **Step 4: 跑测试确认通过** + `pnpm --filter @ddw/console test` 全绿 + typecheck 零错误。
- [ ] **Step 5: Commit** `fix(console): 依赖状态文案单点化 + 返回按钮直链兜底回首页`

### Task 5: 全量回归 + 总账登记

- [ ] **Step 1:** `pnpm --filter @ddw/console-api test && pnpm --filter @ddw/console test`（不占端口）。
- [ ] **Step 2:** `docs/阶段成果总账.md` 追加 P16 补充 15 收官登记（五项一句话各一），并从「遗留」口径注销对应三条 Minor（task.role trim / handlers 措辞 / seedFrom 形状 / 文案 / router.back）。
- [ ] **Step 3:** Commit `docs(总账): 观察项清理批次收官（P16 补充 15）`
