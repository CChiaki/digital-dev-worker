# 任务计划与能力注册表 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 单员工任务计划执行器（plan 逐项执行/失败即停/续跑）+ 后台可配能力注册表（四类预置）+ mcp-deploy 产物发布接入执行链 + 管理端任务创建双模式与能力管理页。

**Architecture:** `TaskPackage` 增加可选 `plan: PlanItem[]`（与 `tasks` 互斥）；计划执行器（console-api `plan-runner.ts`）复用 `EmployeeRuntime` 按项逐个驱动——同一 session 延续上下文（新 `resumeWith`），每项按能力注册表的 kind 映射注入工具集，verify 命令由执行器亲自运行；进度落 `TaskRecord.planProgress`，失败即停、resume API 续跑。能力注册表 = `FileCapabilityStore`（`<dataDir>/capabilities.json`，首启 seed 四类）+ CRUD API。deploy 走 `mcp-deploy` 新 `createArtifactTransport`（产物复制 + 重启命令，local/ssh 两模式）。

**Tech Stack:** TypeScript monorepo（pnpm）、node:sqlite、vitest、Vue3 + element-plus（apps/console）。

**Spec:** `docs/superpowers/specs/2026-09-05-任务计划与能力注册表-design.md`

## Global Constraints

- 老任务包（无 `plan` 字段）行为完全零回归；`plan` 与 `tasks` 互斥，同时存在报错可读。
- `kind` 可选缺省 `dev`；放大权限项（commit/devops）必须显式声明。
- 任务项状态判定三层：员工 task_check 申报（带证据）→ `verify` 命令执行器亲自跑（退出码判定）→ shadow 级人工放行；任何一层不过即失败停链。
- 权限声明式绑定：每项可用工具 = 能力注册表 kind 映射；模型只在授权工具集内自主调用。
- 未注册/停用 kind：创建任务时报 400 可读；运行中注册表被改 → 该项执行报错按失败即停。
- 提交规范：中文 conventional commit + `Co-Authored-By: Claude Code <noreply@anthropic.com>`。
- 严禁提交 `runtime-local.yaml`；严禁测试中监听端口/启动服务（handler 纯函数直接调用）。
- 测试命令一律在包目录跑：`pnpm -C packages/runtime test`、`pnpm -C packages/console-api test`、`pnpm -C packages/mcp-deploy test`、`pnpm -C apps/console test`。

---

### Task 1: runtime 类型与解析（PlanItem + plan 字段 + planItemInstruction + resumeWith）

**Files:**
- Modify: `packages/runtime/src/task/package.ts`（PlanItem 类型 + parse 校验）
- Modify: `packages/runtime/src/task/brief.ts`（planItemInstruction）
- Modify: `packages/runtime/src/task/checkpoint.ts:28`（items 类型放宽为 `{id:string}[]`）
- Modify: `packages/runtime/src/runtime/employee-runtime.ts`（resumeWith）
- Test: `packages/runtime/test/task-package.test.ts`（追加）、`packages/runtime/test/task-brief.test.ts`（新建，若已有同主题测试文件则追加）
- Test: `packages/runtime/test/employee-runtime.test.ts`（追加 resumeWith 用例；文件名以实际为准，先 `ls packages/runtime/test`）

**Interfaces:**
- Consumes: 现状 `parseTaskPackage`/`buildTaskBrief`/`EmployeeRuntime.run`。
- Produces（后续任务依赖，签名照抄）:
  - `runtime` 导出 `interface PlanItem { id: string; kind?: string; title: string; detail: string; verify?: string }`；`TaskPackage.plan?: PlanItem[]`（经 `packages/runtime/src/index.ts` 确认已桶导出）。
  - `buildPlanItemInstruction(pkg: TaskPackage, item: PlanItem, index: number, total: number, doneSummary: string[]): string`（`task/brief.ts`）。
  - `EmployeeRuntime.resumeWith(taskId: string, instruction: string, extraTools?: Tool[]): Promise<EmployeeOutcome>`。
  - `createTaskCheckTool` 的 `items?: { id: string }[]`（DevTask 结构子集兼容）。

- [ ] **Step 1: 写失败测试——plan 解析**

在 task-package 测试文件追加：

```ts
describe('plan 字段（任务计划，2026-09-05）', () => {
  const planYaml = `
taskId: feat-login
title: 登录功能开发
repo: { url: "http://localhost:3000/demo/web-app.git", branch: develop }
plan:
  - id: t1
    title: 开发登录接口
    detail: 实现 POST /api/login
  - id: t2
    kind: commit
    title: 提交并建 MR
    detail: 提交 feature 分支并建 MR
    verify: npm run build
role: backend
`;
  it('plan 解析：kind 缺省 dev，verify/kind 透传', () => {
    const pkg = parseTaskPackage(planYaml);
    expect(pkg.plan).toHaveLength(2);
    expect(pkg.plan![0]!.kind).toBeUndefined();      // 解析层不填缺省值，运行层查注册表时落 dev
    expect(pkg.plan![1]!.kind).toBe('commit');
    expect(pkg.plan![1]!.verify).toBe('npm run build');
  });

  it('plan 与 tasks 互斥：同时存在报错可读', () => {
    const both = planYaml + 'tasks:\n  - id: d1\n    title: x\n    files: [a]\n    requirement: r\n    acceptance: [a1]\n';
    expect(() => parseTaskPackage(both)).toThrow(/plan 与 tasks 互斥/);
  });

  it('plan 项缺 id/title/detail 报错可读；id 重复报错可读', () => {
    const miss = planYaml.replace('    detail: 实现 POST /api/login\n', '');
    expect(() => parseTaskPackage(miss)).toThrow(/plan\[t1\]\.detail/);
    const dup = planYaml.replace('  - id: t2', '  - id: t1');
    expect(() => parseTaskPackage(dup)).toThrow(/plan 项 id 重复: t1/);
  });

  it('无 plan 的老任务包解析零回归（tasks 照旧必填）', () => {
    const old = `
taskId: t
title: x
repo: { url: "http://x/a.git", branch: main }
tasks:
  - id: d1
    title: x
    files: [a]
    requirement: r
    acceptance: [a1]
`;
    expect(parseTaskPackage(old).plan).toBeUndefined();
    expect(() => parseTaskPackage(old.replace(/tasks:[\s\S]*/, ''))).toThrow(/tasks/);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -C packages/runtime test -- task-package`
Expected: FAIL（plan 未定义 / 校验未实现）

- [ ] **Step 3: 实现 PlanItem 与解析**

`packages/runtime/src/task/package.ts`：

```ts
/** 计划项（任务计划模式，2026-09-05）：逐项驱动执行，kind 查能力注册表（缺省 dev） */
export interface PlanItem {
  id: string;
  kind?: string;
  title: string;
  detail: string;
  /** 可选验证命令：执行器在员工申报后亲自运行，退出码 0 该项才 done */
  verify?: string;
}
```

`TaskPackage` 增加字段：

```ts
  /** 任务计划（可选，与 tasks 互斥）：有 plan 走计划执行器逐项驱动 */
  plan?: PlanItem[];
```

`parseTaskPackage` 中，在 `const tasksRaw = requireField(raw, 'tasks')` **之前**插入 plan 分支，并把 tasks 的 require 改为条件化：

```ts
  // 任务计划（2026-09-05）：plan 与 tasks 互斥；有 plan 时 tasks 可省略
  let plan: PlanItem[] | undefined;
  if (raw['plan'] !== undefined && raw['plan'] !== null) {
    if (raw['tasks'] !== undefined && raw['tasks'] !== null) {
      throw new Error('任务包字段冲突: plan 与 tasks 互斥（计划模式用 plan，勿再写 tasks）');
    }
    if (!Array.isArray(raw['plan']) || raw['plan'].length === 0) {
      throw new Error('任务包字段类型错误: plan 应为非空数组');
    }
    const seen = new Set<string>();
    plan = (raw['plan'] as Record<string, unknown>[]).map((p) => {
      const id = requireField(p, 'id') as string;
      for (const k of ['title', 'detail'] as const) {
        const v = p[k];
        if (v === undefined || v === null || v === '') {
          throw new Error(`任务包缺少必填字段: plan[${id}].${k}`);
        }
      }
      if (seen.has(id)) throw new Error(`任务包字段错误: plan 项 id 重复: ${id}`);
      seen.add(id);
      return {
        id,
        ...(p['kind'] !== undefined && p['kind'] !== null && p['kind'] !== '' ? { kind: p['kind'] as string } : {}),
        title: p['title'] as string,
        detail: p['detail'] as string,
        ...(p['verify'] ? { verify: p['verify'] as string } : {}),
      };
    });
  }
```

将 `const tasksRaw = requireField(raw, 'tasks')` 一段改为条件化（plan 不存在时保持现状）：

```ts
  if (!plan) {
    const tasksRaw = requireField(raw, 'tasks') as Record<string, unknown>[];
    if (!Array.isArray(tasksRaw) || tasksRaw.length === 0) {
      throw new Error('任务包缺少必填字段: tasks（至少一个开发任务）');
    }
    // …（现有 tasks.map 解析代码原样移入此分支）
  }
```

返回对象追加 `...(plan ? { plan } : {})`。

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -C packages/runtime test -- task-package`
Expected: PASS（含老用例全绿）

- [ ] **Step 5: 写失败测试——planItemInstruction**

新建/追加 `packages/runtime/test/task-brief.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import { buildPlanItemInstruction } from '../src/task/brief.js';
import type { TaskPackage, PlanItem } from '../src/task/package.js';

const pkg: TaskPackage = {
  taskId: 'feat-login', title: '登录功能开发',
  repo: { url: 'http://localhost:3000/demo/web-app.git', branch: 'develop' },
  tasks: [],
};
const item: PlanItem = { id: 't2', kind: 'test', title: 'e2e 测试登录', detail: '覆盖正确/错误密码', verify: 'npm run e2e' };

describe('buildPlanItemInstruction（计划项指令，2026-09-05）', () => {
  it('包含序号/kind/标题/要求/验证命令/task_check 申报指引', () => {
    const s = buildPlanItemInstruction(pkg, item, 1, 3, ['t1 开发登录接口：已完成（构建通过）']);
    expect(s).toContain('第 2/3 项');
    expect(s).toContain('e2e 测试登录');
    expect(s).toContain('覆盖正确/错误密码');
    expect(s).toContain('npm run e2e');
    expect(s).toContain('task_check');
    expect(s).toContain('t2');
    expect(s).toContain('t1 开发登录接口：已完成（构建通过）'); // 前序项结果摘要
  });
});
```

- [ ] **Step 6: 跑测试确认失败**（`pnpm -C packages/runtime test -- task-brief`，FAIL：导出不存在）

- [ ] **Step 7: 实现 buildPlanItemInstruction**

`packages/runtime/src/task/brief.ts` 追加：

```ts
import type { PlanItem } from './package.js';

/** 计划项执行指令（计划执行器逐项下发）：序号/kind/要求/验证命令/前序摘要/申报指引 */
export function buildPlanItemInstruction(
  pkg: TaskPackage, item: PlanItem, index: number, total: number, doneSummary: string[],
): string {
  const lines: string[] = [];
  lines.push(`# 执行计划第 ${index + 1}/${total} 项：${item.title}`);
  lines.push(`- 任务包：${pkg.title}（${pkg.taskId}）`);
  lines.push(`- 仓库：${pkg.repo.url}　分支：${pkg.repo.branch}`);
  lines.push(`- 能力类型：${item.kind ?? 'dev'}`);
  lines.push(`- 本项要求：${item.detail}`);
  if (item.verify) lines.push(`- 完成后必须能通过验证命令：\`${item.verify}\`（系统会实际运行核对）`);
  if (doneSummary.length > 0) {
    lines.push('- 前序已完成项：');
    for (const s of doneSummary) lines.push(`  - ${s}`);
  }
  lines.push('');
  lines.push('完成本项后，调用 task_check 申报（item 参数填 "' + item.id + '"），并附验证输出摘要；未通过不要申报。');
  return lines.join('\n');
}
```

- [ ] **Step 8: 跑测试确认通过**（`pnpm -C packages/runtime test -- task-brief`，PASS）

- [ ] **Step 9: 写失败测试——resumeWith**

在 employee-runtime 测试文件追加（复用该文件既有的 faux agentFactory 构造方式；核心断言：第二次调用带历史上下文 + 自定义指令生效）：

```ts
it('resumeWith：延续 session 上下文并执行自定义指令（计划执行器逐项驱动基础）', async () => {
  const runtime = new EmployeeRuntime({ ...deps });   // 照抄文件内既有 deps 构造（faux agentFactory）
  await runtime.run({ taskId: 't-plan', instruction: '第一项：开发' });
  const out = await runtime.resumeWith('t-plan', '第二项：测试（应能看到第一项产出）');
  expect(out.status).toBe('done');
  // faux 断言：按该文件既有模式校验第二次 prompt 的文本包含 '第二项'
});
```

- [ ] **Step 10: 跑测试确认失败**（FAIL：resumeWith 不存在）

- [ ] **Step 11: 实现 resumeWith**

`employee-runtime.ts` 在 `resume` 方法后追加（`resume` 改为委托它，保持单一实现）：

```ts
  /** 从 session 恢复并下发自定义指令（计划执行器逐项驱动：同会话上下文延续）。 */
  async resumeWith(taskId: string, instruction: string, extraTools: Tool[] = []): Promise<EmployeeOutcome> {
    const session = await this.deps.sessions.open(taskId);
    const messages = await loadMessages(session);
    const agent = this.makeAgent(taskId, messages, extraTools);
    const stop = wireSession(agent, session);
    this.active.set(taskId, agent);
    try {
      await agent.prompt(instruction);
    } finally {
      await stop();
      this.active.delete(taskId);
    }
    return this.collect(taskId, agent);
  }
```

并把现有 `resume` 方法体替换为：

```ts
  async resume(taskId: string): Promise<EmployeeOutcome> {
    return this.resumeWith(taskId, '继续完成任务并汇报结果');
  }
```

- [ ] **Step 12: checkpoint items 类型放宽**

`checkpoint.ts:28` 的 `items?: DevTask[]` 改为 `items?: { id: string }[]`（工具内部只消费 `id`；DevTask 结构兼容），文件头 `import type { DevTask }` 若不再使用则删除。

- [ ] **Step 13: 全量测试 + 提交**

Run: `pnpm -C packages/runtime test`
Expected: 全绿

```bash
git add packages/runtime/src packages/runtime/test
git commit -m "feat(runtime): 任务计划 PlanItem 解析与计划项指令/会话续跑 resumeWith"
```

---

### Task 2: 能力注册表（CapabilityStore + seed + CRUD API + 任务包 kind 校验）

**Files:**
- Create: `packages/console-api/src/team/capabilities.ts`
- Modify: `packages/console-api/src/http/handlers.ts`（CRUD 路由 + POST /api/tasks 的 kind 校验）
- Modify: `packages/console-api/src/http/server.ts`（构造 CapabilityStore 注入 handlers 与 pipeline）
- Test: `packages/console-api/test/capabilities.test.ts`（新建）
- Test: `packages/console-api/test/handlers.test.ts`（追加；文件名以实际为准，先 `ls packages/console-api/test`）

**Interfaces:**
- Consumes: Task 1 的 `PlanItem`（`@ddw/runtime` 导出）。
- Produces:
  - `interface CapabilityDef { kind: string; name: string; description?: string; tools: { builtin: string[]; mcp: string[] }; enabled: boolean }`
  - `const CAPABILITY_PRESETS: CapabilityDef[]`（dev/test/commit/devops 四条，见 Step 3）
  - `interface CapabilityStore { list(): Promise<CapabilityDef[]>; upsert(def: CapabilityDef): Promise<void>; remove(kind: string): Promise<boolean>; ensureSeed(): Promise<void> }`
  - `class FileCapabilityStore implements CapabilityStore`（构造参数 `filePath: string`）
  - handlers deps 增加可选项 `capabilities?: CapabilityStore`

- [ ] **Step 1: 写失败测试——store 与 seed**

`packages/console-api/test/capabilities.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import { mkdtemp, readFile, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { FileCapabilityStore, CAPABILITY_PRESETS } from '../src/team/capabilities.js';

const tmp = async (): Promise<string> => await mkdtemp(join(tmpdir(), 'caps-'));

describe('FileCapabilityStore（能力注册表，2026-09-05）', () => {
  it('ensureSeed 首启写入四类预置；幂等（已有内容不覆盖）', async () => {
    const dir = await tmp();
    try {
      const store = new FileCapabilityStore(join(dir, 'capabilities.json'));
      await store.ensureSeed();
      const list = await store.list();
      expect(list.map((c) => c.kind)).toEqual(['dev', 'test', 'commit', 'devops']);
      expect(list.find((c) => c.kind === 'devops')?.tools.mcp).toEqual(['deploy']);
      await store.upsert({ ...CAPABILITY_PRESETS[0]!, name: '开发编码改', enabled: false });
      await store.ensureSeed();  // 二次 seed 不覆盖用户修改
      expect((await store.list()).find((c) => c.kind === 'dev')?.name).toBe('开发编码改');
    } finally { await rm(dir, { recursive: true, force: true }); }
  });

  it('upsert 新增与修改；remove 删除；list 排序稳定', async () => {
    const dir = await tmp();
    try {
      const store = new FileCapabilityStore(join(dir, 'capabilities.json'));
      await store.ensureSeed();
      await store.upsert({ kind: 'scan', name: '安全扫描', tools: { builtin: ['bash'], mcp: [] }, enabled: true });
      expect((await store.list()).map((c) => c.kind)).toContain('scan');
      await store.upsert({ kind: 'scan', name: '安全扫描2', tools: { builtin: ['bash'], mcp: [] }, enabled: false });
      expect((await store.list()).find((c) => c.kind === 'scan')?.enabled).toBe(false);
      expect(await store.remove('scan')).toBe(true);
      expect(await store.remove('scan')).toBe(false);
    } finally { await rm(dir, { recursive: true, force: true }); }
  });

  it('upsert 校验：kind/name 非空、tools.builtin/mcp 须为字符串数组（mcp 仅允许 forge/deploy）', async () => {
    const dir = await tmp();
    try {
      const store = new FileCapabilityStore(join(dir, 'capabilities.json'));
      await expect(store.upsert({ kind: '', name: 'x', tools: { builtin: [], mcp: [] }, enabled: true }))
        .rejects.toThrow(/kind 必须是非空字符串/);
      await expect(store.upsert({ kind: 'k', name: '', tools: { builtin: [], mcp: [] }, enabled: true }))
        .rejects.toThrow(/name 必须是非空字符串/);
      await expect(store.upsert({ kind: 'k', name: 'n', tools: { builtin: 'bash', mcp: [] } as never, enabled: true }))
        .rejects.toThrow(/tools\.builtin 必须是字符串数组/);
      await expect(store.upsert({ kind: 'k', name: 'n', tools: { builtin: [], mcp: ['nope'] }, enabled: true }))
        .rejects.toThrow(/tools\.mcp 含未注册的工具包/);
    } finally { await rm(dir, { recursive: true, force: true }); }
  });
});
```

- [ ] **Step 2: 跑测试确认失败**（`pnpm -C packages/console-api test -- capabilities`，FAIL：模块不存在）

- [ ] **Step 3: 实现 capabilities.ts**

`packages/console-api/src/team/capabilities.ts`：

```ts
import { mkdir, readFile, rename, writeFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';

/** 能力定义（任务项 kind → 工具集映射；后台可配置，2026-09-05） */
export interface CapabilityDef {
  kind: string;
  name: string;
  description?: string;
  /** builtin: 'bash' | 'files'；mcp: 工具包标识（装配器已注册的：'forge' | 'deploy'） */
  tools: { builtin: string[]; mcp: string[] };
  enabled: boolean;
}

/** mcp 工具包标识注册表（新工具包接入 = 此处加标识 + default-tools.ts 加实例工厂，一次性代码接入） */
export const KNOWN_MCP_PACKS = ['forge', 'deploy'] as const;
const KNOWN_BUILTIN = ['bash', 'files'] as const;

/** 首批四类预置（ensureSeed 写入；后台可改可停可增） */
export const CAPABILITY_PRESETS: CapabilityDef[] = [
  { kind: 'dev', name: '开发编码', description: '阅读、编写、修改代码与文档，构建验证',
    tools: { builtin: ['bash', 'files'], mcp: [] }, enabled: true },
  { kind: 'test', name: '测试验证', description: '运行单元测试、e2e 测试并核对结果',
    tools: { builtin: ['bash', 'files'], mcp: [] }, enabled: true },
  { kind: 'commit', name: '代码提交', description: '建分支、提交文件、创建与查询 MR',
    tools: { builtin: ['files'], mcp: ['forge'] }, enabled: true },
  { kind: 'devops', name: '发布部署', description: '传输产物到目标环境并重启服务',
    tools: { builtin: [], mcp: ['deploy'] }, enabled: true },
];

export interface CapabilityStore {
  list(): Promise<CapabilityDef[]>;
  upsert(def: CapabilityDef): Promise<void>;
  remove(kind: string): Promise<boolean>;
  /** 文件不存在时写入预置四类；已存在不覆盖（幂等） */
  ensureSeed(): Promise<void>;
}

const BUILTIN_SET = new Set<string>(KNOWN_BUILTIN);
const MCP_SET = new Set<string>(KNOWN_MCP_PACKS);

/** upsert 前的形状与取值校验（错误信息可读，HTTP 层原样 400） */
export function validateCapabilityDef(def: CapabilityDef): void {
  if (typeof def.kind !== 'string' || !def.kind.trim()) throw new Error('能力 kind 必须是非空字符串');
  if (typeof def.name !== 'string' || !def.name.trim()) throw new Error('能力 name 必须是非空字符串');
  if (!Array.isArray(def.tools?.builtin) || !def.tools.builtin.every((x) => typeof x === 'string')) {
    throw new Error('能力 tools.builtin 必须是字符串数组');
  }
  if (!def.tools.builtin.every((x) => BUILTIN_SET.has(x))) {
    throw new Error(`能力 tools.builtin 含未知内置能力: ${def.tools.builtin.filter((x) => !BUILTIN_SET.has(x)).join(', ')}（允许: bash, files）`);
  }
  if (!Array.isArray(def.tools.mcp) || !def.tools.mcp.every((x) => typeof x === 'string')) {
    throw new Error('能力 tools.mcp 必须是字符串数组');
  }
  if (!def.tools.mcp.every((x) => MCP_SET.has(x))) {
    throw new Error(`能力 tools.mcp 含未注册的工具包: ${def.tools.mcp.filter((x) => !MCP_SET.has(x)).join(', ')}（允许: ${[...MCP_SET].join(', ')}）`);
  }
  if (typeof def.enabled !== 'boolean') throw new Error('能力 enabled 必须是布尔值');
}

/** 能力注册表文件实现：单 JSON 文件 + mutate 队列串行化（能力量少读多写少，文件足够） */
export class FileCapabilityStore implements CapabilityStore {
  private cache: CapabilityDef[] | undefined;
  private queue: Promise<unknown> = Promise.resolve();

  constructor(private readonly filePath: string) {}

  private async load(): Promise<CapabilityDef[]> {
    if (this.cache) return this.cache;
    try {
      this.cache = JSON.parse(await readFile(this.filePath, 'utf8')) as CapabilityDef[];
    } catch {
      this.cache = [];
    }
    return this.cache;
  }

  private async flush(defs: CapabilityDef[]): Promise<void> {
    this.cache = defs;
    await mkdir(dirname(this.filePath), { recursive: true });
    const tmpPath = join(dirname(this.filePath), `.capabilities-${process.pid}.tmp`);
    await writeFile(tmpPath, JSON.stringify(defs, null, 2), 'utf8');
    await rename(tmpPath, this.filePath);
  }

  list(): Promise<CapabilityDef[]> {
    return this.load().then((defs) => defs.map((d) => structuredClone(d)));
  }

  upsert(def: CapabilityDef): Promise<void> {
    const run = this.queue.then(async () => {
      validateCapabilityDef(def);
      const defs = await this.load();
      const idx = defs.findIndex((d) => d.kind === def.kind);
      if (idx >= 0) defs[idx] = { ...def };
      else defs.push({ ...def });
      await this.flush(defs);
    });
    this.queue = run.catch(() => undefined);
    return run;
  }

  remove(kind: string): Promise<boolean> {
    const run = this.queue.then(async () => {
      const defs = await this.load();
      const next = defs.filter((d) => d.kind !== kind);
      if (next.length === defs.length) return false;
      await this.flush(next);
      return true;
    });
    this.queue = run.catch(() => undefined);
    return run;
  }

  async ensureSeed(): Promise<void> {
    const run = this.queue.then(async () => {
      let exists = true;
      try { await readFile(this.filePath, 'utf8'); } catch { exists = false; }
      if (exists) return;
      await this.flush(CAPABILITY_PRESETS.map((d) => ({ ...d })));
    });
    this.queue = run.catch(() => undefined);
    return run;
  }
}
```

- [ ] **Step 4: 跑测试确认通过**（`pnpm -C packages/console-api test -- capabilities`，PASS）

- [ ] **Step 5: 写失败测试——handlers CRUD 与任务包 kind 校验**

在 handlers 测试文件追加（照抄该文件既有 deps 构造，追加 `capabilities`）：

```ts
describe('能力注册表 API（2026-09-05）', () => {
  it('GET /api/capabilities 返回 seed 四类；POST 新增；PUT 修改；DELETE 删除', async () => {
    const caps = new FileCapabilityStore(join(dataDir, 'capabilities.json'));
    await caps.ensureSeed();
    const handle = createHandlers({ tasks, events, capabilities: caps });
    const seeded = await handle({ method: 'GET', path: '/api/capabilities' });
    expect(seeded.status).toBe(200);
    expect((seeded.json as CapabilityDef[]).map((c) => c.kind)).toEqual(['dev', 'test', 'commit', 'devops']);

    const created = await handle({ method: 'POST', path: '/api/capabilities',
      body: { kind: 'scan', name: '安全扫描', tools: { builtin: ['bash'], mcp: [] }, enabled: true } });
    expect(created.status).toBe(201);

    const updated = await handle({ method: 'PUT', path: '/api/capabilities/scan',
      body: { kind: 'scan', name: '安全扫描2', tools: { builtin: ['bash'], mcp: [] }, enabled: false } });
    expect(updated.status).toBe(200);
    expect((await handle({ method: 'GET', path: '/api/capabilities' })).json
      ).toEqual(expect.arrayContaining([expect.objectContaining({ kind: 'scan', enabled: false })]));

    expect((await handle({ method: 'DELETE', path: '/api/capabilities/scan' })).status).toBe(200);
    expect((await handle({ method: 'DELETE', path: '/api/capabilities/scan' })).status).toBe(404);
  });

  it('POST/PUT 非法 body 报 400 可读；未配置 capabilities 时 CRUD 返回 400', async () => {
    const handle = createHandlers({ tasks, events });  // 无 capabilities
    expect((await handle({ method: 'GET', path: '/api/capabilities' })).status).toBe(400);
    const handle2 = createHandlers({ tasks, events, capabilities: caps });
    const bad = await handle2({ method: 'POST', path: '/api/capabilities', body: { kind: 'x' } });
    expect(bad.status).toBe(400);
    expect((bad.json as { error: string }).error).toMatch(/name 必须是非空字符串|tools 必须是对象/);
  });

  it('POST /api/tasks：plan 引用未注册/停用 kind 报 400 可读（缺省 dev 不校验白名单放行）', async () => {
    const handle = createHandlers({ tasks, events, capabilities: caps }); // seed 四类，devops 已停用场景单独构造
    const bad = await handle({ method: 'POST', path: '/api/tasks', body: planYamlWithKind('devopsx') });
    expect(bad.status).toBe(400);
    expect((bad.json as { error: string }).error).toMatch(/任务项 t2\.kind='devopsx' 未注册或已停用/);
  });
});
```

（`planYamlWithKind` 为测试内 helper：合法任务包 yaml，plan 第二项 kind 用参数替换。）

- [ ] **Step 6: 跑测试确认失败**（FAIL：路由不存在）

- [ ] **Step 7: 实现 handlers 路由与校验**

`handlers.ts` deps 增加 `capabilities?: CapabilityStore`（import 自 `../team/capabilities.js`）。在 `POST /api/tasks` 分支中 `parseTaskPackage` 成功后、`tasks.add` 之前插入：

```ts
        if (pkg.plan && deps.capabilities) {
          const defs = await deps.capabilities.list();
          for (const item of pkg.plan) {
            const kind = item.kind ?? 'dev';
            const def = defs.find((c) => c.kind === kind);
            if (!def || !def.enabled) {
              return err(400, `任务项 ${item.id}.kind='${kind}' 未注册或已停用（能力管理中登记后方可使用）`);
            }
          }
        }
```

在 `GET /api/checks` 路由之前插入能力 CRUD（未配置 capabilities 统一 400）：

```ts
      // 能力注册表（2026-09-05）：kind → 工具集映射的后台管理
      if (seg[1] === 'capabilities') {
        if (!deps.capabilities) return err(400, '未启用能力注册表');
        if (method === 'GET' && seg.length === 2) return ok(await deps.capabilities.list());
        if (method === 'POST' && seg.length === 2) {
          const def = body as CapabilityDef;
          try { validateCapabilityDef(def); } catch (e) { return err(400, e instanceof Error ? e.message : String(e)); }
          await deps.capabilities.upsert(def);
          return ok({ kind: def.kind }, 201);
        }
        if (seg.length === 3) {
          const kind = seg[2]!;
          if (method === 'PUT') {
            const def = body as CapabilityDef;
            try { validateCapabilityDef(def); } catch (e) { return err(400, e instanceof Error ? e.message : String(e)); }
            if (def.kind !== kind) return err(400, `路径 kind (${kind}) 与 body.kind (${def.kind}) 不一致`);
            await deps.capabilities.upsert(def);
            return ok({ kind });
          }
          if (method === 'DELETE') {
            const removed = await deps.capabilities.remove(kind);
            if (!removed) return err(404, `能力不存在: ${kind}`);
            return ok({ kind, removed: true });
          }
        }
      }
```

- [ ] **Step 8: server.ts 装配**

`server.ts`：构造 `const capabilities = new FileCapabilityStore(join(opts.dataDir, 'capabilities.json')); await capabilities.ensureSeed();`（ensureSeed 在 startConsoleServer 里 async 化——`startConsoleServer` 改为非 async 的话，将 ensureSeed 挂到 server listening 前的 `void` promise；**采取**：`startConsoleServer` 保持同步签名，构造后立即 `void capabilities.ensureSeed()`，handlers/capabilities 的读路径容忍暂空（list 返回空数组时 GET 显示空表，seed 毫秒级完成））。`createHandlers` deps 追加 `capabilities`；`createRuntimePipeline` 的 `opts.runtime` 无需变更（Task 4 处理 executor 侧注入）。

- [ ] **Step 9: 全量测试 + 提交**

Run: `pnpm -C packages/console-api test`
Expected: 全绿

```bash
git add packages/console-api/src packages/console-api/test
git commit -m "feat(console-api): 能力注册表——kind→工具集映射后台可配（seed 四类 + CRUD API + 任务包 kind 校验）"
```

---

### Task 3: deploy 链（yaml deploy 段 + ArtifactTransport + defaultToolsFor 分档）

**Files:**
- Modify: `packages/console-api/src/team/runtime-config.ts`（DeployConfig 解析）
- Create: `packages/mcp-deploy/src/artifact-transport.ts`
- Modify: `packages/mcp-deploy/src/index.ts`（桶导出，若为 `export *` 则确认覆盖）
- Modify: `packages/console-api/src/team/default-tools.ts`（builtin/mcp 分档 + deploy 工具注册）
- Test: `packages/console-api/test/runtime-config.test.ts`（追加）
- Test: `packages/mcp-deploy/test/artifact-transport.test.ts`（新建）
- Test: `packages/console-api/test/default-tools.test.ts`（追加；文件名以实际为准）

**Interfaces:**
- Consumes: Task 2 的 `CapabilityDef`。
- Produces:
  - `interface DeployEnvConfig { name: string; artifactDir: string; restartCommand?: string; artifactPath?: string; host?: string; user?: string }`；`interface DeployConfig { envs: DeployEnvConfig[] }`（runtime-config.ts 导出，追加进 `ConsoleRuntimeConfig` 可选字段 `deploy?`）
  - `createArtifactTransport(deps: { envs: DeployEnvConfig[]; workspaceDir: string; timeoutMs?: number }): DeployTransport`（mcp-deploy 包）
  - `defaultToolsFor(wsDir, opts)` 的 opts 追加 `deploy?: DeployConfig`、`builtin?: string[]`、`mcp?: string[]`（**不传时行为与现状完全一致**：bash+files 恒注册、forge 按配置注册、deploy 不注册）

- [ ] **Step 1: 写失败测试——deploy 段解析**

`runtime-config.test.ts` 追加：

```ts
describe('deploy 段（发布部署，2026-09-05）', () => {
  it('deploy.envs 合法解析透传', () => {
    const cfg = parseRuntimeConfig(baseYaml(
      'deploy:\n  envs:\n    - name: test\n      artifactDir: /opt/ddw/web-app\n      restartCommand: "bash /opt/ddw/restart.sh"\n      artifactPath: dist\n      host: 127.0.0.1\n      user: root\n',
    ));
    expect(cfg.deploy?.envs).toEqual([
      { name: 'test', artifactDir: '/opt/ddw/web-app', restartCommand: 'bash /opt/ddw/restart.sh', artifactPath: 'dist', host: '127.0.0.1', user: 'root' },
    ]);
  });

  it('缺省不注入；name/artifactDir 缺失报错可读', () => {
    expect(parseRuntimeConfig(baseYaml()).deploy).toBeUndefined();
    expect(() => parseRuntimeConfig(baseYaml('deploy:\n  envs:\n    - artifactDir: /x\n')))
      .toThrow(/deploy\.envs\[0\]\.name 必须是非空字符串/);
    expect(() => parseRuntimeConfig(baseYaml('deploy:\n  envs:\n    - name: test\n')))
      .toThrow(/deploy\.envs\[0\]\.artifactDir 必须是非空字符串/);
    expect(() => parseRuntimeConfig(baseYaml('deploy:\n  envs: []\n')))
      .toThrow(/deploy\.envs 必须是非空数组/);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**（FAIL）

- [ ] **Step 3: 实现 DeployConfig 解析**

`runtime-config.ts`：类型追加（ForgeConfig 旁）：

```ts
/** 发布部署配置（deploy 段）：产物 → 目标目录 + 重启命令（本版不接 DevOps 平台） */
export interface DeployEnvConfig {
  name: string;
  artifactDir: string;
  restartCommand?: string;
  /** 员工工作区内产物相对路径（缺省 dist） */
  artifactPath?: string;
  /** 配了走 ssh（scp/ssh exec），缺省本机目录直拷 */
  host?: string;
  user?: string;
}
export interface DeployConfig { envs: DeployEnvConfig[]; }
```

`parseRuntimeConfig` 中 forge 段之后追加：

```ts
  // deploy 段（可选）：发布部署环境列表
  let deploy: DeployConfig | undefined;
  if (raw['deploy'] !== undefined) {
    const d = raw['deploy'];
    if (!d || typeof d !== 'object') fail('deploy', '必须是对象');
    const envsRaw = (d as Record<string, unknown>)['envs'];
    if (!Array.isArray(envsRaw) || envsRaw.length === 0) fail('deploy.envs', '必须是非空数组');
    const envs: DeployEnvConfig[] = envsRaw.map((e: unknown, i: number) => {
      const field = `deploy.envs[${i}]`;
      if (!e || typeof e !== 'object') fail(field, '必须是对象');
      const o = e as Record<string, unknown>;
      for (const k of ['name', 'artifactDir'] as const) {
        const v = o[k];
        if (typeof v !== 'string' || !v.trim()) fail(`${field}.${k}`, '必须是非空字符串');
      }
      return {
        name: (o['name'] as string).trim(),
        artifactDir: (o['artifactDir'] as string).trim(),
        ...(o['restartCommand'] ? { restartCommand: o['restartCommand'] as string } : {}),
        ...(o['artifactPath'] ? { artifactPath: o['artifactPath'] as string } : {}),
        ...(o['host'] ? { host: o['host'] as string } : {}),
        ...(o['user'] ? { user: o['user'] as string } : {}),
      };
    });
    deploy = { envs };
  }
```

返回对象追加 `...(deploy ? { deploy } : {})`；`ConsoleRuntimeConfig` 接口追加 `deploy?: DeployConfig`。

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 写失败测试——ArtifactTransport**

`packages/mcp-deploy/test/artifact-transport.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import { mkdir, mkdtemp, readFile, rm, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { createArtifactTransport } from '../src/artifact-transport.js';

const tmp = async (): Promise<string> => await mkdtemp(join(tmpdir(), 'deploy-'));

describe('createArtifactTransport（产物发布，2026-09-05）', () => {
  it('local：复制产物目录到目标 + 执行重启命令；未知环境报错可读', async () => {
    const dir = await tmp();
    try {
      const ws = join(dir, 'ws');
      const target = join(dir, 'opt', 'app');
      await mkdir(join(ws, 'dist'), { recursive: true });
      await writeFile(join(ws, 'dist', 'app.js'), 'console.log(1)');
      const restartScript = join(dir, 'restart.sh');
      await writeFile(restartScript, `echo "restarted $(cat ${join(target, 'app.js') | ''})" > ${join(dir, 'flag.txt')}`);
      const transport = createArtifactTransport({
        envs: [{ name: 'test', artifactDir: target, restartCommand: `bash ${restartScript}` }],
        workspaceDir: ws,
      });
      const res = await transport.deploy({ project: 'web-app', env: 'test', version: 'abc123' });
      expect(res.status).toBe('done');
      expect(await readFile(join(target, 'app.js'), 'utf8')).toContain('console.log(1)');
      await expect(transport.deploy({ project: 'p', env: 'prod', version: 'x' }))
        .rejects.toThrow(/未配置发布环境: prod/);
    } finally { await rm(dir, { recursive: true, force: true }); }
  });

  it('restartCommand 非零退出 → 抛错（工具层映射任务项失败）', async () => {
    const dir = await tmp();
    try {
      const ws = join(dir, 'ws');
      await mkdir(join(ws, 'dist'), { recursive: true });
      const transport = createArtifactTransport({
        envs: [{ name: 'test', artifactDir: join(dir, 't'), restartCommand: 'exit 3' }],
        workspaceDir: ws,
      });
      await expect(transport.deploy({ project: 'p', env: 'test', version: 'v' })).rejects.toThrow();
    } finally { await rm(dir, { recursive: true, force: true }); }
  });
});
```

注意：上面 restartScript 写法跨平台不稳，实现时改为 `echo ok > <flag>` 的直接命令字符串即可（断言 flag 文件存在）。

- [ ] **Step 6: 实现 artifact-transport.ts**

`packages/mcp-deploy/src/artifact-transport.ts`：

```ts
import { exec } from 'node:child_process';
import { cp, mkdir } from 'node:fs/promises';
import { join } from 'node:path';
import { promisify } from 'node:util';
import type { DeployTransport } from './transport.js';

const run = promisify(exec);

/** 产物发布环境配置（与 console-api DeployEnvConfig 同形；本包零依赖不反向 import） */
export interface ArtifactEnvConfig {
  name: string;
  artifactDir: string;
  restartCommand?: string;
  artifactPath?: string;
  host?: string;
  user?: string;
}

/**
 * 产物发布 transport（2026-09-05，本版范围：传产物 + 重启，不接 DevOps 平台）：
 * deploy() 同步完成"复制产物目录到目标 + 执行重启命令"，完成后即 done——
 * deploy_wait 首轮轮询即返回 ok；失败抛错 → 工具层 error → 任务项失败停链。
 * local：fs.cp + 本机 exec；配 host：ssh mkdir/scp -r/ssh exec。
 */
export function createArtifactTransport(deps: {
  envs: ArtifactEnvConfig[];
  workspaceDir: string;
  timeoutMs?: number;
}): DeployTransport {
  const timeoutMs = deps.timeoutMs ?? 10 * 60_000;
  const remote = (cfg: ArtifactEnvConfig, cmd: string): string =>
    `ssh ${cfg.user ? `${cfg.user}@` : ''}${cfg.host} "${cmd.replaceAll('"', '\\"')}"`;
  const scpTarget = (cfg: ArtifactEnvConfig): string =>
    `${cfg.user ? `${cfg.user}@` : ''}${cfg.host}:${cfg.artifactDir}`;

  return {
    async deploy({ project, env, version }) {
      const cfg = deps.envs.find((e) => e.name === env);
      if (!cfg) {
        throw new Error(`未配置发布环境: ${env}（可用: ${deps.envs.map((e) => e.name).join(', ') || '无'}）`);
      }
      const id = `deploy-${project}-${env}-${version}-${Date.now()}`;
      const src = join(deps.workspaceDir, cfg.artifactPath ?? 'dist');
      if (cfg.host) {
        await run(`ssh ${cfg.user ? `${cfg.user}@` : ''}${cfg.host} "mkdir -p ${cfg.artifactDir}"`, { timeout: timeoutMs });
        await run(`scp -r ${src} ${scpTarget(cfg)}`, { timeout: timeoutMs });
        if (cfg.restartCommand) await run(remote(cfg, cfg.restartCommand), { timeout: timeoutMs });
      } else {
        await mkdir(cfg.artifactDir, { recursive: true });
        await cp(src, cfg.artifactDir, { recursive: true });
        if (cfg.restartCommand) await run(cfg.restartCommand, { cwd: cfg.artifactDir, timeout: timeoutMs });
      }
      return { id, status: 'done' };
    },
    async getDeploy(id) {
      return { id, status: 'done' };
    },
  };
}
```

`packages/mcp-deploy/src/index.ts` 确认追加 `export * from './artifact-transport.js';`。

- [ ] **Step 7: 跑测试确认通过**（`pnpm -C packages/mcp-deploy test`）

- [ ] **Step 8: 写失败测试——defaultToolsFor 分档**

`default-tools.test.ts` 追加：

```ts
describe('defaultToolsFor builtin/mcp 分档（能力注册表工具集，2026-09-05）', () => {
  const forge = { provider: 'gitea' as const, baseUrl: 'http://localhost:3000', token: 'pat-x' };
  const deployCfg = { envs: [{ name: 'test', artifactDir: '/tmp/x' }] };

  it('缺省（不传 builtin/mcp）行为零回归：bash+files+forge（配了 forge 时），无 deploy 工具', () => {
    const tools = defaultToolsFor('/tmp/ws-x', { forge });
    const names = tools.list().map((t) => t.name);
    expect(names).toContain('bash');
    expect(names.some((n) => n.startsWith('gitlab_') || n.startsWith('gitea_'))).toBe(true);
    expect(names).not.toContain('deploy_to_env');
  });

  it('mcp:[] 时即便配了 forge 也不注入 forge 工具（dev/test 项最小工具集）', () => {
    const tools = defaultToolsFor('/tmp/ws-x', { forge, mcp: [] });
    expect(tools.list().map((t) => t.name).some((n) => n.startsWith('gitlab_') || n.startsWith('gitea_'))).toBe(false);
  });

  it('mcp:["deploy"] 注册 deploy 工具（须同时传 deploy 配置）', () => {
    const tools = defaultToolsFor('/tmp/ws-x', { deploy: deployCfg, mcp: ['deploy'] });
    expect(tools.list().map((t) => t.name)).toContain('deploy_to_env');
  });

  it('builtin:["files"] 不注册 bash（devops 项无裸 shell）', () => {
    const tools = defaultToolsFor('/tmp/ws-x', { builtin: ['files'] });
    expect(tools.list().map((t) => t.name)).not.toContain('bash');
  });
});
```

- [ ] **Step 9: 实现 defaultToolsFor 分档**

`default-tools.ts` 改造（签名与行为兼容）：

```ts
import { createDeployTools, createArtifactTransport } from '@ddw/mcp-deploy';
import type { DeployConfig } from './runtime-config.js';

export function defaultToolsFor(
  wsDir: string,
  opts: {
    bashWhitelist?: string[]; backend?: SandboxBackend;
    forge?: ForgeConfig; defaultRepo?: string;
    /** 发布部署配置（mcp 含 'deploy' 时须传） */
    deploy?: DeployConfig;
    /** 内置能力分档（缺省 ['bash','files'] = 现状）；mcp 工具包分档（缺省 ['forge'] = 现状） */
    builtin?: string[]; mcp?: string[];
  } = {},
): ToolRegistry {
  const builtin = opts.builtin ?? ['bash', 'files'];
  const mcp = opts.mcp ?? ['forge'];
  const tools = new ToolRegistry();
  if (builtin.includes('files')) {
    for (const t of createWorkspaceTools(new LocalWorkspace(wsDir))) tools.register(t);
  }
  if (builtin.includes('bash')) {
    tools.register(createBashTool(new ControlledBash({
      root: wsDir,
      whitelist: opts.bashWhitelist ?? ['git', 'node', 'npm', 'npx', 'python3'],
      ...(opts.backend ? { backend: opts.backend } : {}),
    })));
  }
  if (mcp.includes('forge') && opts.forge) {
    // …现有 forge 注册代码原样保留
  }
  if (mcp.includes('deploy') && opts.deploy) {
    const transport = createArtifactTransport({ envs: opts.deploy.envs, workspaceDir: wsDir });
    for (const t of createDeployTools({ transport })) tools.register(t);
  }
  return tools;
}
```

- [ ] **Step 10: 全量测试 + 提交**

Run: `pnpm -C packages/console-api test && pnpm -C packages/mcp-deploy test`
Expected: 全绿

```bash
git add packages/console-api/src packages/console-api/test packages/mcp-deploy/src packages/mcp-deploy/test
git commit -m "feat(deploy): 产物发布接入执行链——yaml deploy 段 + ArtifactTransport + defaultToolsFor 分档"
```

---

### Task 4: 计划执行器与接线（TaskRecord.progress + runner + executor/worker/scheduler + resume API）

**Files:**
- Modify: `packages/console-api/src/stores/types.ts`（TaskRecord 扩展 + TaskStore.finish 签名 + resumePlan）
- Modify: `packages/console-api/src/stores/file-task-store.ts`、`packages/console-api/src/stores/sqlite-task-store.ts`
- Create: `packages/console-api/src/team/plan-runner.ts`
- Modify: `packages/console-api/src/team/executor.ts`（plan 分流 + capabilities/deploy 注入）
- Modify: `packages/console-api/src/team/worker.ts`（WorkerJob.config 增 capabilities/deploy；plan 分流）
- Modify: `packages/console-api/src/team/process-executor.ts`（config 透传，无逻辑变化）
- Modify: `packages/console-api/src/team/pipeline.ts`（capabilities/deploy 进 executor 组装）
- Modify: `packages/console-api/src/team/scheduler.ts`（progress 透传 finish）
- Modify: `packages/console-api/src/http/handlers.ts`（POST /api/tasks/:id/resume；GET /api/tasks 摘要带 planProgress）
- Modify: `packages/console-api/src/team/runtime-config.ts`（pipeline 透传 deploy/capabilities 由 server 注入，此处零改动——确认即可）
- Test: `packages/console-api/test/plan-runner.test.ts`（新建）、stores/executor/scheduler/handlers 测试文件追加

**Interfaces:**
- Consumes: Task 1 `PlanItem`/`buildPlanItemInstruction`/`resumeWith`；Task 2 `CapabilityStore`/`CapabilityDef`；Task 3 `DeployConfig`/`defaultToolsFor` 分档。
- Produces:
  - `TaskRecord` 追加 `planProgress?: PlanProgress[]; failedItemId?: string`；`interface PlanProgress { itemId: string; kind: string; title: string; status: 'done' | 'failed' | 'skipped' }`
  - `TaskStore.finish(taskId, outcome, ok, progress?, failedItemId?)`；`TaskStore.resumePlan(taskId): Promise<TaskRecord>`（仅 failed 可 resume → pending，保留 progress，清 failedItemId）
  - `runTaskPlan(input: PlanRunInput): Promise<EmployeeOutcome>`（plan-runner.ts 导出；`EmployeeOutcome` 增加可选字段 `planProgress?: PlanProgress[]; failedItemId?: string`——**改 runtime 的类型定义** `packages/runtime/src/types.ts` 中 `EmployeeOutcome`，done/max_turns 分支均可携带）
  - `EmployeeExecutor` 入参追加可选 `progress?: PlanProgress[]`
  - handlers：`POST /api/tasks/:id/resume`（仅 failed；409 其他状态）；`GET /api/tasks` 摘要追加 `planProgress`/`failedItemId`

- [ ] **Step 1: runtime 类型扩展（先行小步）**

`packages/runtime/src/types.ts` 的 `EmployeeOutcome` 扩展：

```ts
/** 计划进度留痕（计划执行器逐项回写） */
export interface PlanProgress {
  itemId: string;
  kind: string;
  title: string;
  status: 'done' | 'failed' | 'skipped';
}

export type EmployeeOutcome =
  | { status: 'done' | 'max_turns'; reply: string; turns: number;
      /** 计划模式：逐项进度与停点（无 plan 时缺省） */
      planProgress?: PlanProgress[]; failedItemId?: string };
```

`index.ts` 确认桶导出 `PlanProgress`。跑 `pnpm -C packages/runtime test`（类型兼容，零行为变化）并提交：

```bash
git add packages/runtime/src
git commit -m "feat(runtime): EmployeeOutcome 携带计划进度 planProgress/failedItemId"
```

- [ ] **Step 2: 写失败测试——store 进度与 resumePlan**

stores 测试文件追加（file/sqlite 两个实现都测；照抄既有参数化写法）：

```ts
it('finish 携带 planProgress/failedItemId 落库；resumePlan 仅 failed 可续（→pending，保留 progress，清停点）', async () => {
  await store.add(pkg);
  await store.claim(pkg.taskId, 'emp-01');
  await store.markRunning(pkg.taskId);
  const progress = [
    { itemId: 't1', kind: 'dev', title: '开发', status: 'done' as const },
    { itemId: 't2', kind: 'test', title: '测试', status: 'failed' as const },
    { itemId: 't3', kind: 'commit', title: '提交', status: 'skipped' as const },
  ];
  await store.finish(pkg.taskId, { status: 'done', reply: '测试未过', turns: 3 }, false, progress, 't2');
  let rec = await store.get(pkg.taskId);
  expect(rec?.planProgress).toEqual(progress);
  expect(rec?.failedItemId).toBe('t2');
  expect(rec?.status).toBe('failed');

  const resumed = await store.resumePlan(pkg.taskId);
  expect(resumed.status).toBe('pending');
  expect(resumed.planProgress).toEqual(progress);   // 保留：续跑跳过 done 项
  expect(resumed.failedItemId).toBeUndefined();

  await expect(store.resumePlan(pkg.taskId)).rejects.toThrow(/仅 failed 任务可续跑/);
});
```

- [ ] **Step 3: 实现 store 扩展**

`types.ts`：

```ts
import type { PlanProgress } from '@ddw/runtime';

export interface TaskRecord {
  pkg: TaskPackage;
  status: TaskStatus;
  claimedBy?: string;
  claimedAt?: number;
  result?: EmployeeOutcome;
  /** 计划模式：逐项进度（续跑时 done 项不重做） */
  planProgress?: PlanProgress[];
  /** failed 时停在哪一项 */
  failedItemId?: string;
}
```

`TaskStore.finish` 签名改为 `finish(taskId: string, outcome: EmployeeOutcome, ok: boolean, progress?: PlanProgress[], failedItemId?: string): Promise<TaskRecord>`；接口追加 `resumePlan(taskId: string): Promise<TaskRecord>`。

`file-task-store.ts` finish：

```ts
  async finish(taskId: string, outcome: EmployeeOutcome, ok: boolean, progress?: PlanProgress[], failedItemId?: string): Promise<TaskRecord> {
    return this.mutate(taskId, (rec) => ({
      ...rec,
      status: ok ? 'done' : 'failed',
      result: outcome,
      ...(progress ? { planProgress: progress } : {}),
      ...(failedItemId ? { failedItemId } : {}),
    }));
  }

  /** 计划续跑：仅 failed 可续（→pending，保留 progress，清停点）；其余状态抛错（HTTP 层 409） */
  async resumePlan(taskId: string): Promise<TaskRecord> {
    return this.mutate(taskId, (rec) => {
      if (rec.status !== 'failed') throw new Error(`任务状态为 ${rec.status}，仅 failed 任务可续跑`);
      return { ...rec, status: 'pending', failedItemId: undefined };
    });
  }
```

`sqlite-task-store.ts` finish 的 UPDATE 追加 `plan_progress`、`failed_item_id` 两列（TEXT，JSON 序列化 progress；建表语句同步加列，`CREATE TABLE IF NOT EXISTS` 对存量库以 `ALTER TABLE ... ADD COLUMN` 容错包裹 try/catch）；resumePlan 用 `UPDATE ... WHERE id=? AND status='failed'` 原子判定（changes=0 时读状态抛可读错误）。

- [ ] **Step 4: 跑测试确认通过**

- [ ] **Step 5: 写失败测试——plan-runner**

`packages/console-api/test/plan-runner.test.ts`（核心用例；faux 依赖注入方式照抄 executor/worker 既有测试的 agentFactory stub——stub agent 首轮 prompt 后即调 task_check 工具申报再结束，或直接让 stub outcome done，verify 单独验）：

```ts
import { describe, expect, it } from 'vitest';
import { runTaskPlan } from '../src/team/plan-runner.js';
import { FileCapabilityStore } from '../src/team/capabilities.js';
import type { TaskPackage, PlanItem } from '@ddw/runtime';

// deps 构造：gateway/events/sessions 用既有测试的内存 fake（照抄 executor.test 的做法）
// toolsForItem 注入可观测 fake registry；verify 执行器打桩记录

describe('runTaskPlan（计划执行器，2026-09-05）', () => {
  const pkg: TaskPackage = {
    taskId: 'plan-1', title: '计划任务', repo: { url: 'http://x/a.git', branch: 'main' }, tasks: [],
    plan: [
      { id: 't1', title: '开发', detail: 'd1' },
      { id: 't2', kind: 'test', title: '测试', detail: 'd2', verify: 'exit 0' },
      { id: 't3', kind: 'commit', title: '提交', detail: 'd3' },
    ],
  };

  it('逐项执行：全部通过 → outcome.planProgress 三项 done，无 failedItemId', async () => {
    const input = makeDeps({ verifyResult: 0 });
    const outcome = await runTaskPlan({ pkg, employee: emp, ...input });
    expect(outcome.status).toBe('done');
    expect(outcome.planProgress!.map((p) => [p.itemId, p.status])).toEqual([['t1','done'],['t2','done'],['t3','done']]);
    expect(outcome.failedItemId).toBeUndefined();
  });

  it('失败即停：t2 verify 非零 → t2 failed、t3 skipped，failedItemId=t2', async () => {
    const input = makeDeps({ verifyResult: 1 });
    const outcome = await runTaskPlan({ pkg, employee: emp, ...input });
    expect(outcome.status).toBe('done');            // 员工侧正常结束；链停语义由 progress 表达
    expect(outcome.failedItemId).toBe('t2');
    expect(outcome.planProgress!.map((p) => p.status)).toEqual(['done', 'failed', 'skipped']);
  });

  it('startFrom 续跑：progress 里 t1 done 不重做，从 t2 开始', async () => {
    const input = makeDeps({ verifyResult: 0, executed: [] });
    await runTaskPlan({ pkg, employee: emp, ...input,
      progress: [{ itemId: 't1', kind: 'dev', title: '开发', status: 'done' }] });
    expect(input.executed).toEqual(['t2', 't3']);   // t1 未再次执行
  });

  it('未注册 kind 启动即报错可读（不带病执行）', async () => {
    const bad: TaskPackage = { ...pkg, plan: [{ id: 'x', kind: 'nope', title: 't', detail: 'd' }] };
    await expect(runTaskPlan({ pkg: bad, employee: emp, ...makeDeps({ verifyResult: 0 }) }))
      .rejects.toThrow(/任务项 x\.kind='nope' 未注册或已停用/);
  });

  it('kind 依赖的 MCP 未配置 → 启动即报错（commit 需 forge 配置）', async () => {
    const caps = new FileCapabilityStore(join(dataDir, 'caps.json'));
    await caps.ensureSeed();
    await expect(runTaskPlan({ pkg, employee: emp, ...makeDeps({ verifyResult: 0, forge: undefined }) }))
      .rejects.toThrow(/能力 commit 依赖 forge 工具包，但运行时未配置/);
  });

  it('每项开始/完成/失败 emit 事件留痕（type=report，payload.plan）', async () => {
    const input = makeDeps({ verifyResult: 0, events: collected });
    await runTaskPlan({ pkg, employee: emp, ...input });
    const planEvents = collected.filter((e) => (e.payload as { plan?: unknown }).plan !== undefined);
    expect(planEvents.length).toBeGreaterThanOrEqual(6); // 3 项 × start/done
  });
});
```

- [ ] **Step 6: 实现 plan-runner.ts**

`packages/console-api/src/team/plan-runner.ts`：

```ts
import { exec } from 'node:child_process';
import { randomUUID } from 'node:crypto';
import { promisify } from 'node:util';
import {
  createTaskCheckTool,
  type AgentEvent, type EmployeeOutcome, type EmployeeProfile, type PlanItem, type PlanProgress,
  type ToolRegistry,
} from '@ddw/runtime';
import { assembleDefaultRuntime } from './executor.js';
import { defaultToolsFor, repoPathFromUrl } from './default-tools.js';
import { buildPlanItemInstruction } from '@ddw/runtime';
import type { CapabilityDef } from './capabilities.js';
import type { DeployConfig } from './runtime-config.js';
import type { AgentFactory, CheckGate, EventBus, ModelGateway } from '@ddw/runtime';

const runCmd = promisify(exec);

export interface PlanRunInput {
  pkg: TaskPackage & { plan: PlanItem[] };
  employee: EmployeeProfile;
  workspaceDir: string;
  sessionsRoot: string;
  gateway: ModelGateway;
  events: EventBus;
  checkGate?: (taskId: string) => CheckGate;
  agentFactory?: AgentFactory;
  /** 每项工具集：按 kind 查注册表后组装（executor/worker 各自注入 base 配置） */
  toolsForItem: (item: PlanItem, mcp: string[], builtin: string[]) => ToolRegistry;
  /** 能力定义（启动校验 + kind 缺省判定） */
  capabilities: CapabilityDef[];
  /** deploy 配置（devops 项可用性校验用） */
  deploy?: DeployConfig;
  forge?: unknown; // 仅存在性判断（commit 项校验）
  /** 续跑起点：已有进度（done 项跳过） */
  progress?: PlanProgress[];
}

const kindOf = (item: PlanItem): string => item.kind ?? 'dev';

/** 启动校验：每项 kind 已注册且启用；mcp 依赖的运行时配置在场（fail fast，不带病执行） */
function validatePlan(items: PlanItem[], capabilities: CapabilityDef[], input: PlanRunInput): void {
  for (const item of items) {
    const kind = kindOf(item);
    const def = capabilities.find((c) => c.kind === kind);
    if (!def || !def.enabled) {
      throw new Error(`任务项 ${item.id}.kind='${kind}' 未注册或已停用（能力管理中登记后方可使用）`);
    }
    if (def.tools.mcp.includes('forge') && !input.forge) {
      throw new Error(`能力 ${kind} 依赖 forge 工具包，但运行时未配置 forge 段（代码托管协作）`);
    }
    if (def.tools.mcp.includes('deploy') && !input.deploy) {
      throw new Error(`能力 ${kind} 依赖 deploy 工具包，但运行时未配置 deploy 段（发布部署）`);
    }
  }
}

/**
 * 计划执行器（2026-09-05，spec 任务计划模式）：同一员工同一会话逐项驱动。
 * 每项：kind→工具集 → 员工执行 + task_check 申报 → verify 命令执行器亲自跑 →
 * 通过记 done 继续 / 失败记 failed 即停（后续 skipped）。
 * 返回 outcome 携带 planProgress/failedItemId（调度器 finish 落库）。
 */
export async function runTaskPlan(input: PlanRunInput): Promise<EmployeeOutcome> {
  const items = input.pkg.plan;
  validatePlan(items, input.capabilities, input);
  const done = new Map<string, PlanProgress>();
  for (const p of input.progress ?? []) done.set(p.itemId, p);

  const total = items.length;
  const progress: PlanProgress[] = items.map((it) => done.get(it.id) ?? { itemId: it.id, kind: kindOf(it), title: it.title, status: 'skipped' });
  const firstPending = items.findIndex((it) => done.get(it.id)?.status !== 'done');

  await input.events.emit({
    id: randomUUID(), ts: Date.now(), taskId: input.pkg.taskId, employeeId: input.employee.id,
    type: 'report', summary: `计划执行：共 ${total} 项${firstPending > 0 ? `（续跑，从第 ${firstPending + 1} 项起）` : ''}`,
    payload: { plan: { phase: 'start' } },
  });

  for (let i = firstPending; i < total; i++) {
    const item = items[i]!;
    const kind = kindOf(item);
    const def = input.capabilities.find((c) => c.kind === kind)!;
    await input.events.emit({
      id: randomUUID(), ts: Date.now(), taskId: input.pkg.taskId, employeeId: input.employee.id,
      type: 'report', summary: `计划项 ${i + 1}/${total} 开始：${item.title}`,
      payload: { plan: { phase: 'start', itemId: item.id, kind } },
    });

    const runtime = assembleDefaultRuntime({
      gateway: input.gateway,
      events: input.events,
      tools: input.toolsForItem(item, def.tools.mcp, def.tools.builtin),
      sessionsRoot: input.sessionsRoot,
      employee: input.employee,
      taskId: input.pkg.taskId,
      ...(input.checkGate ? { checkGate: input.checkGate } : {}),
      ...(input.agentFactory ? { agentFactory: input.agentFactory } : {}),
    });

    const doneSummary = progress
      .filter((p) => p.status === 'done')
      .map((p) => `${p.itemId} ${p.title}：已完成`);
    const instruction = buildPlanItemInstruction(input.pkg, item, i, total, doneSummary);
    const checkTool = createTaskCheckTool({
      emit: (e) => input.events.emit({ id: randomUUID(), ts: Date.now(), taskId: input.pkg.taskId, employeeId: input.employee.id, ...e }),
      items: [item],
      ...(input.checkGate && input.employee.supervision?.level === 'shadow' ? { gate: input.checkGate(input.pkg.taskId) } : {}),
    });

    let failed = false;
    let failReason = '';
    try {
      const outcome = i === 0
        ? await runtime.run({ taskId: input.pkg.taskId, instruction, items: [item] })
        : await runtime.resumeWith(input.pkg.taskId, instruction, checkTool);
      // verify：员工申报通过后执行器亲自跑（客观门禁，模型骗不过）
      if (item.verify) {
        try {
          await runCmd(item.verify, { cwd: input.workspaceDir, timeout: 10 * 60_000 });
        } catch (e) {
          failed = true;
          failReason = `验证命令失败: ${item.verify} — ${e instanceof Error ? e.message.split('\n')[0] : String(e)}`;
        }
      }
      if (!failed && outcome.status === 'max_turns') {
        failed = true;
        failReason = `达到轮次上限（max_turns）`;
      }
    } catch (e) {
      failed = true;
      failReason = `执行异常: ${e instanceof Error ? e.message : String(e)}`;
    }

    const entry = progress[i]!;
    if (failed) {
      entry.status = 'failed';
      await input.events.emit({
        id: randomUUID(), ts: Date.now(), taskId: input.pkg.taskId, employeeId: input.employee.id,
        type: 'report', summary: `计划项 ${i + 1}/${total} 失败：${item.title}（${failReason}）`,
        payload: { plan: { phase: 'failed', itemId: item.id, reason: failReason } },
      });
      for (let j = i + 1; j < total; j++) progress[j]!.status = 'skipped';
      return {
        status: 'done', reply: `计划停在第 ${i + 1} 项（${item.title}）：${failReason}`, turns: 0,
        planProgress: progress, failedItemId: item.id,
      };
    }
    entry.status = 'done';
    await input.events.emit({
      id: randomUUID(), ts: Date.now(), taskId: input.pkg.taskId, employeeId: input.employee.id,
      type: 'report', summary: `计划项 ${i + 1}/${total} 完成：${item.title}`,
      payload: { plan: { phase: 'done', itemId: item.id } },
    });
  }

  return { status: 'done', reply: `计划全部完成（${total} 项）`, turns: 0, planProgress: progress };
}
```

实现注意（写代码时核对）：
- `runtime.run` 的 items 参数传 `[item]` 时 task_check 合法项即该项；`resumeWith` 的 extraTools 传同一 `checkTool`（首项之后的项必须仍能申报）。
- `i === 0` 且为续跑（firstPending > 0）时 session 已有历史，也应走 `resumeWith`——实现改为：`const useResume = firstPending > 0 || i > 0;` 用 `useResume ? runtime.resumeWith(...) : runtime.run(...)`。
- EmployeeOutcome 为判别联合，返回对象带 `planProgress` 需 TS 允许——Task 4 Step 1 已扩展。

- [ ] **Step 7: 跑测试确认通过**

- [ ] **Step 8: executor/worker/pipeline/scheduler/handlers 接线**

1. `executor.ts`：`EmployeeExecutor` 入参追加 `progress?: PlanProgress[]`；`EmployeeExecutorOptions` 追加：
```ts
  /** 能力定义提供者（计划模式 per-item 工具集；后台可改，分派时取最新） */
  capabilities?: () => Promise<CapabilityDef[]>;
  /** 发布部署配置（devops 项） */
  deploy?: DeployConfig;
  forge?: ForgeConfig;
```
`createEmployeeExecutor` 执行体改为分流：

```ts
  return async ({ task, employee, progress }) => {
    const wsDir = join(opts.workspaceRoot, employee.id, task.taskId.replaceAll('/', '_'));
    await mkdir(wsDir, { recursive: true });
    await opts.prepareWorkspace?.(wsDir, task, employee);

    if (task.plan) {
      const capabilities = await opts.capabilities?.() ?? [];
      return runTaskPlan({
        pkg: task as TaskPackage & { plan: PlanItem[] },
        employee, workspaceDir: wsDir,
        sessionsRoot: opts.sessionsRoot, gateway: opts.gateway, events: opts.events,
        ...(opts.checkGate ? { checkGate: opts.checkGate } : {}),
        capabilities, ...(opts.deploy ? { deploy: opts.deploy } : {}),
        ...(opts.forge ? { forge: opts.forge } : {}),
        ...(progress ? { progress } : {}),
        toolsForItem: (item, mcp, builtin) => defaultToolsFor(wsDir, {
          bashWhitelist: opts.bashWhitelist,
          ...(opts.backend ? { backend: opts.backend } : {}),   // 注意：EmployeeExecutorOptions 现无 backend——保持现状不传
          ...(opts.forge ? { forge: opts.forge, defaultRepo: repoPathFromUrl(task.repo.url) } : {}),
          ...(opts.deploy ? { deploy: opts.deploy } : {}),
          ...(mcp.length ? { mcp } : { mcp: [] as string[] }),
          ...(builtin.length ? { builtin } : { builtin: [] as string[] }),
        }),
      });
    }
    // …现有非 plan 路径原样（toolsFor 全量工具集 + runtimeFor 分支）
  };
```

2. `worker.ts`：`WorkerJob.config` 追加 `capabilities?: CapabilityDef[]; deploy?: DeployConfig;`；`runJob` 同样分流（toolsForItem 闭包用 config 组装；verify/进度逻辑全在共享的 runTaskPlan 里）。`process-executor.ts` 仅 config 透传（类型自动带过）。

3. `pipeline.ts`：`ConsoleRuntimeOptions` 追加 `capabilities?: () => Promise<CapabilityDef[]>;` 与 `deploy?: DeployConfig;`（deploy 由 runtime yaml 解析结果直接传入——`runtime-config.ts` 的 `ConsoleRuntimeConfig` 已有）；inproc executor 组装处与 fork `createProcessExecutor` 的 config 都带上 `deploy`；capabilities provider 传 `opts.capabilities`。

4. `scheduler.ts` `runOne`：executor 返回后：

```ts
      const result = await this.deps.executor({ task: rec.pkg, employee, ...(rec.planProgress ? { progress: rec.planProgress } : {}) });
      ok = result.status === 'done' && result.failedItemId === undefined;
      await this.deps.tasks.finish(taskId, result, ok, result.planProgress, result.failedItemId);
```

（executor 类型签名 `Promise<EmployeeOutcome>` 已含扩展字段。）

5. `handlers.ts`：`GET /api/tasks` 摘要映射追加 `planProgress: rec.planProgress`（局部变量解构处取）、`failedItemId: rec.failedItemId`；新增路由（`/api/tasks/:id` 段内）：

```ts
        if (method === 'POST' && seg[3] === 'resume') {
          try {
            return ok(await deps.tasks.resumePlan(taskId));
          } catch (e) {
            return err(409, e instanceof Error ? e.message : String(e));
          }
        }
```

6. `server.ts`：`createRuntimePipeline` 的 `opts.runtime` 组装处追加 `capabilities: () => capabilitiesStore.list()`（capabilitiesStore 为 Task 2 构造的 FileCapabilityStore）与 `...(runtime.deploy ? { deploy: runtime.deploy } : {})`；fork 分支 `createProcessExecutor` 的 config 追加同两项（capabilities 在 fork 下取快照：`await capabilitiesStore.list()` 于 pipeline 组装时）——**fork 快照语义**：worker 收到的是分派时刻的定义，后台改动对后续分派生效。

- [ ] **Step 9: 写失败测试——resume API 与 scheduler 透传**

handlers/scheduler 测试追加：

```ts
it('POST /api/tasks/:id/resume：failed → pending（409 其他状态）；前端可再次触发调度', async () => {
  // 造 failed 记录后：
  const res = await handle({ method: 'POST', path: `/api/tasks/${taskId}/resume`, body: {} });
  expect(res.status).toBe(200);
  expect((res.json as { status: string }).status).toBe('pending');
  expect((await handle({ method: 'POST', path: `/api/tasks/${taskId}/resume`, body: {} })).status).toBe(409);
});

it('调度器 finish 透传 planProgress/failedItemId；续跑任务重新分派时 executor 收到 progress', async () => {
  // faux executor 记录入参；第一轮让 t2 失败（outcome.failedItemId='t2'）→ finish 落库
  // resume 后第二轮 tick 断言 executor 收到 progress 且 t1 未重跑（由 runner 用例保证，此处验接线）
});
```

- [ ] **Step 10: 全量测试 + 提交**

Run: `pnpm -C packages/console-api test && pnpm -C packages/runtime test`
Expected: 全绿

```bash
git add packages/console-api/src packages/console-api/test packages/runtime/src
git commit -m "feat(console-api): 计划执行器——逐项驱动/verify 客观门禁/失败即停/续跑 API 全链接线"
```

---

### Task 5: 管理端前端（能力管理页 + 任务创建双模式 + 计划进度/续跑）

**Files:**
- Modify: `apps/console/src/api.ts`（capabilities CRUD + resumeTask + 类型）
- Create: `apps/console/src/task-form.ts`（表单 ↔ 任务包 yaml 纯逻辑，含计划项）
- Create: `apps/console/src/views/Capabilities.vue`、`apps/console/src/components/TaskCreateDialog.vue`、`apps/console/src/components/PlanProgress.vue`
- Modify: `apps/console/src/App.vue`（导航 + 视图注册，照现有视图接线方式）、`apps/console/src/components/TaskPool.vue`（创建入口接 TaskCreateDialog）、`apps/console/src/components/TaskDetail.vue`（PlanProgress + 续跑按钮）
- Test: `apps/console/test/task-form.test.ts`（新建）、`apps/console/test/capabilities.test.ts`（新建，store 逻辑）、组件测试按既有模式追加

**Interfaces:**
- Consumes: Task 2/4 的 API（`/api/capabilities` CRUD、`POST /api/tasks/:id/resume`、`GET /api/tasks` 摘要的 `planProgress`/`failedItemId`）。
- Produces: `task-form.ts` 导出 `planFormToYaml(form: TaskFormState): string`、`yamlToPlanForm(yaml: string): TaskFormState`（yaml 不含 plan 时 `plan: []`）、`TaskFormState` 类型。

**实现前必读**：`apps/console/src/components/TaskPool.vue` 与 `App.vue` 的既有写法（element-plus 表格/对话框/路由或 tab 接线方式），新组件严格沿用同一模式。

- [ ] **Step 1: 写失败测试——task-form 双向转换**

`apps/console/test/task-form.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import { planFormToYaml, yamlToPlanForm } from '../src/task-form.js';

describe('task-form（任务创建双模式，2026-09-05）', () => {
  const form = {
    taskId: 'feat-login', title: '登录功能开发', role: 'backend',
    repoUrl: 'http://localhost:3000/demo/web-app.git', branch: 'develop',
    plan: [
      { id: 't1', kind: '', title: '开发登录接口', detail: '实现 POST /api/login', verify: '' },
      { id: 't2', kind: 'commit', title: '提交并建 MR', detail: '提交 feature 分支并建 MR', verify: 'npm run build' },
    ],
  };

  it('planFormToYaml：空 kind/verify 字段不输出；生成 yaml 可被后端解析（含 plan）', () => {
    const yaml = planFormToYaml(form);
    expect(yaml).toContain('plan:');
    expect(yaml).toContain('kind: commit');
    expect(yaml).not.toContain('kind:');       // 空缺省不写（除 commit 那行外无其他 kind:）
    expect(yaml.split('kind:').length - 1).toBe(1);
    expect(yaml).not.toContain('verify: \'\'');
  });

  it('yamlToPlanForm：yaml（含 plan）反填表单；不含 plan 的 yaml → plan: []（老包走 yaml 模式展示）', () => {
    const back = yamlToPlanForm(planFormToYaml(form));
    expect(back.taskId).toBe('feat-login');
    expect(back.plan).toHaveLength(2);
    expect(back.plan[1]!.kind).toBe('commit');
    expect(back.plan[0]!.kind).toBe('');
    expect(yamlToPlanForm('taskId: t\ntitle: x\nrepo: { url: "http://x/a.git", branch: main }\ntasks: []\n').plan).toEqual([]);
  });
});
```

- [ ] **Step 2: 实现 task-form.ts**

`apps/console/src/task-form.ts`（前端 yaml 序列化用 `yaml` 包：`pnpm -C apps/console add yaml`）：

```ts
import { parse as parseYaml, stringify as stringifyYaml } from 'yaml';

export interface PlanFormRow { id: string; kind: string; title: string; detail: string; verify: string; }
export interface TaskFormState {
  taskId: string; title: string; role: string;
  repoUrl: string; branch: string;
  plan: PlanFormRow[];
}

const clean = (s: string): string | undefined => (s.trim() ? s.trim() : undefined);

/** 表单 → 任务包 yaml（空 kind/verify 不输出，后端缺省 dev） */
export function planFormToYaml(form: TaskFormState): string {
  const doc: Record<string, unknown> = {
    taskId: form.taskId.trim(),
    title: form.title.trim(),
    repo: { url: form.repoUrl.trim(), branch: form.branch.trim() },
    plan: form.plan
      .filter((r) => r.id.trim() && r.title.trim())
      .map((r) => ({
        id: r.id.trim(),
        ...(clean(r.kind) ? { kind: clean(r.kind) } : {}),
        title: r.title.trim(),
        detail: r.detail.trim(),
        ...(clean(r.verify) ? { verify: clean(r.verify) } : {}),
      })),
    ...(clean(form.role) ? { role: clean(form.role) } : {}),
  };
  return stringifyYaml(doc);
}

/** 任务包 yaml → 表单（不含 plan 的老包 plan: []，仍以 yaml 模式提交零损失） */
export function yamlToPlanForm(yaml: string): TaskFormState {
  const raw = parseYaml(yaml) as Record<string, unknown>;
  const repo = (raw?.['repo'] ?? {}) as Record<string, unknown>;
  const plan = Array.isArray(raw?.['plan']) ? (raw['plan'] as Record<string, unknown>[]) : [];
  return {
    taskId: String(raw?.['taskId'] ?? ''),
    title: String(raw?.['title'] ?? ''),
    role: String(raw?.['role'] ?? ''),
    repoUrl: String(repo['url'] ?? ''),
    branch: String(repo['branch'] ?? ''),
    plan: plan.map((p) => ({
      id: String(p['id'] ?? ''),
      kind: String(p['kind'] ?? ''),
      title: String(p['title'] ?? ''),
      detail: String(p['detail'] ?? ''),
      verify: String(p['verify'] ?? ''),
    })),
  };
}
```

- [ ] **Step 3: 跑测试确认通过**（`pnpm -C apps/console test -- task-form`）

- [ ] **Step 4: api.ts 扩展**

```ts
export interface CapabilityDef {
  kind: string; name: string; description?: string;
  tools: { builtin: string[]; mcp: string[] };
  enabled: boolean;
}
export interface PlanProgress { itemId: string; kind: string; title: string; status: 'done' | 'failed' | 'skipped'; }
// TaskSummary / TaskDetailRecord 追加可选 planProgress?: PlanProgress[]; failedItemId?: string;

// api 对象追加：
  listCapabilities(): Promise<CapabilityDef[]> { return request('GET', '/api/capabilities'); },
  createCapability(def: CapabilityDef): Promise<{ kind: string }> { return request('POST', '/api/capabilities', def); },
  updateCapability(kind: string, def: CapabilityDef): Promise<{ kind: string }> { return request('PUT', `/api/capabilities/${kind}`, def); },
  deleteCapability(kind: string): Promise<{ kind: string }> { return request('DELETE', `/api/capabilities/${kind}`); },
  resumeTask(taskId: string): Promise<{ taskId: string; status: string }> { return request('POST', `/api/tasks/${taskId}/resume`, {}); },
```

- [ ] **Step 5: Capabilities.vue（能力管理页）**

沿用 `EmployeeRoster.vue` 的表格 + 对话框模式：表格列（kind/name/工具来源[builtin+mcp 徽标]/启停 switch → 调 updateCapability/删除按钮 → deleteCapability）+「新增能力」对话框（kind/name/description/工具来源多选 el-checkbox-group：bash、files、forge、deploy/enabled switch）。提交错误 `ElMessage.error(err.message)`。

- [ ] **Step 6: TaskCreateDialog.vue（双模式创建）**

- `el-radio-group` 模式切换：`表单配置` / `yaml 直贴`（yaml 直贴 = 现有 TaskPool 创建逻辑迁入：textarea + 提交 `api.createTask(yaml)`）；
- 表单模式：基本信息（taskId/title/role/repoUrl/branch）+ 计划项 `el-table` 可编辑行（增/删/上移下移），kind 列下拉选项 = `api.listCapabilities()` 中 `enabled` 项 + 空选项「(缺省 dev)」；
- 底部「生成 yaml 预览」（调 planFormToYaml 只读展示）与「从 yaml 反填」（yamlToPlanForm）——两模式互导；
- 提交统一走 `api.createTask(yaml)`（表单模式先 planFormToYaml），后端 400 错误原样展示。

- [ ] **Step 7: PlanProgress.vue + TaskDetail 接线**

`PlanProgress.vue`：props `progress?: PlanProgress[]`、`failedItemId?: string`；时间线/徽标展示（done=success、failed=danger 高亮停点、skipped=info 待执行）。`TaskDetail.vue` 在状态区追加 `<PlanProgress ... />`，`status==='failed' && failedItemId` 时显示「从失败项续跑」按钮 → `api.resumeTask(taskId)` 成功后 `ElMessage.success` + 刷新详情。

- [ ] **Step 8: App.vue 导航 + TaskPool 创建入口替换**

- [ ] **Step 9: 全量测试 + 提交**

Run: `pnpm -C apps/console test && pnpm -C apps/console exec vue-tsc --noEmit`（若该命令为项目既有 typecheck 方式；以 package.json scripts 为准）
Expected: 全绿

```bash
git add apps/console
git commit -m "feat(console): 能力管理页 + 任务创建双模式（表单/yaml 互导）+ 计划进度与续跑"
```

---

### Task 6: 示例配置 + 总账登记

**Files:**
- Modify: `examples/console-runtime.example.yaml`（deploy 段示例 + 注释）
- Create: `examples/demo/task-plan-demo.yaml`（计划模式任务包示例：dev→test→commit→devops 四项）
- Modify: `docs/阶段成果总账.md`（追加一节）
- Test: 无新测试（文档任务，跑全量回归即可）

- [ ] **Step 1: example yaml 追加 deploy 段**

`examples/console-runtime.example.yaml` 追加（注释说明各字段）：

```yaml
# 发布部署（可选）：devops 能力项依赖本段；本版范围 = 产物复制到目标目录 + 执行重启命令
# deploy:
#   envs:
#     - name: test                       # 任务项 detail 中引用的环境名
#       artifactDir: /opt/ddw/web-app    # 产物目标目录
#       restartCommand: "bash /opt/ddw/restart.sh"   # 发布后执行（重启服务/脚本）
#       artifactPath: dist               # 员工工作区内产物相对路径（缺省 dist）
#       # host: 127.0.0.1                # 配了走 ssh（scp/ssh exec），缺省本机直拷
#       # user: root
```

- [ ] **Step 2: 计划模式任务包示例**

`examples/demo/task-plan-demo.yaml`：

```yaml
# 计划模式任务包示例（2026-09-05）：单员工任务链 dev→test→commit→devops
# plan 与 tasks 互斥；kind 查能力注册表（缺省 dev）；verify 由执行器亲自运行核对
taskId: demo-plan-login
title: 登录功能全链交付
repo: { url: "http://localhost:3000/demo/web-app.git", branch: develop, baseBranch: main }
role: backend
plan:
  - id: t1
    title: 开发登录接口与页面
    detail: 实现 POST /api/login 与前端登录表单，含单元测试
    verify: npm run build
  - id: t2
    kind: test
    title: e2e 测试登录流程
    detail: 覆盖正确/错误密码、锁定策略
    verify: npm run e2e
  - id: t3
    kind: commit
    title: 提交代码并建 MR
    detail: 提交 feature/demo-login 分支并建 MR 至 develop
  - id: t4
    kind: devops
    title: 发布到测试环境
    detail: 部署 test 环境并确认部署完成
```

- [ ] **Step 3: 总账登记 + 回归**

`docs/阶段成果总账.md` 追加「### P16 补充 5 · 任务计划与能力注册表（2026-09-05）」：交付内容（计划执行器/能力注册表/deploy 接入/管理端双模式）、关键决策（失败即停可续跑、kind 声明式绑定、verify 客观门禁）、测试数字。

Run: `pnpm -C packages/runtime test && pnpm -C packages/console-api test && pnpm -C packages/mcp-deploy test && pnpm -C apps/console test`
Expected: 全绿

```bash
git add examples docs
git commit -m "docs(examples): 计划模式任务包示例 + deploy 段示例 + 总账登记"
```

---

## Self-Review 记录

- **Spec coverage**：plan 字段与互斥（Task 1）、三层状态判定（Task 1 task_check + Task 4 verify/闸门）、能力注册表后台配置（Task 2）、四类预置（Task 2 seed）、mcp-deploy 收窄范围（Task 3）、失败即停/续跑（Task 4）、任务创建双模式 + 能力管理页 + 进度/续跑（Task 5）、示例与总账（Task 6）。spec §5 错误处理表逐行有落点（400 校验 Task 2、运行中改注册表 Task 4 每项查表、MCP 未配置 fail fast Task 4 validatePlan、deploy failed 映射 Task 3、事件留痕 Task 4）。
- **Placeholder scan**：无 TBD/TODO；Task 5 的 .vue 组件步骤为"沿用既有组件模式 + 明确行为清单"——组件视觉代码以实现前必读的既有文件为准，逻辑层（task-form.ts）代码完整。
- **Type consistency**：`PlanProgress` 定义在 runtime types（Task 4 Step 1），stores/runner/frontend 三处引用同名同形；`CapabilityDef` 在 console-api 定义、前端 api.ts 独立同形声明（前端不 import 后端类型，与现有 TaskSummary 模式一致）；`DeployEnvConfig`（console-api）与 `ArtifactEnvConfig`（mcp-deploy）同形不互引（mcp-deploy 零依赖约束）。
