# P7 · 二期第 1 步：数据面中间层与 SQLite（2026-09-05）

**Goal**：落实 spec 5.2「存储中间层定稿」——业务端只依赖 `TaskStore`/`EventStore` 接口，文件实现降级为测试 Fake，新增 SQLite 实现（`node:sqlite` 内置驱动，零依赖），事件/审计落库带 hash 链（不可篡改可验证）。**换库 = 组装处注入一行，业务端零改造。**

**决策依据（用户已拍板）**：D1 = SQLite 起步 + 中间层；D2 试点期单进程多 runtime；D3 依赖声明 yaml `dependsOn` 起步；D4 数据库化最先。

## Task 1: Store 接口抽取 + contract 测试基建

**Files:**
- Create: `packages/console-api/src/stores/types.ts`（`TaskStore`/`EventStore` 接口，从现有类方法签名抽出）
- Rename: `task-store.ts` → `file-task-store.ts`（`class FileTaskStore implements TaskStore`）；`event-store.ts` → `file-event-store.ts`（同法）
- Modify: 全部 import 处（handlers/server/测试）机械替换
- Create: `test/stores.contract.ts`——**同一组契约用例跑任意实现**（工厂注入），文件实现先跑通

**契约用例集**：add/get/list、claim 状态机（pending→claimed→running→done、重复 claim 拒绝）、finish 回写、append/list 过滤（taskId/employeeId/type/since）、半行容错（文件实现特有→挪出契约）、同毫秒 since 语义（>=）。
- [ ] 失败测试 → 改造 → 全绿 → 提交

## Task 2: SQLite 实现（node:sqlite）

**Files:**
- Create: `packages/console-api/src/stores/sqlite/schema.ts`（建表：task/event/audit_log；WAL 模式）
- Create: `sqlite-task-store.ts` / `sqlite-event-store.ts`（implements 同一接口）
- Modify: `test/stores.contract.ts` 对 Sqlite 实现跑同一契约集

**Schema 要点**：
- `task(id TEXT PK, pkg TEXT, status, claimed_by, claimed_at, result TEXT, updated_at)`——pkg/result 存 JSON 文本
- `event(seq INTEGER PK AUTOINCREMENT, id TEXT UNIQUE, ts, task_id, employee_id, type, summary, payload TEXT, prev_hash, hash)`
- 接单原子性：`UPDATE ... WHERE status='pending'` 检查受影响行数（替代文件实现的状态检查）
- [ ] 契约全过 → 提交

## Task 3: 审计 hash 链

**Files:**
- Modify: `sqlite-event-store.ts`（append 时计算 `prev_hash`/`hash` = sha256(content + prev_hash)，创世 prev_hash = 64 个 '0'）
- Create: `sqlite/verify.ts`：`verifyChain(store)` → `{ ok, brokenAt? }`
- Modify: handlers `GET /api/audit` 返回体加 `integrity: { ok, verifiedUpTo }`
- Test: 篡改中间一条 → verify 报 brokenAt；全量追加 → ok
- [ ] 提交

## Task 4: 组装处注入 + 全链路 e2e 双实现验证

**Files:**
- Modify: `src/http/server.ts`（`startConsoleServer` 接受 `store?: 'sqlite' | 'file'`，默认 sqlite；CLI 加 `--store` 参数）
- Modify: `test/e2e-full-chain.test.ts` 增加 sqlite 变体（同一 Faux 脚本跑第二遍，断言事件/审计/SSE 等价 + integrity ok）
- [ ] 全量回归 → 提交 → 总账登记

## P7 验收标准

1. `pnpm -r test` 全绿 + typecheck 干净，全离线不占端口；业务端（handlers/e2e/前端）代码中不出现任何具体存储实现的 import（只有接口）
2. 同一组 contract 用例覆盖 File 与 Sqlite 两实现；claim 状态机在 SQLite 下靠 UPDATE WHERE 保证原子
3. SQLite append 带哈希链；篡改可被 verifyChain 发现；/api/audit 暴露 integrity
4. startConsoleServer/CLI 一行切换存储实现；全链路 e2e 两种实现等价通过
5. 零新增 npm 依赖（node:sqlite 内置）
