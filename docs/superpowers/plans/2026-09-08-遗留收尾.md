# 遗留收尾批次 实施计划（2026-09-08）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 清掉多岗位/Skill 管理等特性收官时登记的全部可代码化遗留项——新旧调度语义一致性、员工档案卫生、竞态防护、前端功能收尾、skillCategories 退役、蒸馏去重。

**Architecture:** 八个独立小任务，后端一致性（T1-T4）→ 前端收尾（T5-T7）→ Skill 模块（T8）。每任务一次 commit，互不依赖可按序独立实施。

**Tech Stack:** TypeScript / Node（pnpm workspace）、vitest、Vue3 + element-plus + Tailwind v4。

## Global Constraints

- 全程简体中文注释与文案；conventional commits 中文描述
- runtime 包改动仅限 Task 2 的 EmployeeRoster（yaml 回退路径），其余包不动
- 不改 `packages/console-api/data/` 运行数据
- 每任务收尾：`pnpm -r typecheck` 零错误 + 相关包 vitest 全绿
- 行为变化必须有测试锁死（TDD：先红后绿）

---

### Task 1: e2e 补 ManagedRoster 调度路径用例

**Files:** Modify: `packages/console-api/test/`（新增或扩展 e2e 用例，优先扩展现有 e2e 文件）

**要求：** 现有 e2e（e2e-console / e2e-fork / 班组 e2e）未覆盖 ManagedRoster 的岗位匹配调度链。补一条 e2e：装配 ManagedRoster（员工档案 roles 多岗）→ 发布带岗位的任务包 → 断言按「pkg.role ∈ emp.roles」分派到正确员工；含一条失配（岗位无人接 → waiting/排队）断言。用现有测试夹具模式，不引入新依赖。

### Task 2: runtime EmployeeRoster（yaml 回退路径）调度语义对齐

**Files:** Modify: `packages/runtime/src/`（EmployeeRoster 及其消费点）；Test: 对应 runtime 测试

**要求：** 调查 runtime 内 EmployeeRoster 的岗位匹配现状（多岗位/岗位管理两轮改造后仍按旧 skills 集合匹配，与 ManagedRoster 的 `roles.includes` 语义分叉）。对齐原则：EmployeeProfile 无 roles 概念（runtime 零改动约束下 profile.role 为单值），回退路径按 **role 精确匹配**对齐（与 ManagedRoster 单岗行为一致），移除 skills 匹配分叉；yaml 回退语义变化须在代码注释与测试中留痕。若调查后发现 EmployeeRoster 在装配层已不被消费（死路径），可改为明确标注退役并保持行为不变——以调查结论为准，报告里说明。

### Task 3: 员工档案卫生——脏档警告日志 + trim 归一

**Files:** Modify: `packages/console-api/src/team/employee-store.ts`；Test: `test/employee-store.test.ts`

**要求：**
1. `load()` 归一后若存在 `roles` 为空数组的记录（旧脏档），输出一次性警告日志（console.warn，中文，含员工 id/name），不抛错不阻断启动；
2. `normalizeEmployeeInput` 与 `normalizeStored` 对 roles 逐项做 `trim()`（trim 后为空的项剔除；剔除后为空数组保持原逻辑——输入路径由校验拒绝、load 路径触发警告）；
3. 调度点 `assignee` 与任务包 `role` 存值 trim（消费侧入口处归一，一处收口即可，说明收口点选择理由）。

### Task 4: 运行中重提交同 taskId 竞态防护

**Files:** Modify: `packages/console-api/src/`（任务创建/store 的 add 入口）；Test: 对应测试

**要求：** 任务重跑特性收官时登记：运行中（running/in_progress 等活跃态）重新提交同 taskId 目前不校验状态直接 add。补防护：同 taskId 已存在且处于活跃态 → 400（中文错误，提示先等完成或走 resume）；终态（done/failed/blocked/cancelled）→ 允许覆盖（保持重跑语义）。状态集以代码内现有枚举为准。

### Task 5: TaskDag 编排视图恢复入口

**Files:** Modify: `apps/console/src/`（TaskDag.vue 与导航/任务中心入口）；Test: 相关前端测试

**要求：** TaskDag.vue 因样式重构被有意封存（文件头 TODO：编排视图缓发）。恢复：任务中心/任务详情提供入口（入口形态对齐现有 UI 惯例），TaskDag 样式适配 Tailwind v4 体系（对齐任务中心风格），功能不回归（DAG 分层展示、状态联动）。若 TaskDag 现实现依赖已废弃的接口/组件，按最小改造接回。

### Task 6: 任务中心状态筛选 + 菜单徽标

**Files:** Modify: `apps/console/src/views/TaskCenter.vue`、`apps/console/src/App.vue`（或消息铃铛组件）；Test: task-center 相关测试

**要求：**
1. 任务中心增加状态筛选（对齐现有检索区风格；状态枚举与 TASK_STATUS_TAG 单点映射一致）；
2. 消息铃铛未读数徽标（数据源用现有 messages API，遵循抽屉打开期间漏弹修复的既有语义；为 0 不显示）。
两项各自独立测试锁死。

### Task 7: skillCategories 退役字段下线

**Files:** Modify: `packages/console-api/src/http/handlers.ts`（GET 视图）、`apps/console/src/api.ts` 及前端消费点；Test: handlers.test.ts + 前端相关测试

**要求：** 前置调查前端对 GET 员工视图 `skillCategories` 的全部消费点，逐一改除（或确认无消费）后：handlers 员工视图两分支移除 `skillCategories` 下发；api.ts 类型同步移除；employee-store 中该退役字段的结构兼容（读忽略）保留不动。全程 grep 确认无残留引用。

### Task 8: Skill distiller 跨任务去重

**Files:** Modify: `packages/console-api/src/team/`（distiller 相关）；Test: skill-distiller.test.ts

**要求：** 同一经验在多个任务完成后会重复蒸馏出语义相同的候选。实现轻量去重：新候选入库前与同岗位/分类下现有 pending（及 approved？以调查现状为准，报告说明取择）候选做相似判断——用便宜可靠的口径（如标题/要点归一化后的精确或包含匹配，**不引入 embedding 依赖**）；命中重复则跳过入库（消息/日志留痕「重复跳过」）。阈值宁松勿严：宁可漏过去重不可误杀新知识。
