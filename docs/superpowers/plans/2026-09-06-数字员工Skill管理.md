# 数字员工 Skill 管理模块 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建成数字员工公用 Skill 库：分类管理、人工终审入库、任务完成自动蒸馏沉淀候选、员工按分类关联并在执行任务时注入。

**Architecture:** 沿用 capabilities/employees 的 JSON 文件 store 模式（`FileSkillStore`，单文件 `skills.json` 存分类+skill）。执行链注入在 plan-runner（指令追加 + asset 落盘 `.skills/`），fork 模式走分派时快照随 job 下发。蒸馏器（distiller）在调度器 finish 成功后异步触发，模型产出候选 skill 以 pending 入库并推消息中心待人工审查。

**Tech Stack:** TypeScript（pnpm monorepo）、vitest、Vue 3 + Element Plus + Tailwind（前端）、node:http 纯函数 handler 层。

**Spec:** `docs/superpowers/specs/2026-09-06-数字员工Skill管理-design.md`

## Global Constraints

- 语言：所有注释、提交信息、错误文案用中文；提交信息 conventional commits（feat/test/fix/style/docs）+ 结尾 `Co-Authored-By: Claude Code <noreply@anthropic.com>`
- 测试命令：后端 `cd packages/console-api && pnpm vitest run`；前端 `cd apps/console && pnpm vitest run`
- 错误处理风格：store/校验层抛可读中文 Error，HTTP handler 层 catch 后 `err(400/404/409, msg)`
- skill 状态机：`pending → approved | rejected`（review 接口允许改判）；普通 POST/PUT 不可改 status
- 截断常量：每条 skill 2000 字符、注入总量 8000 字符
- 每任务蒸馏候选上限：2 条（`MAX_CANDIDATES_PER_TASK = 2`）
- assetFiles.path 校验：禁空、禁绝对路径（`/` 或 `X:` 开头）、禁含 `..` 路径段
- 新 skill id 格式：`skill-<randomUUID().slice(0, 8)>`
- **未经用户命令不得执行启动/打包/构建命令（防端口占用）——只跑 vitest**
- 不改动现有 `EmployeeRecord.skills`（岗位标签）语义；调度 role 匹配不受影响
- 注入只作用于 plan 模式（`runTaskPlan`）；老 tasks 包（`runTaskPackage`）零改动

---

### Task 1: FileSkillStore（数据模型 + 文件存储 + 校验）

**Files:**
- Create: `packages/console-api/src/team/skill-store.ts`
- Test: `packages/console-api/test/skill-store.test.ts`

**Interfaces:**
- Consumes: 无（底层模块）
- Produces（后续任务依赖的精确签名）:
  - `type SkillType = 'knowledge' | 'constraint' | 'asset'`
  - `interface SkillRecord { id: string; categoryId: string; name: string; description: string; type: SkillType; content: string; assetFiles?: { path: string; content: string }[]; status: 'pending' | 'approved' | 'rejected'; source: string; sourceTaskId?: string; createdAt: number; reviewedAt?: number }`
  - `interface SkillCategory { id: string; name: string; description?: string }`
  - `validateSkillRecord(rec: SkillRecord, categoryIds: string[]): void`（非法抛中文 Error）
  - `validateSkillCategory(cat: SkillCategory): void`
  - `newSkillId(): string`
  - `class FileSkillStore`：`listCategories(): Promise<SkillCategory[]>`、`upsertCategory(cat): Promise<void>`、`deleteCategory(id): Promise<'deleted' | 'missing' | 'mounted'>`、`listSkills(filter?: { status?: string; categoryId?: string }): Promise<SkillRecord[]>`、`getSkill(id): Promise<SkillRecord | null>`、`upsertSkill(rec): Promise<void>`、`removeSkill(id): Promise<boolean>`、`reviewSkill(id, action: 'approve' | 'reject'): Promise<SkillRecord | null>`、`skillsForCategories(categoryIds: string[]): Promise<SkillRecord[]>`（仅 approved）、`ensureSeed(): Promise<void>`

- [ ] **Step 1: 写失败测试**

```ts
// packages/console-api/test/skill-store.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { mkdtemp, readFile, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import {
  FileSkillStore, newSkillId, validateSkillRecord, validateSkillCategory,
  type SkillRecord,
} from '../src/team/skill-store.js';

let dir: string;
let store: FileSkillStore;

const rec = (over: Partial<SkillRecord> = {}): SkillRecord => ({
  id: newSkillId(), categoryId: 'backend', name: '接口异常码规范', description: 'REST 异常码约定',
  type: 'knowledge', content: '所有接口异常码以 E 开头…', status: 'pending', source: 'manual', createdAt: Date.now(),
  ...over,
});

beforeEach(async () => {
  dir = await mkdtemp(join(tmpdir(), 'skills-'));
  store = new FileSkillStore(join(dir, 'skills.json'));
});

describe('FileSkillStore 分类', () => {
  it('ensureSeed：空目录初始化内置五类并落盘', async () => {
    await store.ensureSeed();
    const cats = await store.listCategories();
    expect(cats.map((c) => c.id)).toEqual(['backend', 'frontend', 'testing', 'devops', 'security']);
    // 落盘校验：文件确实写出来了
    const raw = JSON.parse(await readFile(join(dir, 'skills.json'), 'utf8'));
    expect(raw.categories.length).toBe(5);
    expect(raw.skills).toEqual([]);
  });

  it('ensureSeed 幂等：已有文件不覆盖', async () => {
    await store.upsertCategory({ id: 'custom', name: '自定义' });
    await store.ensureSeed();
    const cats = await store.listCategories();
    expect(cats.map((c) => c.id)).toContain('custom');
    expect(cats.map((c) => c.id)).not.toContain('backend'); // 未被 seed 覆盖
  });

  it('upsertCategory：新增与更新', async () => {
    await store.ensureSeed();
    await store.upsertCategory({ id: 'backend', name: '后端开发（改）' });
    const cats = await store.listCategories();
    expect(cats.find((c) => c.id === 'backend')?.name).toBe('后端开发（改）');
    expect(cats.length).toBe(5);
  });

  it('deleteCategory：挂载保护 / 不存在 / 正常删除', async () => {
    await store.ensureSeed();
    await store.upsertSkill(rec());
    expect(await store.deleteCategory('backend')).toBe('mounted');
    expect(await store.deleteCategory('nope')).toBe('missing');
    await store.upsertCategory({ id: 'tmp', name: '临时' });
    expect(await store.deleteCategory('tmp')).toBe('deleted');
  });
});

describe('FileSkillStore skill CRUD 与审查', () => {
  beforeEach(() => store.ensureSeed());

  it('upsertSkill + listSkills 过滤（status/categoryId）', async () => {
    const a = rec({ status: 'approved', reviewedAt: Date.now() });
    const b = rec({ categoryId: 'frontend' });
    await store.upsertSkill(a);
    await store.upsertSkill(b);
    expect((await store.listSkills()).length).toBe(2);
    expect((await store.listSkills({ status: 'pending' })).map((s) => s.id)).toEqual([b.id]);
    expect((await store.listSkills({ categoryId: 'backend' })).map((s) => s.id)).toEqual([a.id]);
  });

  it('reviewSkill：approve / reject / 改判 / 不存在', async () => {
    const s = rec();
    await store.upsertSkill(s);
    expect((await store.reviewSkill(s.id, 'approve'))?.status).toBe('approved');
    expect((await store.reviewSkill(s.id, 'reject'))?.status).toBe('rejected'); // 允许改判
    expect((await store.reviewSkill(s.id, 'reject'))?.reviewedAt).toBeGreaterThan(0);
    expect(await store.reviewSkill('nope', 'approve')).toBeNull();
  });

  it('removeSkill：存在 true / 不存在 false', async () => {
    const s = rec();
    await store.upsertSkill(s);
    expect(await store.removeSkill(s.id)).toBe(true);
    expect(await store.removeSkill(s.id)).toBe(false);
  });

  it('skillsForCategories：仅返回 approved 且命中分类', async () => {
    await store.upsertSkill(rec({ status: 'approved' }));
    await store.upsertSkill(rec()); // pending 不算
    await store.upsertSkill(rec({ categoryId: 'frontend', status: 'approved' }));
    const got = await store.skillsForCategories(['backend']);
    expect(got.length).toBe(1);
    expect(got[0]!.status).toBe('approved');
  });

  it('原子写与重载：新实例读到同一数据', async () => {
    const s = rec();
    await store.upsertSkill(s);
    const again = new FileSkillStore(join(dir, 'skills.json'));
    expect((await again.getSkill(s.id))?.name).toBe(s.name);
  });
});

describe('校验函数', () => {
  it('validateSkillRecord：type 枚举 / categoryId 必须注册 / asset 必须带文件 / 路径逃逸', () => {
    const ids = ['backend'];
    expect(() => validateSkillRecord(rec(), ids)).not.toThrow();
    expect(() => validateSkillRecord(rec({ type: 'hack' as never }), ids)).toThrow('type');
    expect(() => validateSkillRecord(rec({ categoryId: 'nope' }), ids)).toThrow('categoryId');
    expect(() => validateSkillRecord(rec({ type: 'asset' }), ids)).toThrow('assetFiles');
    expect(() => validateSkillRecord(rec({ type: 'asset', assetFiles: [{ path: '../evil.sh', content: 'x' }] }), ids)).toThrow('..');
    expect(() => validateSkillRecord(rec({ type: 'asset', assetFiles: [{ path: '/etc/passwd', content: 'x' }] }), ids)).toThrow('绝对路径');
    expect(() => validateSkillRecord(rec({ type: 'asset', assetFiles: [{ path: '', content: 'x' }] }), ids)).toThrow('不能为空');
    expect(() => validateSkillRecord(rec({ type: 'asset', assetFiles: [{ path: 'scripts/check.sh', content: 'x' }] }), ids)).not.toThrow();
    expect(() => validateSkillRecord(rec({ name: ' ' }), ids)).toThrow('name');
    expect(() => validateSkillRecord(rec({ content: '' }), ids)).toThrow('content');
  });

  it('validateSkillCategory：id/name 非空', () => {
    expect(() => validateSkillCategory({ id: 'x', name: '测试分类' })).not.toThrow();
    expect(() => validateSkillCategory({ id: '', name: 'x' })).toThrow('id');
    expect(() => validateSkillCategory({ id: 'x', name: '' })).toThrow('name');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/console-api && pnpm vitest run test/skill-store.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 skill-store.ts**

```ts
// packages/console-api/src/team/skill-store.ts
import { mkdir, readFile, rename, writeFile } from 'node:fs/promises';
import { randomUUID } from 'node:crypto';
import { dirname, join } from 'node:path';

/** Skill 类型：knowledge 知识文档 / constraint 开发约束 / asset 可执行资产（附带脚本模板） */
export type SkillType = 'knowledge' | 'constraint' | 'asset';

/** Skill 记录（2026-09-06 Skill 库）：status 仅 review 接口可变（普通 upsert 保留原值） */
export interface SkillRecord {
  id: string;                 // skill-<uuid8>
  categoryId: string;         // 所属技能分类 id
  name: string;
  description: string;
  type: SkillType;
  content: string;            // markdown 正文（knowledge/constraint 即生效内容；asset 为使用说明）
  assetFiles?: { path: string; content: string }[];  // 仅 asset：相对 .skills/<skillId>/ 的文件
  status: 'pending' | 'approved' | 'rejected';
  source: string;             // 'manual' | auto:<taskId>
  sourceTaskId?: string;      // 沉淀来源任务（审查时回看任务上下文）
  createdAt: number;
  reviewedAt?: number;
}

export interface SkillCategory { id: string; name: string; description?: string; }

export const SKILL_TYPES = new Set<SkillType>(['knowledge', 'constraint', 'asset']);

/** assetFiles 路径安全校验：禁空、禁绝对路径、禁 .. 路径段（防路径逃逸） */
function assertSafeAssetPath(p: string): void {
  if (!p.trim()) throw new Error('assetFiles.path 不能为空');
  if (p.startsWith('/') || /^[a-zA-Z]:/.test(p)) throw new Error(`assetFiles.path 禁止绝对路径: ${p}`);
  if (p.split('/').includes('..')) throw new Error(`assetFiles.path 禁止包含 ..: ${p}`);
}

/** upsert 前校验（错误可读，HTTP 层原样 400）；categoryIds 为当前注册分类白名单 */
export function validateSkillRecord(rec: SkillRecord, categoryIds: string[]): void {
  if (typeof rec.name !== 'string' || !rec.name.trim()) throw new Error('skill name 必须是非空字符串');
  if (typeof rec.content !== 'string' || !rec.content.trim()) throw new Error('skill content 必须是非空字符串');
  if (!SKILL_TYPES.has(rec.type)) throw new Error('skill type 必须是 knowledge | constraint | asset');
  if (!categoryIds.includes(rec.categoryId)) throw new Error(`skill categoryId 未注册: ${rec.categoryId}`);
  if (rec.type === 'asset') {
    if (!rec.assetFiles?.length) throw new Error('asset 类型 skill 必须至少附带一个 assetFiles 文件');
    for (const f of rec.assetFiles) assertSafeAssetPath(f.path);
  }
}

export function validateSkillCategory(cat: SkillCategory): void {
  if (typeof cat.id !== 'string' || !cat.id.trim()) throw new Error('分类 id 必须是非空字符串');
  if (typeof cat.name !== 'string' || !cat.name.trim()) throw new Error('分类 name 必须是非空字符串');
}

export function newSkillId(): string {
  return `skill-${randomUUID().slice(0, 8)}`;
}

/** 内置分类 seed（2026-09-06）：首启初始化，管理台可增删改 */
export const DEFAULT_CATEGORIES: SkillCategory[] = [
  { id: 'backend', name: '后端开发' },
  { id: 'frontend', name: '前端开发' },
  { id: 'testing', name: '测试' },
  { id: 'devops', name: 'DevOps' },
  { id: 'security', name: '安全合规' },
];

interface SkillFileShape { categories: SkillCategory[]; skills: SkillRecord[]; }

function isEnoent(err: unknown): boolean {
  return err instanceof Error && 'code' in err && (err as NodeJS.ErrnoException).code === 'ENOENT';
}

function parseShape(raw: string): SkillFileShape {
  const parsed: unknown = JSON.parse(raw);
  if (!parsed || typeof parsed !== 'object' || !Array.isArray((parsed as SkillFileShape).categories) || !Array.isArray((parsed as SkillFileShape).skills)) {
    throw new Error('Skill 库文件格式错误: 应为 { categories: [], skills: [] }');
  }
  return parsed as SkillFileShape;
}

/**
 * Skill 库文件存储：单文件 skills.json（categories + skills），mutate 队列串行化 + tmp 原子写——
 * 与 FileEmployeeStore 同构（缓存、仅 ENOENT 视为不存在、损坏原样上抛）。
 */
export class FileSkillStore {
  private cache: SkillFileShape | undefined;
  private queue: Promise<unknown> = Promise.resolve();

  constructor(private readonly filePath: string) {}

  private async load(): Promise<SkillFileShape> {
    if (this.cache) return this.cache;
    try {
      this.cache = parseShape(await readFile(this.filePath, 'utf8'));
    } catch (err) {
      // 仅"文件不存在"视为未初始化（内存缺省内置分类，写入时持久化）；
      // 损坏/权限等其他读错误原样上抛，不得静默降级为空表缓存
      if (isEnoent(err)) {
        this.cache = { categories: DEFAULT_CATEGORIES.map((c) => ({ ...c })), skills: [] };
      } else {
        throw err;
      }
    }
    return this.cache;
  }

  private async flush(shape: SkillFileShape): Promise<void> {
    this.cache = shape;
    await mkdir(dirname(this.filePath), { recursive: true });
    const tmpPath = join(dirname(this.filePath), `.skills-${process.pid}.tmp`);
    await writeFile(tmpPath, JSON.stringify(shape, null, 2), 'utf8');
    await rename(tmpPath, this.filePath);
  }

  /** 首启 seed：仅文件不存在时写入内置分类（幂等，已有文件不覆盖） */
  async ensureSeed(): Promise<void> {
    const run = this.queue.then(async () => {
      let exists = true;
      try {
        parseShape(await readFile(this.filePath, 'utf8'));
      } catch (err) {
        if (!isEnoent(err)) throw err;
        exists = false;
      }
      if (exists) return;
      await this.flush({ categories: DEFAULT_CATEGORIES.map((c) => ({ ...c })), skills: [] });
    });
    this.queue = run.catch(() => undefined);
    return run;
  }

  listCategories(): Promise<SkillCategory[]> {
    return this.load().then((s) => s.categories.map((c) => ({ ...c })));
  }

  upsertCategory(cat: SkillCategory): Promise<void> {
    const run = this.queue.then(async () => {
      const shape = await this.load();
      const idx = shape.categories.findIndex((c) => c.id === cat.id);
      if (idx >= 0) shape.categories[idx] = { ...cat };
      else shape.categories.push({ ...cat });
      await this.flush(shape);
    });
    this.queue = run.catch(() => undefined);
    return run;
  }

  /** 返回 'deleted' | 'missing'（不存在）| 'mounted'（仍有 skill 挂载，调用方映射 409） */
  async deleteCategory(id: string): Promise<'deleted' | 'missing' | 'mounted'> {
    const run = this.queue.then(async (): Promise<'deleted' | 'missing' | 'mounted'> => {
      const shape = await this.load();
      if (!shape.categories.some((c) => c.id === id)) return 'missing';
      if (shape.skills.some((s) => s.categoryId === id)) return 'mounted';
      await this.flush({ ...shape, categories: shape.categories.filter((c) => c.id !== id) });
      return 'deleted';
    });
    this.queue = run.catch(() => undefined);
    return run;
  }

  async listSkills(filter?: { status?: string; categoryId?: string }): Promise<SkillRecord[]> {
    const shape = await this.load();
    return shape.skills
      .filter((s) => (!filter?.status || s.status === filter.status))
      .filter((s) => (!filter?.categoryId || s.categoryId === filter.categoryId))
      .map((s) => structuredClone(s));
  }

  async getSkill(id: string): Promise<SkillRecord | null> {
    const shape = await this.load();
    const found = shape.skills.find((s) => s.id === id);
    return found ? structuredClone(found) : null;
  }

  upsertSkill(rec: SkillRecord): Promise<void> {
    const run = this.queue.then(async () => {
      const shape = await this.load();
      const idx = shape.skills.findIndex((s) => s.id === rec.id);
      if (idx >= 0) shape.skills[idx] = structuredClone(rec);
      else shape.skills.push(structuredClone(rec));
      await this.flush(shape);
    });
    this.queue = run.catch(() => undefined);
    return run;
  }

  async removeSkill(id: string): Promise<boolean> {
    const run = this.queue.then(async (): Promise<boolean> => {
      const shape = await this.load();
      if (!shape.skills.some((s) => s.id === id)) return false;
      await this.flush({ ...shape, skills: shape.skills.filter((s) => s.id !== id) });
      return true;
    });
    this.queue = run.catch(() => undefined);
    return run;
  }

  /** 人工终审：approve/reject（允许改判，重审幂等）；不存在返回 null */
  async reviewSkill(id: string, action: 'approve' | 'reject'): Promise<SkillRecord | null> {
    const run = this.queue.then(async (): Promise<SkillRecord | null> => {
      const shape = await this.load();
      const found = shape.skills.find((s) => s.id === id);
      if (!found) return null;
      found.status = action === 'approve' ? 'approved' : 'rejected';
      found.reviewedAt = Date.now();
      await this.flush(shape);
      return structuredClone(found);
    });
    this.queue = run.catch(() => undefined);
    return run;
  }

  /** 员工分类 → 生效 skill（仅 approved，注入链路消费） */
  async skillsForCategories(categoryIds: string[]): Promise<SkillRecord[]> {
    const shape = await this.load();
    const ids = new Set(categoryIds);
    return shape.skills.filter((s) => s.status === 'approved' && ids.has(s.categoryId)).map((s) => structuredClone(s));
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd packages/console-api && pnpm vitest run test/skill-store.test.ts`
Expected: PASS（全部用例绿）

- [ ] **Step 5: Commit**

```bash
git add packages/console-api/src/team/skill-store.ts packages/console-api/test/skill-store.test.ts
git commit -m "feat(console-api): FileSkillStore——Skill 库文件存储（分类 seed/CRUD/人工终审/assetFiles 路径逃逸校验）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 2: Skill 库 API 路由（handlers）

**Files:**
- Modify: `packages/console-api/src/http/handlers.ts`
- Modify: `packages/console-api/src/http/server.ts`（接线 skillStore 到 handlers deps）
- Test: `packages/console-api/test/handlers.test.ts`（追加用例）

**Interfaces:**
- Consumes: Task 1 的 `FileSkillStore`/`validateSkillRecord`/`validateSkillCategory`/`newSkillId`/`SkillRecord`/`SkillCategory`
- Produces: HTTP 路由
  - `GET/POST /api/skill-categories`、`DELETE /api/skill-categories/:id`
  - `GET /api/skills?status=&categoryId=&q=`、`POST /api/skills`（201，status=pending）、`PUT /api/skills/:id`（status/source/createdAt 保留原值）、`DELETE /api/skills/:id`、`POST /api/skills/:id/review`（body `{ action: 'approve' | 'reject' }`）
  - handlers deps 新增 `skills?: FileSkillStore`（未注入 = 400 未启用 Skill 库）

- [ ] **Step 1: 写失败测试（追加到 handlers.test.ts 末尾）**

在 `packages/console-api/test/handlers.test.ts` 文件顶部 import 区加入：

```ts
import { FileSkillStore } from '../src/team/skill-store.js';
import { mkdtemp } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
```

文件末尾追加：

```ts
describe('Skill 库 API（2026-09-06）', () => {
  const SKILL = {
    categoryId: 'backend', name: '接口异常码规范', description: 'REST 异常码约定',
    type: 'knowledge' as const, content: '所有接口异常码以 E 开头',
  };

  const makeSkillDeps = async () => {
    const dir = await mkdtemp(join(tmpdir(), 'skills-'));
    return new FileSkillStore(join(dir, 'skills.json'));
  };

  it('未注入 skillStore：400 未启用', async () => {
    const handle = createHandlers(baseDeps());
    const res = await handle({ method: 'GET', path: '/api/skills' });
    expect(res.status).toBe(400);
  });

  it('分类 CRUD：seed 后 GET 内置五类；POST 新增；DELETE 挂载 409/不存在 404', async () => {
    const skills = await makeSkillDeps();
    const handle = createHandlers({ ...baseDeps(), skills });
    await skills.ensureSeed();
    const list = await handle({ method: 'GET', path: '/api/skill-categories' });
    expect((list.json as { id: string }[]).map((c) => c.id)).toContain('backend');

    const created = await handle({ method: 'POST', path: '/api/skill-categories', body: { id: 'data', name: '数据开发' } });
    expect(created.status).toBe(201);

    await handle({ method: 'POST', path: '/api/skills', body: SKILL });
    const mounted = await handle({ method: 'DELETE', path: '/api/skill-categories/backend' });
    expect(mounted.status).toBe(409);
    const missing = await handle({ method: 'DELETE', path: '/api/skill-categories/nope' });
    expect(missing.status).toBe(404);
    const okDel = await handle({ method: 'DELETE', path: '/api/skill-categories/data' });
    expect(okDel.status).toBe(200);
  });

  it('skill POST：201 pending + 自动 id；校验失败 400', async () => {
    const skills = await makeSkillDeps();
    const handle = createHandlers({ ...baseDeps(), skills });
    const res = await handle({ method: 'POST', path: '/api/skills', body: SKILL });
    expect(res.status).toBe(201);
    const body = res.json as { id: string; status: string };
    expect(body.status).toBe('pending');
    expect(body.id).toMatch(/^skill-/);
    expect((await skills.listSkills({ status: 'pending' })).length).toBe(1);

    const bad = await handle({ method: 'POST', path: '/api/skills', body: { ...SKILL, categoryId: 'nope' } });
    expect(bad.status).toBe(400);
    expect((bad.json as { error: string }).error).toContain('categoryId');

    const esc = await handle({ method: 'POST', path: '/api/skills', body: { ...SKILL, type: 'asset', assetFiles: [{ path: '../x.sh', content: 'x' }] } });
    expect(esc.status).toBe(400);
    expect((esc.json as { error: string }).error).toContain('..');
  });

  it('skill PUT：可编辑但 status/source/createdAt 保留原值', async () => {
    const skills = await makeSkillDeps();
    const handle = createHandlers({ ...baseDeps(), skills });
    const created = await handle({ method: 'POST', path: '/api/skills', body: SKILL });
    const id = (created.json as { id: string }).id;
    const res = await handle({ method: 'PUT', path: `/api/skills/${id}`, body: { ...SKILL, name: '改名', status: 'approved' } });
    expect(res.status).toBe(200);
    const stored = await skills.getSkill(id);
    expect(stored?.name).toBe('改名');
    expect(stored?.status).toBe('pending'); // status 不可经 PUT 篡改
  });

  it('skill review：approve/reject/改判；action 非法 400；不存在 404', async () => {
    const skills = await makeSkillDeps();
    const handle = createHandlers({ ...baseDeps(), skills });
    const created = await handle({ method: 'POST', path: '/api/skills', body: SKILL });
    const id = (created.json as { id: string }).id;

    const ok1 = await handle({ method: 'POST', path: `/api/skills/${id}/review`, body: { action: 'approve' } });
    expect(ok1.json).toMatchObject({ status: 'approved' });
    const ok2 = await handle({ method: 'POST', path: `/api/skills/${id}/review`, body: { action: 'reject' } });
    expect(ok2.json).toMatchObject({ status: 'rejected' }); // 允许改判
    const bad = await handle({ method: 'POST', path: `/api/skills/${id}/review`, body: { action: 'hack' } });
    expect(bad.status).toBe(400);
    const missing = await handle({ method: 'POST', path: '/api/skills/nope/review', body: { action: 'approve' } });
    expect(missing.status).toBe(404);
  });

  it('skill GET 过滤：status/categoryId/q', async () => {
    const skills = await makeSkillDeps();
    const handle = createHandlers({ ...baseDeps(), skills });
    await handle({ method: 'POST', path: '/api/skills', body: SKILL });
    await handle({ method: 'POST', path: '/api/skills', body: { ...SKILL, name: '前端规范', categoryId: 'frontend' } });
    const byCat = await handle({ method: 'GET', path: '/api/skills?categoryId=frontend' });
    expect((byCat.json as { name: string }[]).map((s) => s.name)).toEqual(['前端规范']);
    const byQ = await handle({ method: 'GET', path: '/api/skills?q=异常码' });
    expect((byQ.json as unknown[]).length).toBe(1);
  });
});
```

注意：若 `handlers.test.ts` 中现无 `baseDeps()` 工厂，则以文件内现有用例构造最小 deps 的方式为准，抽出/复用等价 helper——`createHandlers({ tasks, events, ... })` 的 tasks/events 用该文件既有测试的现成替身。**禁止改既有用例的断言**。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/console-api && pnpm vitest run test/handlers.test.ts`
Expected: 新增用例 FAIL（路由 404）

- [ ] **Step 3: 实现 handlers 路由**

`handlers.ts` 顶部 import 区加：

```ts
import type { FileSkillStore, SkillRecord, SkillCategory } from '../team/skill-store.js';
import { validateSkillRecord, validateSkillCategory, newSkillId } from '../team/skill-store.js';
```

`createHandlers` deps 加（`repoCheck` 之后）：

```ts
  /** Skill 库（2026-09-06，可选）：分类 + skill 记录 CRUD + 人工终审 */
  skills?: FileSkillStore;
```

在 `/api/capabilities/meta` 路由**之前**插入两个路由块：

```ts
      // Skill 库分类（2026-09-06）：CRUD；删除挂载保护 → 409
      if (seg[0] === 'api' && seg[1] === 'skill-categories') {
        if (!deps.skills) return err(400, '未启用 Skill 库');
        if (method === 'GET' && seg.length === 2) return ok(await deps.skills.listCategories());
        if (method === 'POST' && seg.length === 2) {
          const cat = body as SkillCategory;
          try { validateSkillCategory(cat); } catch (e) { return err(400, e instanceof Error ? e.message : String(e)); }
          await deps.skills.upsertCategory(cat);
          return ok({ id: cat.id }, 201);
        }
        if (method === 'DELETE' && seg.length === 3 && seg[2]) {
          const r = await deps.skills.deleteCategory(seg[2]);
          if (r === 'missing') return err(404, `分类不存在: ${seg[2]}`);
          if (r === 'mounted') return err(409, `分类 ${seg[2]} 仍有 skill 挂载，先删除或迁移相关 skill`);
          return ok({ id: seg[2], removed: true });
        }
      }

      // Skill 库（2026-09-06）：记录 CRUD + 人工终审（status 仅 review 接口可变）
      if (seg[0] === 'api' && seg[1] === 'skills') {
        if (!deps.skills) return err(400, '未启用 Skill 库');
        const store = deps.skills;
        if (method === 'GET' && seg.length === 2) {
          let list = await store.listSkills();
          if (query.status) list = list.filter((s) => s.status === query.status);
          if (query.categoryId) list = list.filter((s) => s.categoryId === query.categoryId);
          if (query.q) {
            const q = query.q.toLowerCase();
            list = list.filter((s) => s.name.toLowerCase().includes(q) || s.description.toLowerCase().includes(q));
          }
          return ok(list);
        }
        if (method === 'POST' && seg.length === 2) {
          const rec = body as SkillRecord;
          const categoryIds = (await store.listCategories()).map((c) => c.id);
          try { validateSkillRecord(rec, categoryIds); } catch (e) { return err(400, e instanceof Error ? e.message : String(e)); }
          const full: SkillRecord = {
            ...rec, id: rec.id || newSkillId(), status: 'pending',
            source: rec.source || 'manual', createdAt: Date.now(),
          };
          await store.upsertSkill(full);
          return ok({ id: full.id, status: 'pending' }, 201);
        }
        if (seg.length === 3 && seg[2]) {
          const id = seg[2];
          if (method === 'PUT') {
            const existing = await store.getSkill(id);
            if (!existing) return err(404, `skill 不存在: ${id}`);
            const rec = body as SkillRecord;
            const categoryIds = (await store.listCategories()).map((c) => c.id);
            try { validateSkillRecord({ ...rec, id }, categoryIds); } catch (e) { return err(400, e instanceof Error ? e.message : String(e)); }
            // status/source/createdAt 保留原值（body 不含/被篡改均不生效）
            await store.upsertSkill({ ...existing, ...rec, id, status: existing.status, source: existing.source, createdAt: existing.createdAt });
            return ok({ id });
          }
          if (method === 'DELETE') {
            if (!(await store.removeSkill(id))) return err(404, `skill 不存在: ${id}`);
            return ok({ id, removed: true });
          }
          if (method === 'POST' && seg[3] === 'review') {
            const { action } = (body ?? {}) as { action?: string };
            if (action !== 'approve' && action !== 'reject') return err(400, 'action 必须是 approve | reject');
            const reviewed = await store.reviewSkill(id, action);
            if (!reviewed) return err(404, `skill 不存在: ${id}`);
            return ok({ id, status: reviewed.status });
          }
        }
      }
```

`server.ts`：在 `const capabilities = ...` 之后加：

```ts
  // Skill 库（2026-09-06）：单文件 skills.json，内置分类 seed 异步触发不阻塞启动
  const skillStore = new FileSkillStore(join(opts.dataDir, 'skills.json'));
  void skillStore.ensureSeed().catch((err: unknown) => {
    console.error('[skills] seed 失败:', err instanceof Error ? err.message : err);
  });
```

顶部 import 加 `import { FileSkillStore } from '../team/skill-store.js';`，`createHandlers({...})` deps 加 `skills: skillStore,`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd packages/console-api && pnpm vitest run test/handlers.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add packages/console-api/src/http/handlers.ts packages/console-api/src/http/server.ts packages/console-api/test/handlers.test.ts
git commit -m "feat(console-api): Skill 库 API——分类/skill CRUD + 人工终审路由（status 仅 review 可变）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 3: 员工 skillCategories 字段

**Files:**
- Modify: `packages/console-api/src/team/employee-store.ts`
- Modify: `packages/console-api/src/http/handlers.ts`（employees GET/POST/PUT 透传 + 校验传分类白名单）
- Modify: `packages/console-api/src/http/server.ts`（handlers deps 传 skillStore 已在 Task 2 完成——本任务只改 employees 分支）
- Test: `packages/console-api/test/employee-store.test.ts`（追加）、`packages/console-api/test/handlers.test.ts`（追加）

**Interfaces:**
- Consumes: Task 1 的 `FileSkillStore.listCategories()`
- Produces:
  - `EmployeeRecord.skillCategories: string[]`（可空数组 = 不挂任何 skill）
  - `validateEmployeeRecord(rec, capabilityKinds, skillCategoryIds = [])` 第三参可选（缺省不设限，既有调用零回归）

- [ ] **Step 1: 写失败测试（追加到 employee-store.test.ts）**

```ts
describe('EmployeeRecord.skillCategories（2026-09-06 Skill 关联）', () => {
  const base = { id: 'emp-9', name: '小测', role: 'backend', skills: ['backend'], capabilities: [], enabled: true } as const;

  it('validateEmployeeRecord：分类白名单校验（白名单空 = 不设限）', async () => {
    const { validateEmployeeRecord } = await import('../src/team/employee-store.js');
    expect(() => validateEmployeeRecord({ ...base, skillCategories: ['backend'], createdAt: 1 }, [], [])).not.toThrow();
    expect(() => validateEmployeeRecord({ ...base, skillCategories: ['backend'], createdAt: 1 }, [], ['backend'])).not.toThrow();
    expect(() => validateEmployeeRecord({ ...base, skillCategories: ['nope'], createdAt: 1 }, [], ['backend']))
      .toThrow('skillCategories');
    // 缺省第三参（既有调用）不设限
    expect(() => validateEmployeeRecord({ ...base, skillCategories: ['任意'], createdAt: 1 }, [])).not.toThrow();
    expect(() => validateEmployeeRecord({ ...base, skillCategories: 'x' as never, createdAt: 1 }, [])).toThrow('skillCategories 必须是字符串数组');
  });

  it('seedFrom：skillCategories 置空数组', async () => {
    const { FileEmployeeStore } = await import('../src/team/employee-store.js');
    const { mkdtemp } = await import('node:fs/promises');
    const { tmpdir } = await import('node:os');
    const { join } = await import('node:path');
    const dir = await mkdtemp(join(tmpdir(), 'emp-'));
    const store = new FileEmployeeStore(join(dir, 'employees.json'));
    await store.seedFrom([{ id: 'p1', name: '甲', role: 'backend', skills: ['backend'] } as never]);
    const got = await store.get('p1');
    expect(got?.skillCategories).toEqual([]);
  });
});
```

handlers.test.ts 追加：

```ts
it('employees GET/POST 透传 skillCategories；POST 校验分类白名单', async () => {
  const skills = new FileSkillStore(join(tmpdir(), 'skills-emp-', String(Date.now()), 'skills.json'));
  await skills.ensureSeed();
  const store = /* 该文件既有 employeeStore 测试替身/临时 FileEmployeeStore，复用既有 helper */;
  const handle = createHandlers({ ...baseDeps(), employeeStore: store, skills });
  const res = await handle({ method: 'POST', path: '/api/employees', body: {
    id: 'emp-sk', name: '小技', role: 'backend', skills: ['backend'], capabilities: [],
    skillCategories: ['backend'], supervision: 'shadow', enabled: true,
  } });
  expect(res.status).toBe(201);
  const bad = await handle({ method: 'POST', path: '/api/employees', body: {
    id: 'emp-bad', name: '错', role: 'backend', skills: ['backend'], capabilities: [],
    skillCategories: ['nope'], supervision: 'shadow', enabled: true,
  } });
  expect(bad.status).toBe(400);
  expect((bad.json as { error: string }).error).toContain('skillCategories');
  const list = await handle({ method: 'GET', path: '/api/employees' });
  expect((list.json as { skillCategories: string[] }[]).find((e) => e.id === 'emp-sk')?.skillCategories).toEqual(['backend']);
});
```

（`store` 构造方式与同文件既有 employees 用例保持一致——若已有 FileEmployeeStore 临时目录 helper 直接复用。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/console-api && pnpm vitest run test/employee-store.test.ts test/handlers.test.ts`
Expected: FAIL

- [ ] **Step 3: 实现**

`employee-store.ts`：

1. `EmployeeRecord` 接口 `capabilities: string[];` 之后加：

```ts
  /** Skill 分类关联（2026-09-06）：该分类下已批准 skill 对该员工生效；空数组 = 不挂任何 skill */
  skillCategories: string[];
```

2. `validateEmployeeRecord` 签名改为 `validateEmployeeRecord(rec: EmployeeRecord, capabilityKinds: string[], skillCategoryIds: string[] = [])`，在 capabilities 白名单校验块之后加：

```ts
  if (!Array.isArray(rec.skillCategories) || !rec.skillCategories.every((x) => typeof x === 'string')) {
    throw new Error('员工 skillCategories 必须是字符串数组');
  }
  // Skill 分类白名单仅在分类表已配置时校验（与 capabilities 同语义：空 = 不设限）
  if (skillCategoryIds.length > 0) {
    const unknownCats = rec.skillCategories.filter((k) => !skillCategoryIds.includes(k));
    if (unknownCats.length > 0) {
      throw new Error(`员工 skillCategories 含未注册的技能分类: ${unknownCats.join(', ')}（允许: ${skillCategoryIds.join(', ') || '无'}）`);
    }
  }
```

3. `seedFrom` 的 flush map 里 `capabilities: []` 之后加 `skillCategories: [],`。

`handlers.ts` employees 分支：

1. GET（store 分支）map 里 `capabilities: r.capabilities,` 后加 `skillCategories: r.skillCategories ?? [],`。
2. POST 分支：`const kinds = ...` 之后加 `const catIds = deps.skills ? (await deps.skills.listCategories()).map((c) => c.id) : [];`，`validateEmployeeRecord(rec, kinds)` 改 `validateEmployeeRecord(rec, kinds, catIds)`。
3. PUT 分支同样处理（`const kinds = ...` 处同步改）。
4. 只读 profiles 回退分支（无 employeeStore）GET 的 map 加 `skillCategories: [],`（保持视图字段齐全）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd packages/console-api && pnpm vitest run`
Expected: 全量 PASS（validateEmployeeRecord 第三参可选保证既有用例零回归）

- [ ] **Step 5: Commit**

```bash
git add packages/console-api/src/team/employee-store.ts packages/console-api/src/http/handlers.ts packages/console-api/test/employee-store.test.ts packages/console-api/test/handlers.test.ts
git commit -m "feat(console-api): 员工档案 skillCategories 字段——按分类关联 Skill 库（白名单校验）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 4: skill-injection.ts（指令追加 + asset 落盘）

**Files:**
- Create: `packages/console-api/src/team/skill-injection.ts`
- Test: `packages/console-api/test/skill-injection.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `SkillRecord`
- Produces（Task 5/6 依赖）:
  - `appendSkillContext(instruction: string, skills: SkillRecord[]): string`（空清单原样返回）
  - `materializeSkillAssets(workspaceDir: string, skills: SkillRecord[]): Promise<number>`（返回写入文件数）
  - 常量 `PER_SKILL_LIMIT = 2000`、`TOTAL_SKILL_LIMIT = 8000`

- [ ] **Step 1: 写失败测试**

```ts
// packages/console-api/test/skill-injection.test.ts
import { describe, it, expect } from 'vitest';
import { mkdtemp, readFile, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { appendSkillContext, materializeSkillAssets, PER_SKILL_LIMIT, TOTAL_SKILL_LIMIT, type SkillRecord } from '../src/team/skill-injection.js';

const skill = (over: Partial<SkillRecord>): SkillRecord => ({
  id: 'skill-a1', categoryId: 'backend', name: '异常码规范', description: '', type: 'knowledge',
  content: '异常码以 E 开头', status: 'approved', source: 'manual', createdAt: 1, ...over,
});

describe('appendSkillContext', () => {
  it('空清单原样返回', () => {
    const ins = '原始指令';
    expect(appendSkillContext(ins, [])).toBe(ins);
  });

  it('knowledge/constraint 追加「技能与约束」段', () => {
    const out = appendSkillContext('原始指令', [
      skill({}), skill({ id: 'skill-b2', name: '禁直连生产库', type: 'constraint', content: '禁止直连' }),
    ]);
    expect(out).toContain('原始指令');
    expect(out).toContain('## 技能与约束');
    expect(out).toContain('异常码规范');
    expect(out).toContain('禁止直连');
    expect(out).toContain('开发约束');
  });

  it('asset 只出资产清单不出正文', () => {
    const out = appendSkillContext('原始', [skill({
      id: 'skill-c3', name: '脚手架', type: 'asset', content: '使用说明',
      assetFiles: [{ path: 'scripts/init.sh', content: 'echo hi' }],
    })]);
    expect(out).toContain('.skills/skill-c3/scripts/init.sh');
    expect(out).toContain('受控 bash');
    expect(out).not.toContain('使用说明'); // asset 正文不注入
  });

  it('截断：单条 2000、总量 8000', () => {
    const long = appendSkillContext('原始', [skill({ content: 'x'.repeat(3000) })]);
    expect(long.length).toBeLessThan('原始'.length + 3000 + 200); // 单条被截
    expect(long).toContain('超长截断');
    const many = appendSkillContext('原始', Array.from({ length: 8 }, (_, i) => skill({ id: `skill-${i}`, content: 'y'.repeat(1500) })));
    const bodyLen = many.length - '原始'.length;
    expect(bodyLen).toBeLessThanOrEqual(TOTAL_SKILL_LIMIT + 2000); // 总量截断（末条可能整体注入后截）
    expect(many).toContain('## 技能与约束');
    expect(PER_SKILL_LIMIT).toBe(2000);
    expect(TOTAL_SKILL_LIMIT).toBe(8000);
  });
});

describe('materializeSkillAssets', () => {
  it('asset 文件落盘 workspace/.skills/<id>/，返回写入数；非 asset 忽略', async () => {
    const dir = await mkdtemp(join(tmpdir(), 'ws-'));
    const n = await materializeSkillAssets(dir, [
      skill({ id: 'skill-d4', type: 'asset', assetFiles: [{ path: 'scripts/a.sh', content: 'echo a' }, { path: 'nested/b.txt', content: 'b' }] }),
      skill({ id: 'skill-d5', type: 'asset', assetFiles: [{ path: 'run.sh', content: 'echo run' }] }),
      skill({}), // 非 asset
    ]);
    expect(n).toBe(3);
    expect(await readFile(join(dir, '.skills', 'skill-d4', 'scripts', 'a.sh'), 'utf8')).toBe('echo a');
    expect(await readFile(join(dir, '.skills', 'skill-d4', 'nested', 'b.txt'), 'utf8')).toBe('b');
    expect(await readFile(join(dir, '.skills', 'skill-d5', 'run.sh'), 'utf8')).toBe('echo run');
    await rm(dir, { recursive: true, force: true });
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/console-api && pnpm vitest run test/skill-injection.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

```ts
// packages/console-api/src/team/skill-injection.ts
import { mkdir, writeFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';
import type { SkillRecord } from './skill-store.js';

/** 单条 skill 正文注入上限（防 token 爆炸，spec §四） */
export const PER_SKILL_LIMIT = 2000;
/** 全部 skill 注入总上限 */
export const TOTAL_SKILL_LIMIT = 8000;

/**
 * 指令追加段（spec §四）：knowledge/constraint 正文拼入「技能与约束」段；
 * asset 只列「可用资产清单」（路径 + 用途），正文不注入——员工用受控 bash 自行调用。
 * 空清单原样返回（零注入零开销）。
 */
export function appendSkillContext(instruction: string, skills: SkillRecord[]): string {
  const docs = skills.filter((s) => s.type !== 'asset');
  const assets = skills.filter((s) => s.type === 'asset');
  if (docs.length === 0 && assets.length === 0) return instruction;

  const lines: string[] = ['', '## 技能与约束（来自 Skill 库，必须遵守）'];
  let used = 0;
  for (const s of docs) {
    if (used >= TOTAL_SKILL_LIMIT) {
      lines.push('…（后续技能因总量限制未注入）');
      break;
    }
    let body = s.content;
    if (body.length > PER_SKILL_LIMIT) body = `${body.slice(0, PER_SKILL_LIMIT)}…（超长截断）`;
    if (used + body.length > TOTAL_SKILL_LIMIT) {
      body = `${body.slice(0, Math.max(0, TOTAL_SKILL_LIMIT - used))}…（总量截断）`;
    }
    used += body.length;
    lines.push(`### ${s.name}（${s.type === 'constraint' ? '开发约束' : '知识'}）`);
    lines.push(body);
  }
  if (assets.length > 0) {
    lines.push('### 可用技能资产');
    lines.push('以下资产已放置在工作区 .skills/ 目录，可用受控 bash 调用：');
    for (const s of assets) {
      for (const f of s.assetFiles ?? []) {
        lines.push(`- .skills/${s.id}/${f.path}：${s.name}${s.description ? `（${s.description}）` : ''}`);
      }
    }
  }
  return `${instruction}\n${lines.join('\n')}`;
}

/** asset 落盘（spec §四）：workspace/.skills/<skillId>/<path> 逐文件写入；返回写入文件数 */
export async function materializeSkillAssets(workspaceDir: string, skills: SkillRecord[]): Promise<number> {
  let count = 0;
  for (const s of skills.filter((x) => x.type === 'asset')) {
    for (const f of s.assetFiles ?? []) {
      const target = join(workspaceDir, '.skills', s.id, f.path);
      await mkdir(dirname(target), { recursive: true });
      await writeFile(target, f.content, 'utf8');
      count++;
    }
  }
  return count;
}
```

注意：测试 import 改为从 `../src/team/skill-injection.js` 同时导入 `type SkillRecord`（该文件 re-export 类型）：在 skill-injection.ts 顶部加 `export type { SkillRecord } from './skill-store.js';`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd packages/console-api && pnpm vitest run test/skill-injection.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add packages/console-api/src/team/skill-injection.ts packages/console-api/test/skill-injection.test.ts
git commit -m "feat(console-api): skill-injection——指令追加（2000/8000 截断）+ asset 落盘 .skills/

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 5: plan-runner / executor 注入接线（inproc）

**Files:**
- Modify: `packages/console-api/src/team/plan-runner.ts`
- Modify: `packages/console-api/src/team/executor.ts`
- Test: `packages/console-api/test/plan-runner.test.ts`（追加；若无此文件则新建，按下方代码自包含）

**Interfaces:**
- Consumes: Task 4 的 `appendSkillContext`/`materializeSkillAssets`、`SkillRecord`
- Produces:
  - `PlanRunInput` 新增 `skills?: SkillRecord[]`（分派时已过滤为 approved 的清单）
  - `EmployeeExecutorOptions` 新增 `skillsFor?: (employeeId: string) => Promise<SkillRecord[]>`（inproc 每任务执行前实时取）

- [ ] **Step 1: 写失败测试**

若 `test/plan-runner.test.ts` 已存在，在其后追加用例（复用文件内既有的 gateway/events/faux agent 替身构造方式）；否则新建自包含测试：

```ts
// packages/console-api/test/plan-runner-skills.test.ts（自包含新文件，避免耦合既有测试装配）
import { describe, it, expect } from 'vitest';
import { runTaskPlan, type PlanRunInput } from '../src/team/plan-runner.js';
import { appendSkillContext, type SkillRecord } from '../src/team/skill-injection.js';
import type { AgentEvent, AgentFactory, EmployeeOutcome, PlanItem, TaskPackage } from '@ddw/runtime';

// faux agent 工厂：把收到的 instruction 原样作为申报结果返回（供断言注入内容）
const recordingAgent = (captured: string[]): AgentFactory => () => ({
  run: async (input: { instruction?: string }) => {
    captured.push(input.instruction ?? '');
    return { reply: 'done', status: 'ok' } as never;
  },
}) as never;

const pkg: TaskPackage & { plan: PlanItem[] } = {
  taskId: 'T-SKILL', title: '技能注入', repo: { url: 'http://x/a.git', branch: 'main' },
  tasks: [],
  plan: [{ id: 't1', kind: 'dev', title: '开发', detail: '实现' }],
} as never;

const skill = (over: Partial<SkillRecord>): SkillRecord => ({
  id: 'skill-s1', categoryId: 'backend', name: '规范', description: '', type: 'knowledge',
  content: '异常码以 E 开头', status: 'approved', source: 'manual', createdAt: 1, ...over,
});

function baseInput(captured: string[]): PlanRunInput {
  return {
    pkg, employee: { id: 'emp-1', name: '甲', role: 'backend', skills: [] } as never,
    workspaceDir: '/tmp/does-not-matter', sessionsRoot: '/tmp/sessions',
    gateway: {} as never, events: { emit: async () => {}, on: () => {}, off: () => {} } as never,
    capabilities: async () => [{ kind: 'dev', name: '开发', description: '', tools: { builtin: ['bash'], mcp: [] }, enabled: true }],
    agentFactory: recordingAgent(captured),
    toolsForItem: () => ({}) as never,
  } as never;
}

describe('plan-runner skill 注入（2026-09-06）', () => {
  it('skills 在场：指令追加「技能与约束」段', async () => {
    const captured: string[] = [];
    const outcome: EmployeeOutcome = await runTaskPlan({ ...baseInput(captured), skills: [skill({})] } as never);
    expect(outcome.status).toBe('done');
    expect(captured[0]).toContain('## 技能与约束');
    expect(captured[0]).toContain('异常码以 E 开头');
  });

  it('skills 缺省/空数组：指令零改动（零回归）', async () => {
    const captured: string[] = [];
    await runTaskPlan(baseInput(captured) as never);
    expect(captured[0]).not.toContain('## 技能与约束');
  });

  it('asset skill：正文不注入、清单注入（落盘在 executor 层测试覆盖）', async () => {
    const captured: string[] = [];
    await runTaskPlan({
      ...baseInput(captured),
      skills: [skill({ id: 'skill-a9', type: 'asset', assetFiles: [{ path: 'x.sh', content: 'y' }] })],
    } as never);
    expect(captured[0]).toContain('.skills/skill-a9/x.sh');
  });
});
```

（若既有 plan-runner.test.ts 的 faux agent 事件协议与上方 recordingAgent 返回形状不匹配导致 outcome 非 done，则以既有测试文件中能跑通的 faux agent 装配为准迁移 `recordingAgent` 实现——**断言目标不变：captured 的 instruction 含/不含「## 技能与约束」**。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/console-api && pnpm vitest run test/plan-runner-skills.test.ts`
Expected: FAIL（PlanRunInput 无 skills 字段，注入不生效）

- [ ] **Step 3: 实现**

`plan-runner.ts`：

1. import 区加：

```ts
import { appendSkillContext, materializeSkillAssets } from './skill-injection.js';
import type { SkillRecord } from './skill-store.js';
```

2. `PlanRunInput` 的 `progress?: PlanProgress[];` 之后加：

```ts
  /** Skill 注入清单（2026-09-06）：分派时按员工分类过滤后的 approved 清单；缺省 = 不注入 */
  skills?: SkillRecord[];
```

3. `runTaskPlan` 内 `await validatePlan(...)` 之后、`const done = new Map...` 之前加：

```ts
  // Skill 注入（2026-09-06）：asset 落盘一次（整计划共用），文档类每项追加指令
  const skills = input.skills ?? [];
  if (skills.some((s) => s.type === 'asset')) {
    await materializeSkillAssets(input.workspaceDir, skills);
  }
```

4. `const instruction = buildPlanItemInstruction(...)` 改为：

```ts
    const baseInstruction = buildPlanItemInstruction(input.pkg, item, i, total, doneSummary);
    const instruction = skills.length > 0 ? appendSkillContext(baseInstruction, skills) : baseInstruction;
```

`executor.ts`：

1. import 加 `import type { SkillRecord } from './skill-store.js';`
2. `EmployeeExecutorOptions` 接口加（`prepareWorkspace` 声明附近）：

```ts
  /** Skill 注入（2026-09-06）：按员工 id 实时取 approved 清单（后台改判/新入库下个任务即生效） */
  skillsFor?: (employeeId: string) => Promise<SkillRecord[]>;
```

3. `createEmployeeExecutor` 内 plan 分支 `return runTaskPlan({` 的对象里 `...(progress ? { progress } : {}),` 之后加：

```ts
        ...(opts.skillsFor ? { skills: await opts.skillsFor(employee.id) } : {}),
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd packages/console-api && pnpm vitest run test/plan-runner-skills.test.ts && pnpm vitest run`
Expected: 全量 PASS

- [ ] **Step 5: Commit**

```bash
git add packages/console-api/src/team/plan-runner.ts packages/console-api/src/team/executor.ts packages/console-api/test/plan-runner-skills.test.ts
git commit -m "feat(console-api): plan-runner 注入 Skill——指令追加 + asset 落盘，executor skillsFor 接线

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 6: fork 链路（worker 快照 + process-executor + pipeline）

**Files:**
- Modify: `packages/console-api/src/team/worker.ts`
- Modify: `packages/console-api/src/team/process-executor.ts`
- Modify: `packages/console-api/src/team/pipeline.ts`
- Test: `packages/console-api/test/worker.test.ts`（追加；若无则新建自包含）

**Interfaces:**
- Consumes: Task 5 的 `PlanRunInput.skills`
- Produces:
  - `WorkerJob.config.skills?: SkillRecord[]`（分派时刻快照，IPC JSON）
  - `ConsoleRuntimeOptions.skillsFor?: (employeeId: string) => Promise<SkillRecord[]>`（pipeline 装配层消费，两条执行链共用）

- [ ] **Step 1: 写失败测试**

```ts
// 追加到 packages/console-api/test/worker.test.ts（无则新建，下面自包含）
import { describe, it, expect } from 'vitest';
import { runWorker, type MainToWorker, type WorkerToMain } from '../src/team/worker.js';

/** 收集 worker 事件，直到 done/error */
function collect(io: { messages: WorkerToMain[] }): void {
  // 测试替身 io 在用例内组装
}

describe('worker skill 快照注入（2026-09-06 fork 链路）', () => {
  it('config.skills 随 job 下发：指令含技能段', async () => {
    const messages: WorkerToMain[] = [];
    const job = {
      task: {
        taskId: 'T-FK', title: 'fork 注入', repo: { url: 'http://x/a.git', branch: 'main' }, tasks: [],
        plan: [{ id: 't1', kind: 'dev', title: '开发', detail: '实现' }],
      },
      employee: { id: 'emp-1', name: '甲', role: 'backend', skills: [] },
      config: {
        routes: [], backendKind: 'noop' as const, workspaceRoot: '/tmp/ws-fk', sessionsRoot: '/tmp/ss-fk',
        skipWorkspacePrepare: true,
        capabilities: [{ kind: 'dev', name: '开发', description: '', tools: { builtin: ['bash'], mcp: [] }, enabled: true }],
        skills: [{
          id: 'skill-f1', categoryId: 'backend', name: 'fork规范', description: '', type: 'knowledge',
          content: 'fork 注入内容', status: 'approved' as const, source: 'manual', createdAt: 1,
        }],
      },
    };
    const done = new Promise<void>((resolve) => {
      runWorker({
        send: (m) => { messages.push(m); if (m.type === 'done' || m.type === 'error') resolve(); },
        onMessage: (handler) => handler({ type: 'job', job } as MainToWorker),
      });
    });
    await done;
    const instruction = messages
      .filter((m): m is Extract<WorkerToMain, { type: 'event' }> => m.type === 'event')
      .map((m) => (m.event.payload as { instruction?: string } | undefined)?.instruction ?? '')
      .join('\n');
    // 事件里若无 instruction，则以 outcome 状态与无异常为准；核心断言：无 error 消息
    expect(messages.find((m) => m.type === 'error')).toBeUndefined();
    void instruction;
  }, 20_000);
});
```

说明：worker 事件流不含原始 instruction 文本，故该用例主断言为「携带 skills 的 job 全链路无 error」；注入文本正确性已由 Task 5 的 plan-runner 用例覆盖（fork 与 inproc 共用 `runTaskPlan` 同一处注入实现）。若既有 worker.test.ts 有更精确的断言手段（如 faux agentModulePath 捕获指令），优先复用其装配方式并断言 `captured` 含 `fork 注入内容`。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/console-api && pnpm vitest run test/worker.test.ts`
Expected: 新用例 PASS 或 FAIL 取决于现有类型检查——核心是 **Step 3 完成后 `pnpm vitest run` 全量 + `tsc` 无类型错误**。此步预期：`config.skills` 字段不存在导致 TS 编译失败（vitest transform 报错）。

- [ ] **Step 3: 实现**

`worker.ts`：

1. import 加 `import type { SkillRecord } from './skill-store.js';`
2. `WorkerJob.config` 的 `mcpServers?: McpServerConfig[];` 之后加：

```ts
    /** Skill 快照（2026-09-06）：分派时刻按员工分类过滤的 approved 清单（IPC JSON，fork 天生快照） */
    skills?: SkillRecord[];
```

3. plan 分支 `runTaskPlan({...})` 对象里 `...(config.deploy ? { deploy: config.deploy } : {}),` 之后加：

```ts
        ...(config.skills?.length ? { skills: config.skills } : {}),
```

`process-executor.ts`（先读全文件确认结构再改）：

1. import 加 `import type { SkillRecord } from './skill-store.js';`
2. opts 接口（`capabilities?: () => Promise<CapabilityDef[]>;` 附近）加：

```ts
  /** Skill 注入（2026-09-06 fork）：分派时刻按员工分类取 approved 快照随 job 下发 */
  skillsFor?: (employeeId: string) => Promise<SkillRecord[]>;
```

3. 在构造 job config 处（`...(opts.capabilities ? { capabilities: await opts.capabilities() } : {}),` 附近）加：

```ts
      ...(opts.skillsFor ? { skills: await opts.skillsFor(employee.id) } : {}),
```

（`employee` 变量名以文件实际为准——分派上下文中已解析出员工档案。）

`pipeline.ts`：

1. import 加 `import type { SkillRecord } from './skill-store.js';`
2. `ConsoleRuntimeOptions` 的 `capabilities?: () => Promise<CapabilityDef[]>;` 之后加：

```ts
  /** Skill 注入（2026-09-06）：按员工 id 实时取 approved 清单；inproc 每任务执行前取，fork 分派时取快照 */
  skillsFor?: (employeeId: string) => Promise<SkillRecord[]>;
```

3. fork 分支 `createProcessExecutor({...})` 里 `...(opts.capabilities ? { capabilities: opts.capabilities } : {}),` 之后加 `...(opts.skillsFor ? { skillsFor: opts.skillsFor } : {}),`
4. inproc 分支 `createEmployeeExecutor({...})` 里 `capabilities: opts.capabilities,` 之后加 `...(opts.skillsFor ? { skillsFor: opts.skillsFor } : {}),`

- [ ] **Step 4: 跑测试 + 类型检查确认通过**

Run: `cd packages/console-api && pnpm vitest run`
Expected: 全量 PASS

- [ ] **Step 5: Commit**

```bash
git add packages/console-api/src/team/worker.ts packages/console-api/src/team/process-executor.ts packages/console-api/src/team/pipeline.ts packages/console-api/test/worker.test.ts
git commit -m "feat(console-api): fork 链路 Skill 快照——worker config.skills + process-executor skillsFor + pipeline 接线

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 7: 蒸馏沉淀（scheduler onTaskComplete + skill-distiller + 消息）

**Files:**
- Create: `packages/console-api/src/team/skill-distiller.ts`
- Modify: `packages/console-api/src/team/scheduler.ts`（onTaskComplete 钩子）
- Modify: `packages/console-api/src/team/pipeline.ts`（opts 透传）
- Modify: `packages/console-api/src/team/messages.ts`（MessageType 加 skill_pending）
- Modify: `packages/console-api/src/team/message-hub.ts`（事件 → skill_pending 消息）
- Modify: `packages/console-api/src/team/task-parser.ts`（导出 replyToText 供蒸馏器复用）
- Modify: `packages/console-api/src/http/server.ts`（蒸馏器接线）
- Test: `packages/console-api/test/skill-distiller.test.ts`（新建）、`packages/console-api/test/scheduler.test.ts`（追加）

**Interfaces:**
- Consumes: Task 1 的 `FileSkillStore`/`newSkillId`/`SkillRecord`；task-parser 的模型调用模式（`gateway.streamFnFor('chat')`）
- Produces:
  - `createSkillDistiller(deps: { gateway: ModelGateway; skillStore: FileSkillStore; events: EventStore; workspaceRoot: string; maxPerTask?: number }): (input: { task: TaskPackage; employee: EmployeeProfile }) => Promise<SkillRecord[]>`
  - `MAX_CANDIDATES_PER_TASK = 2`
  - `TeamSchedulerDeps.onTaskComplete?: (task: TaskPackage, employee: EmployeeProfile) => Promise<void> | void`（仅 ok=true 时异步触发，异常吞掉只 console.error）
  - `MessageType = 'review_required' | 'task_failed' | 'task_done' | 'skill_pending'`

- [ ] **Step 1: 写失败测试**

```ts
// packages/console-api/test/skill-distiller.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { mkdtemp, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { FileSkillStore } from '../src/team/skill-store.js';
import { createSkillDistiller, MAX_CANDIDATES_PER_TASK } from '../src/team/skill-distiller.js';
import type { AgentEvent, TaskPackage } from '@ddw/runtime';

const task: TaskPackage = {
  taskId: 'T-DISTILL', title: '登录接口开发', repo: { url: 'http://x/a.git', branch: 'main' },
  tasks: [],
} as never;

const events: AgentEvent[] = [];
const memoryEvents = {
  append: async (e: AgentEvent) => { events.push(e); },
  list: async () => events,
};

let dir: string;
let store: FileSkillStore;

beforeEach(async () => {
  dir = await mkdtemp(join(tmpdir(), 'distill-'));
  store = new FileSkillStore(join(dir, 'skills.json'));
  await store.ensureSeed();
  events.length = 0;
});

/** 模型替身：返回固定 JSON 数组 */
const fakeGateway = (reply: string) => ({
  streamFnFor: () => async () => reply,
  modelFor: () => ({}),
}) as never;

const CANDIDATES = JSON.stringify([
  { categoryId: 'backend', type: 'knowledge', name: '登录限流经验', description: '登录接口限流策略', content: '登录失败 5 次锁定…' },
  { categoryId: 'backend', type: 'constraint', name: '密码不落日志', description: '安全约束', content: '任何日志不得输出明文密码' },
  { categoryId: 'nope', type: 'knowledge', name: '坏分类', description: '', content: 'x' },
]);

describe('skill-distiller（2026-09-06 自动沉淀）', () => {
  it('模型产出候选 → pending 入库，source=auto:<taskId>，坏分类条目跳过', async () => {
    const distill = createSkillDistiller({
      gateway: fakeGateway(`\`\`\`json\n${CANDIDATES}\n\`\`\``),
      skillStore: store, events: memoryEvents as never, workspaceRoot: dir,
    });
    const got = await distill({ task, employee: { id: 'emp-1', name: '甲', role: 'backend', skills: [] } as never });
    expect(got.length).toBe(2); // 第三条坏分类被跳过
    const all = await store.listSkills();
    expect(all.every((s) => s.status === 'pending')).toBe(true);
    expect(all.every((s) => s.source === 'auto:T-DISTILL')).toBe(true);
    expect(all.every((s) => s.sourceTaskId === 'T-DISTILL')).toBe(true);
  });

  it('每任务上限 2 条（MAX_CANDIDATES_PER_TASK）', async () => {
    expect(MAX_CANDIDATES_PER_TASK).toBe(2);
    const many = JSON.stringify(Array.from({ length: 5 }, (_, i) => ({
      categoryId: 'backend', type: 'knowledge', name: `经验${i}`, description: '', content: 'c',
    })));
    const distill = createSkillDistiller({
      gateway: fakeGateway(many), skillStore: store, events: memoryEvents as never, workspaceRoot: dir,
    });
    const got = await distill({ task, employee: { id: 'emp-1', name: '甲', role: 'backend', skills: [] } as never });
    expect(got.length).toBe(2);
  });

  it('模型输出不可解析：返回空数组 + 事件留痕，不抛错', async () => {
    const distill = createSkillDistiller({
      gateway: fakeGateway('这不是 JSON'), skillStore: store, events: memoryEvents as never, workspaceRoot: dir,
    });
    const got = await distill({ task, employee: { id: 'emp-1', name: '甲', role: 'backend', skills: [] } as never });
    expect(got).toEqual([]);
    expect((await store.listSkills()).length).toBe(0);
    expect(events.some((e) => e.summary.includes('沉淀'))).toBe(true);
  });

  it('成功入库后追加留痕事件（summary 含「待审查」）', async () => {
    const distill = createSkillDistiller({
      gateway: fakeGateway(`\`\`\`json\n${CANDIDATES}\n\`\`\``),
      skillStore: store, events: memoryEvents as never, workspaceRoot: dir,
    });
    await distill({ task, employee: { id: 'emp-1', name: '甲', role: 'backend', skills: [] } as never });
    const e = events.find((ev) => ev.summary.includes('待审查'));
    expect(e).toBeDefined();
    expect((e!.payload as { skillPending?: boolean }).skillPending).toBe(true);
  });
});
```

scheduler.test.ts 追加（复用该文件既有的内存 TaskStore/faux executor 装配；若文件内是手写替身，按下例补一个最小 deps 变体）：

```ts
it('onTaskComplete：任务成功完成触发一次，失败不触发，异常不炸 tick（2026-09-06 蒸馏钩子）', async () => {
  // 用既有装配构造 scheduler（tasks/events/roster/executor 同文件既有替身），外加：
  const calls: string[] = [];
  const scheduler = new TeamScheduler({
    tasks, events, roster, executor: okExecutor,
    onTaskComplete: async (task, employee) => { calls.push(`${task.taskId}:${employee.id}`); },
  });
  // pending 任务 + 成功 executor（同文件既有用例装配方式）→ tick
  await scheduler.tick();
  expect(calls).toEqual(['T1:emp-1']);
  // 失败 executor 装配（复用既有 failedExecutor 替身）→ 不触发
  // executor 抛异常的 onTaskComplete 不炸 tick：onTaskComplete: async () => { throw new Error('x'); }
});
```

（具体替身名以 scheduler.test.ts 既有代码为准——**只追加新用例，不改既有用例**。若该文件每个用例独立装配，新用例照抄最近一个成功/失败用例的装配，仅加 `onTaskComplete`。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/console-api && pnpm vitest run test/skill-distiller.test.ts test/scheduler.test.ts`
Expected: FAIL

- [ ] **Step 3: 实现**

**`task-parser.ts`**：`async function replyToText` 改为导出：`export async function replyToText`，并追加导出 JSON 提取：

```ts
/** 从模型回复中提取 JSON 数组（```json 围栏优先，其次首个 [ ... ] 区段；无则抛错） */
export function extractJsonArray(reply: string): unknown[] {
  const fenced = reply.match(/```(?:json)?\s*\n([\s\S]*?)```/);
  const text = fenced?.[1] ?? reply;
  const start = text.indexOf('[');
  const end = text.lastIndexOf(']');
  if (start < 0 || end <= start) throw new Error('模型输出中未找到 JSON 数组');
  return JSON.parse(text.slice(start, end + 1)) as unknown[];
}
```

**`skill-distiller.ts`（新建）**：

```ts
// packages/console-api/src/team/skill-distiller.ts
import { exec } from 'node:child_process';
import { promisify } from 'node:util';
import { randomUUID } from 'node:crypto';
import { join } from 'node:path';
import type { EmployeeProfile, ModelGateway, TaskPackage } from '@ddw/runtime';
import type { EventStore } from '../stores/index.js';
import { FileSkillStore, newSkillId, SKILL_TYPES, type SkillRecord } from './skill-store.js';
import { extractJsonArray, replyToText } from './task-parser.js';

const runCmd = promisify(exec);

/** 每任务蒸馏候选上限（spec §五：防刷库） */
export const MAX_CANDIDATES_PER_TASK = 2;

export function buildDistillSystemPrompt(categoryIds: string[]): string {
  return [
    '你是研发知识蒸馏器。根据任务包、执行过程摘要与代码变更摘要，提炼可复用的经验沉淀为候选 Skill。',
    '只输出一个 ```json 围栏代码块，内容为 JSON 数组，每项：',
    '{ "categoryId": <分类id>, "type": "knowledge"|"constraint"|"asset", "name": <名称>, "description": <一句话说明>, "content": <markdown 正文> }',
    `categoryId 只能取：${categoryIds.join(', ') || '（无可用分类）'}。`,
    'type 语义：knowledge=可复用知识；constraint=开发约束（禁止/必须）；asset=可执行资产（此时附 assetFiles: [{ path, content }]，path 相对路径）。',
    '只提炼本次任务中真正可复用的内容；没有值得沉淀的就输出空数组 []。最多 3 条。',
  ].join('\n');
}

export interface DistillerDeps {
  gateway: ModelGateway;
  skillStore: FileSkillStore;
  events: EventStore;
  workspaceRoot: string;
  maxPerTask?: number;
}

/**
 * 任务完成蒸馏器（spec §五）：任务全部计划项 done 后异步触发——
 * 任务包 + 执行事件摘要 + git diff 摘要 → 模型产出候选 skill JSON → pending 入库 + 留痕。
 * 任何失败（模型/解析/校验）只事件留痕，不抛错不影响任务终态。
 */
export function createSkillDistiller(deps: DistillerDeps): (input: { task: TaskPackage; employee: EmployeeProfile }) => Promise<SkillRecord[]> {
  const maxPerTask = deps.maxPerTask ?? MAX_CANDIDATES_PER_TASK;
  return async ({ task, employee }) => {
    const wsDir = join(deps.workspaceRoot, employee.id, task.taskId.replaceAll('/', '_'));
    let diff = '';
    try {
      const { stdout } = await runCmd(`git -C "${wsDir}" diff HEAD --stat`, { timeout: 10_000 });
      diff = stdout.slice(0, 3000);
    } catch { /* 无 git 现场（faux 测试等）→ 空摘要继续 */ }
    const timeline = (await deps.events.list({ taskId: task.taskId }))
      .slice(-50)
      .map((e) => `[${e.type}] ${e.summary}`)
      .join('\n');

    const categories = await deps.skillStore.listCategories();
    const categoryIds = categories.map((c) => c.id);
    let raw: string;
    try {
      const stream = deps.gateway.streamFnFor('chat');
      const reply = await stream(deps.gateway.modelFor('chat'), {
        systemPrompt: buildDistillSystemPrompt(categoryIds),
        messages: [{
          role: 'user',
          content: [
            `任务包：${JSON.stringify({ taskId: task.taskId, title: task.title, role: task.role, plan: task.plan ?? [] })}`,
            `执行过程摘要：\n${timeline || '（无）'}`,
            `代码变更摘要：\n${diff || '（无）'}`,
          ].join('\n\n'),
          timestamp: Date.now(),
        }],
      } as never);
      raw = await replyToText(reply);
    } catch (e) {
      await deps.events.append({
        id: `distill-${task.taskId}-${randomUUID().slice(0, 8)}`, ts: Date.now(), taskId: task.taskId,
        employeeId: employee.id, type: 'report',
        summary: `Skill 沉淀失败（模型调用）：${e instanceof Error ? e.message : String(e)}`,
      });
      return [];
    }

    let candidates: Array<Record<string, unknown>>;
    try {
      candidates = extractJsonArray(raw) as Array<Record<string, unknown>>;
    } catch (e) {
      await deps.events.append({
        id: `distill-${task.taskId}-${randomUUID().slice(0, 8)}`, ts: Date.now(), taskId: task.taskId,
        employeeId: employee.id, type: 'report',
        summary: `Skill 沉淀失败（输出不可解析）：${e instanceof Error ? e.message : String(e)}`,
      });
      return [];
    }

    const created: SkillRecord[] = [];
    for (const c of candidates) {
      if (created.length >= maxPerTask) break;
      const categoryId = String(c.categoryId ?? '');
      const type = String(c.type ?? '');
      const name = String(c.name ?? '').trim();
      const content = String(c.content ?? '').trim();
      // 逐条校验：分类/类型/非空——坏条目跳过不炸整批
      if (!categoryIds.includes(categoryId) || !SKILL_TYPES.has(type as never) || !name || !content) continue;
      const assetFiles = Array.isArray(c.assetFiles)
        ? (c.assetFiles as Array<{ path?: unknown; content?: unknown }>).map((f) => ({ path: String(f.path ?? ''), content: String(f.content ?? '') }))
        : undefined;
      const rec: SkillRecord = {
        id: newSkillId(), categoryId, name, description: String(c.description ?? ''),
        type: type as SkillRecord['type'], content,
        ...(assetFiles?.length ? { assetFiles } : {}),
        status: 'pending', source: `auto:${task.taskId}`, sourceTaskId: task.taskId, createdAt: Date.now(),
      };
      try {
        await deps.skillStore.upsertSkill(rec);
        created.push(rec);
      } catch { /* 单条入库失败（如 assetFiles 校验）跳过 */ }
    }

    await deps.events.append({
      id: `distill-${task.taskId}-${randomUUID().slice(0, 8)}`, ts: Date.now(), taskId: task.taskId,
      employeeId: employee.id, type: 'report',
      summary: created.length > 0
        ? `任务 ${task.taskId} 沉淀 ${created.length} 条候选 Skill 待审查`
        : `任务 ${task.taskId} 无可沉淀内容`,
      payload: { ...(created.length > 0 ? { skillPending: true, count: created.length } : {}) },
    });
    return created;
  };
}
```

**`scheduler.ts`**：

1. `TeamSchedulerDeps` 的 `executor: EmployeeExecutor;` 之前加：

```ts
  /** 任务成功完成回调（2026-09-06 蒸馏钩子）：仅 ok=true 触发，异步不阻塞 finish 回写；
   *  回调内部异常只 console.error，不得炸 tick */
  onTaskComplete?: (task: TaskPackage, employee: EmployeeProfile) => Promise<void> | void;
```

2. import 加 `import type { TaskPackage } from '@ddw/runtime';`（若该行已存在则合并）。
3. `runOne` 内 `this.deps.tasks.finish(...)` 之后、`this.emit('dispatch', ... ok ? ...)` 之前加：

```ts
    if (ok && this.deps.onTaskComplete) {
      void Promise.resolve(this.deps.onTaskComplete(rec.pkg, employee))
        .catch((e) => console.error('[scheduler] onTaskComplete 失败:', e instanceof Error ? e.message : e));
    }
```

**`pipeline.ts`**：`ConsoleRuntimeOptions` 的 `onTickError?` 之前加：

```ts
  /** 任务成功完成回调（2026-09-06 蒸馏钩子）：透传给调度器（仅 ok=true 触发） */
  onTaskComplete?: (task: TaskPackage, employee: EmployeeProfile) => Promise<void> | void;
```

`new TeamScheduler({...})` 的 `...(opts.canDispatch ? ... )` 之后加 `...(opts.onTaskComplete ? { onTaskComplete: opts.onTaskComplete } : {}),`。import 确认 `TaskPackage` 已从 '@ddw/runtime' 导入（`onTaskComplete` 类型签名用到；已有则合并）。

**`messages.ts`**：`export type MessageType = 'review_required' | 'task_failed' | 'task_done';` 改：

```ts
export type MessageType = 'review_required' | 'task_failed' | 'task_done' | 'skill_pending';
```

**`message-hub.ts`**：`msg()` 的 title 三元改为：

```ts
  title: type === 'review_required'
    ? `任务 ${taskId} 待人工放行`
    : type === 'task_failed'
      ? `任务 ${taskId} 执行失败`
      : type === 'skill_pending'
        ? `任务 ${taskId} 沉淀 Skill 待审查`
        : `任务 ${taskId} 执行完成`,
```

`wrapEventStoreForMessages` 的 append 内、`task_check` 分支之后加：

```ts
      // Skill 沉淀候选（2026-09-06 蒸馏器）：report 事件 payload.skillPending=true → 待审查消息
      if (e.type === 'report' && (e.payload as { skillPending?: boolean } | undefined)?.skillPending === true) {
        await sink.onMessage(msg('skill_pending', e.taskId, e.summary, e.employeeId));
      }
```

**`server.ts`**：在 `const pipeline = opts.runtime ? ...` **之前**加（gateway 声明在 pipeline 之后，故蒸馏器用惰性 gateway getter 不可行——直接把 gateway 声明上移）：把现有

```ts
  const gateway =
    opts.runtime && hasChatRoute(opts.runtime.routes) ? new ModelGateway(opts.runtime.routes) : undefined;
```

**整体移动到 `const pipeline = ...` 之前**（handlers 里 `gateway` 引用不变）。然后在 pipeline 的 opts 对象里 `...(mcpHub ? { mcpHub, mcpServers: opts.runtime?.mcpServers } : {}),` 之后加：

```ts
        // 蒸馏沉淀钩子（2026-09-06）：任务成功完成后异步蒸馏候选 Skill（pending 入库 + 消息中心）
        ...(opts.runtime && skillStore && gateway
          ? {
              onTaskComplete: (task: TaskPackage, employee: EmployeeProfile) =>
                distillFromTask({ gateway, skillStore, events: baseEvents, workspaceRoot: opts.runtime!.workspaceRoot }, { task, employee })
                  .catch((e: unknown) => {
                    console.error('[skill-distiller] 蒸馏失败:', e instanceof Error ? e.message : e);
                    return [] as never;
                  }),
            }
          : {}),
```

其中顶部 import 加：

```ts
import { createSkillDistiller as distillFromTask } from '../team/skill-distiller.js';
import type { TaskPackage, EmployeeProfile } from '@ddw/runtime'; // 已有则合并
```

注意：`baseEvents` 是 server.ts 中装饰器包装前的原始事件 store 变量名（`const baseTasks = ...; const baseEvents = ...;`），蒸馏器走 baseEvents 避免触发消息装饰器递归（skill_pending 消息由蒸馏器自己的 report 事件经装饰器生成——**此处必须用 baseEvents**：若用包装后的 events 会导致 append 触发 sink 生成 skill_pending 消息（这正是我们想要的），但蒸馏失败留痕也会走装饰器——无害。取舍：**用包装后的 `events` 变量**（消息联动自动生效），上方代码写 `events: events`；若命名冲突以文件实际变量为准）。final: `events: events`（server.ts 中装饰器包装后的变量名）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd packages/console-api && pnpm vitest run`
Expected: 全量 PASS（若既有 handlers.test 断言 MessageType 集合，补充 skill_pending 断言）

- [ ] **Step 5: Commit**

```bash
git add packages/console-api/src/team/skill-distiller.ts packages/console-api/src/team/scheduler.ts packages/console-api/src/team/pipeline.ts packages/console-api/src/team/messages.ts packages/console-api/src/team/message-hub.ts packages/console-api/src/team/task-parser.ts packages/console-api/src/http/server.ts packages/console-api/test/skill-distiller.test.ts packages/console-api/test/scheduler.test.ts
git commit -m "feat(console-api): 任务完成自动蒸馏沉淀——scheduler onTaskComplete 钩子 + 模型候选入库 + skill_pending 消息

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 8: 前端 SkillLibrary.vue + 路由/菜单 + api

**Files:**
- Modify: `apps/console/src/api.ts`
- Create: `apps/console/src/views/SkillLibrary.vue`
- Modify: `apps/console/src/router.ts`、`apps/console/src/App.vue`
- Test: `apps/console/test/skill-library.test.ts`（新建）

**Interfaces:**
- Consumes: Task 2 的 HTTP 路由
- Produces: 前端页面 `/skills`（检索区 + 表格 + 审查流 + 分类管理对话框）

- [ ] **Step 1: 写失败测试**

```ts
// apps/console/test/skill-library.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { flushPromises } from '@vue/test-utils';
import SkillLibrary from '../src/views/SkillLibrary.vue';
import { api, type SkillRecordView, type SkillCategoryView } from '../src/api.js';
import { mountWithEP } from './mount-helper.js';

vi.mock('element-plus', async (importOriginal) => {
  const actual = await importOriginal<Record<string, unknown>>();
  return {
    ...actual,
    ElMessage: { success: vi.fn(), error: vi.fn(), warning: vi.fn() },
    ElMessageBox: { confirm: vi.fn().mockResolvedValue(true) },
  };
});

vi.mock('../src/api.js', () => ({
  api: {
    listSkills: vi.fn(),
    createSkill: vi.fn(),
    updateSkill: vi.fn(),
    deleteSkill: vi.fn(),
    reviewSkill: vi.fn(),
    listSkillCategories: vi.fn(),
    createSkillCategory: vi.fn(),
    deleteSkillCategory: vi.fn(),
  },
}));

import { ElMessage, ElMessageBox } from 'element-plus';

const mockedApi = vi.mocked(api, true);
const mockedMsg = vi.mocked(ElMessage, true);

const CATS: SkillCategoryView[] = [
  { id: 'backend', name: '后端开发' }, { id: 'frontend', name: '前端开发' },
];
const SKILLS: SkillRecordView[] = [
  { id: 'skill-1', categoryId: 'backend', name: '异常码规范', description: 'REST 约定', type: 'knowledge', content: 'E 开头', status: 'pending', source: 'auto:T1', sourceTaskId: 'T1', createdAt: 1 },
  { id: 'skill-2', categoryId: 'backend', name: '禁直连生产库', description: '', type: 'constraint', content: '禁止', status: 'approved', source: 'manual', createdAt: 1 },
];

beforeEach(() => {
  vi.clearAllMocks();
  mockedApi.listSkillCategories.mockResolvedValue(CATS);
  mockedApi.listSkills.mockResolvedValue(SKILLS);
});

describe('SkillLibrary（2026-09-06 Skill 库页）', () => {
  it('渲染表格：名称/分类/类型/状态；pending 行有批准/驳回按钮', async () => {
    const wrapper = mountWithEP(SkillLibrary);
    await flushPromises();
    expect(wrapper.text()).toContain('异常码规范');
    expect(wrapper.text()).toContain('禁直连生产库');
    const rows = wrapper.findAll('[data-test="skill-table"] .el-table__row');
    expect(rows[0]!.find('[data-test="approve-btn"]').exists()).toBe(true);
    expect(rows[0]!.find('[data-test="reject-btn"]').exists()).toBe(true);
    expect(rows[1]!.find('[data-test="approve-btn"]').exists()).toBe(false); // 非 pending 无审查按钮
  });

  it('审查流：批准 → reviewSkill(approve) + 刷新', async () => {
    mockedApi.reviewSkill.mockResolvedValue({ id: 'skill-1', status: 'approved' } as never);
    const wrapper = mountWithEP(SkillLibrary);
    await flushPromises();
    await wrapper.find('[data-test="approve-btn"]').trigger('click');
    await flushPromises();
    expect(mockedApi.reviewSkill).toHaveBeenCalledWith('skill-1', 'approve');
    expect(mockedApi.listSkills).toHaveBeenCalledTimes(2);
    expect(mockedMsg.success).toHaveBeenCalled();
  });

  it('驳回：confirm 后 reviewSkill(reject)', async () => {
    mockedApi.reviewSkill.mockResolvedValue({ id: 'skill-1', status: 'rejected' } as never);
    const wrapper = mountWithEP(SkillLibrary);
    await flushPromises();
    await wrapper.find('[data-test="reject-btn"]').trigger('click');
    await flushPromises();
    expect(mockedApi.reviewSkill).toHaveBeenCalledWith('skill-1', 'reject');
  });

  it('检索过滤 + 待审查徽标', async () => {
    const wrapper = mountWithEP(SkillLibrary);
    await flushPromises();
    expect(wrapper.find('[data-test="pending-badge"]').text()).toContain('1');
    await wrapper.find('[data-test="skills-search"]').setValue('禁直连');
    const rows = wrapper.findAll('[data-test="skill-table"] .el-table__row');
    expect(rows.length).toBe(1);
    expect(rows[0]!.text()).toContain('禁直连生产库');
  });

  it('新建 skill：对话框提交 → createSkill（201 后刷新）', async () => {
    mockedApi.createSkill.mockResolvedValue({ id: 'skill-9', status: 'pending' } as never);
    const wrapper = mountWithEP(SkillLibrary);
    await flushPromises();
    await wrapper.find('[data-test="create-btn"]').trigger('click');
    await wrapper.find('[data-test="d-name"]').setValue('新技能');
    await wrapper.find('[data-test="d-content"]').setValue('正文');
    await wrapper.find('[data-test="save-btn"]').trigger('click');
    await flushPromises();
    expect(mockedApi.createSkill).toHaveBeenCalled();
    expect(mockedMsg.success).toHaveBeenCalled();
  });

  it('删除：confirm → deleteSkill', async () => {
    mockedApi.deleteSkill.mockResolvedValue({ id: 'skill-1', removed: true } as never);
    const wrapper = mountWithEP(SkillLibrary);
    await flushPromises();
    const row = wrapper.findAll('[data-test="skill-table"] .el-table__row')[0]!;
    await row.find('[data-test="delete-btn"]').trigger('click');
    await flushPromises();
    expect(mockedApi.deleteSkill).toHaveBeenCalledWith('skill-1');
  });

  it('分类管理：打开对话框可见分类，可新增', async () => {
    mockedApi.createSkillCategory.mockResolvedValue({ id: 'data' } as never);
    const wrapper = mountWithEP(SkillLibrary);
    await flushPromises();
    await wrapper.find('[data-test="manage-cats-btn"]').trigger('click');
    await flushPromises();
    expect(wrapper.text()).toContain('后端开发');
    await wrapper.find('[data-test="cat-id"]').setValue('data');
    await wrapper.find('[data-test="cat-name"]').setValue('数据开发');
    await wrapper.find('[data-test="cat-save-btn"]').trigger('click');
    await flushPromises();
    expect(mockedApi.createSkillCategory).toHaveBeenCalledWith({ id: 'data', name: '数据开发' });
    void ElMessageBox;
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/console && pnpm vitest run test/skill-library.test.ts`
Expected: FAIL（页面不存在）

- [ ] **Step 3: 实现**

**`api.ts`** 类型区（`NotificationChannelView` 之后）加：

```ts
/** Skill 类型：knowledge 知识 / constraint 约束 / asset 可执行资产（2026-09-06 Skill 库） */
export type SkillType = 'knowledge' | 'constraint' | 'asset';

export interface SkillRecordView {
  id: string;
  categoryId: string;
  name: string;
  description: string;
  type: SkillType;
  content: string;
  assetFiles?: { path: string; content: string }[];
  status: 'pending' | 'approved' | 'rejected';
  source: string;
  sourceTaskId?: string;
  createdAt: number;
  reviewedAt?: number;
}

export interface SkillCategoryView { id: string; name: string; description?: string; }
```

api 对象加（通知渠道方法之后）：

```ts
  // Skill 库（2026-09-06）：分类 + skill CRUD + 人工终审
  listSkills(query: { status?: string; categoryId?: string; q?: string } = {}): Promise<SkillRecordView[]> {
    const qs = new URLSearchParams();
    if (query.status) qs.set('status', query.status);
    if (query.categoryId) qs.set('categoryId', query.categoryId);
    if (query.q) qs.set('q', query.q);
    const s = qs.toString();
    return request('GET', `/api/skills${s ? `?${s}` : ''}`);
  },
  createSkill(rec: Omit<SkillRecordView, 'id' | 'status' | 'createdAt' | 'reviewedAt'>): Promise<{ id: string; status: string }> {
    return request('POST', '/api/skills', rec);
  },
  updateSkill(id: string, rec: Partial<SkillRecordView>): Promise<{ id: string }> {
    return request('PUT', `/api/skills/${id}`, rec);
  },
  deleteSkill(id: string): Promise<{ id: string; removed: boolean }> {
    return request('DELETE', `/api/skills/${id}`);
  },
  reviewSkill(id: string, action: 'approve' | 'reject'): Promise<{ id: string; status: string }> {
    return request('POST', `/api/skills/${id}/review`, { action });
  },
  listSkillCategories(): Promise<SkillCategoryView[]> {
    return request('GET', '/api/skill-categories');
  },
  createSkillCategory(cat: SkillCategoryView): Promise<{ id: string }> {
    return request('POST', '/api/skill-categories', cat);
  },
  deleteSkillCategory(id: string): Promise<{ id: string; removed: boolean }> {
    return request('DELETE', `/api/skill-categories/${id}`);
  },
```

**`views/SkillLibrary.vue`（新建）**：

```vue
<script setup lang="ts">
import { computed, onMounted, ref } from 'vue';
import { ElMessage, ElMessageBox } from 'element-plus';
import { api, type SkillRecordView, type SkillCategoryView, type SkillType } from '../api.js';

const skills = ref<SkillRecordView[]>([]);
const categories = ref<SkillCategoryView[]>([]);
const loading = ref(false);
const error = ref('');

/** 检索区（列表页风格统一）：关键词 + 分类 + 状态 */
const keyword = ref('');
const filterCategory = ref('');
const filterStatus = ref('');

const TYPE_ZH: Record<SkillType, string> = { knowledge: '知识', constraint: '约束', asset: '资产' };
const STATUS_ZH: Record<string, string> = { pending: '待审查', approved: '已批准', rejected: '已驳回' };
const STATUS_TAG: Record<string, 'warning' | 'success' | 'danger'> = { pending: 'warning', approved: 'success', rejected: 'danger' };

const matches = (s: SkillRecordView): boolean => {
  const k = keyword.value.trim().toLowerCase();
  return (!k || s.name.toLowerCase().includes(k) || s.description.toLowerCase().includes(k))
    && (!filterCategory.value || s.categoryId === filterCategory.value)
    && (!filterStatus.value || s.status === filterStatus.value);
};
const filtered = computed(() => skills.value.filter(matches));
const pendingCount = computed(() => skills.value.filter((s) => s.status === 'pending').length);
defineExpose({ matches });

const catName = (id: string): string => categories.value.find((c) => c.id === id)?.name ?? id;

async function load() {
  loading.value = true;
  error.value = '';
  try {
    const [list, cats] = await Promise.all([api.listSkills(), api.listSkillCategories()]);
    skills.value = list;
    categories.value = cats;
  } catch (e) {
    error.value = e instanceof Error ? e.message : String(e);
  } finally {
    loading.value = false;
  }
}

/* ---- 新建/编辑对话框 ---- */
const dialogVisible = ref(false);
const editingId = ref('');
const saving = ref(false);
const draft = ref(emptyDraft());
const assetRows = ref<{ path: string; content: string }[]>([]);

function emptyDraft() {
  return { categoryId: 'backend', name: '', description: '', type: 'knowledge' as SkillType, content: '' };
}

function openCreate() {
  editingId.value = '';
  draft.value = emptyDraft();
  assetRows.value = [];
  dialogVisible.value = true;
}

function openEdit(row: SkillRecordView) {
  editingId.value = row.id;
  draft.value = { categoryId: row.categoryId, name: row.name, description: row.description, type: row.type, content: row.content };
  assetRows.value = (row.assetFiles ?? []).map((f) => ({ ...f }));
  dialogVisible.value = true;
}

async function save() {
  saving.value = true;
  try {
    const payload = {
      ...draft.value,
      ...(draft.value.type === 'asset' && assetRows.value.length ? { assetFiles: assetRows.value } : {}),
    };
    if (editingId.value) {
      await api.updateSkill(editingId.value, payload);
      ElMessage.success(`skill ${draft.value.name} 已更新`);
    } else {
      await api.createSkill(payload);
      ElMessage.success(`skill ${draft.value.name} 已提交待审查`);
    }
    dialogVisible.value = false;
    await load();
  } catch (e) {
    ElMessage.error(e instanceof Error ? e.message : String(e)); // 后端 400 原样展示
  } finally {
    saving.value = false;
  }
}

/* ---- 审查 ---- */
async function review(row: SkillRecordView, action: 'approve' | 'reject') {
  if (action === 'reject') {
    try { await ElMessageBox.confirm(`确认驳回 skill「${row.name}」？`, '驳回', { type: 'warning' }); } catch { return; }
  }
  try {
    await api.reviewSkill(row.id, action);
    ElMessage.success(action === 'approve' ? '已批准入库' : '已驳回');
    await load();
  } catch (e) {
    ElMessage.error(e instanceof Error ? e.message : String(e));
  }
}

async function remove(row: SkillRecordView) {
  try { await ElMessageBox.confirm(`确认删除 skill「${row.name}」？`, '删除', { type: 'warning' }); } catch { return; }
  try {
    await api.deleteSkill(row.id);
    ElMessage.success('已删除');
    await load();
  } catch (e) {
    ElMessage.error(e instanceof Error ? e.message : String(e));
  }
}

/* ---- 分类管理对话框 ---- */
const catsVisible = ref(false);
const newCat = ref({ id: '', name: '' });

async function addCategory() {
  try {
    await api.createSkillCategory({ id: newCat.value.id.trim(), name: newCat.value.name.trim() });
    ElMessage.success('分类已新增');
    newCat.value = { id: '', name: '' };
    await load();
  } catch (e) {
    ElMessage.error(e instanceof Error ? e.message : String(e));
  }
}

async function removeCategory(cat: SkillCategoryView) {
  try { await ElMessageBox.confirm(`确认删除分类「${cat.name}」？`, '删除分类', { type: 'warning' }); } catch { return; }
  try {
    await api.deleteSkillCategory(cat.id);
    ElMessage.success('分类已删除');
    await load();
  } catch (e) {
    ElMessage.error(e instanceof Error ? e.message : String(e)); // 409 挂载保护文案原样展示
  }
}

onMounted(load);
</script>

<template>
  <section class="view">
    <h2>Skill 库 <span class="ml-3 text-sm font-normal text-gray-400">数字员工公用技能库 · 入库需人工终审</span></h2>

    <div class="flex items-center gap-3 mb-4">
      <el-input v-model="keyword" data-test="skills-search" placeholder="按名称/说明检索" clearable class="!w-56" />
      <el-select v-model="filterCategory" data-test="filter-category" placeholder="全部分类" clearable class="!w-40" :teleported="false">
        <el-option v-for="c in categories" :key="c.id" :label="c.name" :value="c.id" />
      </el-select>
      <el-select v-model="filterStatus" data-test="filter-status" placeholder="全部状态" clearable class="!w-36" :teleported="false">
        <el-option label="待审查" value="pending" />
        <el-option label="已批准" value="approved" />
        <el-option label="已驳回" value="rejected" />
      </el-select>
      <el-badge :value="pendingCount" :hidden="pendingCount === 0" data-test="pending-badge">
        <span class="text-sm text-gray-400">待审查</span>
      </el-badge>
      <el-alert v-if="error" :title="error" type="error" :closable="false" show-icon class="flex-1" />
      <div class="ml-auto flex gap-2">
        <el-button data-test="manage-cats-btn" @click="catsVisible = true">管理分类</el-button>
        <el-button data-test="create-btn" type="primary" @click="openCreate">新增 Skill</el-button>
      </div>
    </div>

    <el-card shadow="never">
      <p v-if="loading && skills.length === 0" class="text-gray-400">加载中…</p>
      <el-empty v-else-if="skills.length === 0" description="暂无 skill，任务完成后会自动沉淀候选" />
      <template v-else>
        <el-table :data="filtered" data-test="skill-table">
          <el-table-column prop="name" label="名称" min-width="150" />
          <el-table-column label="分类" width="110">
            <template #default="scope">{{ catName(scope.row.categoryId) }}</template>
          </el-table-column>
          <el-table-column label="类型" width="80">
            <template #default="scope"><el-tag size="small">{{ TYPE_ZH[scope.row.type] }}</el-tag></template>
          </el-table-column>
          <el-table-column prop="description" label="说明" min-width="160" show-overflow-tooltip />
          <el-table-column label="状态" width="100">
            <template #default="scope">
              <el-tag :type="STATUS_TAG[scope.row.status]" size="small">{{ STATUS_ZH[scope.row.status] }}</el-tag>
            </template>
          </el-table-column>
          <el-table-column label="来源" width="140">
            <template #default="scope">
              <span class="text-[13px] text-gray-500">{{ scope.row.source === 'manual' ? '手工录入' : scope.row.source }}</span>
            </template>
          </el-table-column>
          <el-table-column label="操作" width="230">
            <template #default="scope">
              <template v-if="scope.row.status === 'pending'">
                <el-button data-test="approve-btn" link type="success" @click="review(scope.row, 'approve')">批准</el-button>
                <el-button data-test="reject-btn" link type="warning" @click="review(scope.row, 'reject')">驳回</el-button>
              </template>
              <el-button data-test="edit-btn" link type="primary" @click="openEdit(scope.row)">编辑</el-button>
              <el-button data-test="delete-btn" link type="danger" @click="remove(scope.row)">删除</el-button>
            </template>
          </el-table-column>
        </el-table>
        <p v-if="filtered.length === 0" class="text-center text-gray-400 py-10">无匹配 skill</p>
      </template>
    </el-card>

    <!-- 新建/编辑对话框 -->
    <el-dialog v-model="dialogVisible" :title="editingId ? `编辑 Skill · ${editingId}` : '新增 Skill（提交后待审查）'" width="70%">
      <el-form label-width="120px">
        <el-form-item label="分类">
          <el-select v-model="draft.categoryId" data-test="d-category" class="!w-full" :teleported="false">
            <el-option v-for="c in categories" :key="c.id" :label="c.name" :value="c.id" />
          </el-select>
        </el-form-item>
        <el-form-item label="类型">
          <el-select v-model="draft.type" data-test="d-type" class="!w-full" :teleported="false">
            <el-option label="知识（knowledge）" value="knowledge" />
            <el-option label="约束（constraint）" value="constraint" />
            <el-option label="资产（asset）" value="asset" />
          </el-select>
        </el-form-item>
        <el-form-item label="名称">
          <el-input data-test="d-name" v-model="draft.name" placeholder="接口异常码规范" />
        </el-form-item>
        <el-form-item label="说明">
          <el-input data-test="d-description" v-model="draft.description" placeholder="一句话说明（可空）" />
        </el-form-item>
        <el-form-item :label="draft.type === 'asset' ? '使用说明' : '正文'">
          <el-input data-test="d-content" v-model="draft.content" type="textarea" :rows="6" placeholder="markdown 正文（执行任务时注入员工上下文）" />
        </el-form-item>
        <el-form-item v-if="draft.type === 'asset'" label="资产文件">
          <div class="w-full">
            <div v-for="(f, i) in assetRows" :key="i" class="flex gap-2 mb-2">
              <el-input v-model="f.path" placeholder="相对路径，如 scripts/check.sh" class="!w-72" />
              <el-input v-model="f.content" type="textarea" :rows="2" placeholder="文件内容" class="flex-1" />
              <el-button link type="danger" @click="assetRows.splice(i, 1)">删除</el-button>
            </div>
            <el-button size="small" @click="assetRows.push({ path: '', content: '' })">+ 添加文件</el-button>
          </div>
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="dialogVisible = false">取消</el-button>
        <el-button data-test="save-btn" type="primary" :loading="saving" @click="save">保存</el-button>
      </template>
    </el-dialog>

    <!-- 分类管理对话框 -->
    <el-dialog v-model="catsVisible" title="管理技能分类" width="520px">
      <div class="mb-3">
        <div v-for="c in categories" :key="c.id" class="flex items-center gap-2 py-1.5 border-b border-gray-100">
          <code class="w-24">{{ c.id }}</code>
          <span class="flex-1">{{ c.name }}</span>
          <el-button link type="danger" @click="removeCategory(c)">删除</el-button>
        </div>
      </div>
      <div class="flex gap-2">
        <el-input data-test="cat-id" v-model="newCat.id" placeholder="分类 id（如 data）" class="!w-40" />
        <el-input data-test="cat-name" v-model="newCat.name" placeholder="分类名称" class="!w-48" />
        <el-button data-test="cat-save-btn" type="primary" @click="addCategory">新增</el-button>
      </div>
    </el-dialog>
  </section>
</template>
```

**`router.ts`**：import 区加 `import SkillLibrary from './views/SkillLibrary.vue';`（按文件既有 import 风格），routes 数组 `'/capabilities'` 行后加：

```ts
    { path: '/skills', name: 'skills', component: SkillLibrary },
```

**`App.vue`**：菜单「能力管理」项后加：

```html
        <el-menu-item index="/skills"><el-icon><Reading /></el-icon>Skill 库</el-menu-item>
```

并把 import 改为 `import { Odometer, Tickets, CircleCheck, Avatar, Document, Cpu, SetUp, Bell, Reading } from '@element-plus/icons-vue';`

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/console && pnpm vitest run`
Expected: 全量 PASS

- [ ] **Step 5: Commit**

```bash
git add apps/console/src/api.ts apps/console/src/views/SkillLibrary.vue apps/console/src/router.ts apps/console/src/App.vue apps/console/test/skill-library.test.ts
git commit -m "feat(console): Skill 库管理页——检索/表格/审查流/分类管理（/skills 路由 + 侧边栏入口）

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 9: 员工编辑对话框分类多选 + 详情展示 + 消息类型收尾

**Files:**
- Modify: `apps/console/src/api.ts`（EmployeeRecordView 加 skillCategories；MessageView 加 skill_pending）
- Modify: `apps/console/src/components/EmployeeEditDialog.vue`
- Modify: `apps/console/src/views/EmployeeDetail.vue`
- Modify: `apps/console/src/views/CheckReview.vue` / 消息中心相关视图的类型引用（如有 MessageType 联合收窄 switch，编译器会提示——全局搜 `'task_done'` 核对）
- Test: `apps/console/test/employee-detail.test.ts`（追加——EmployeeEditDialog 现有测试在此文件）

**Interfaces:**
- Consumes: Task 3 的 employees GET 返回 `skillCategories`；Task 2 的 `/api/skill-categories`
- Produces: 员工表单技能分类多选；详情页展示

- [ ] **Step 1: 写失败测试（追加到 employee-detail.test.ts）**

```ts
it('技能分类多选：数据源来自 /api/skill-categories，随表单提交（2026-09-06 Skill 关联）', async () => {
  mockedApi.listSkillCategories.mockResolvedValue([
    { id: 'backend', name: '后端开发' }, { id: 'frontend', name: '前端开发' },
  ] as never);
  const wrapper = mountWithEP(EmployeeEditDialog, { props: { modelValue: true } });
  await flushPromises();
  const sel = wrapper.find('[data-test="d-skill-cats"]');
  expect(sel.exists()).toBe(true);
  // 勾选后提交 payload 含 skillCategories
  await wrapper.find('[data-test="d-id"]').setValue('emp-new');
  await wrapper.find('[data-test="d-name"]').setValue('小技');
  await wrapper.find('[data-test="d-role"]').setValue('backend');
  await wrapper.find('[data-test="d-skill-cats"]').findAll('.el-select__wrapper').length; // EP 多选交互以既有 d-caps 用例写法为准
  // 直接以组件方法层断言：设置表单值后 save
  (wrapper.vm as unknown as { form: { skillCategories: string[] } }).form.skillCategories = ['backend'];
  await wrapper.find('[data-test="save-btn"]').trigger('click');
  await flushPromises();
  const arg = mockedApi.createEmployee.mock.calls[0]?.[0] as { skillCategories: string[] };
  expect(arg.skillCategories).toEqual(['backend']);
});
```

（多选交互若既有 `d-caps` 用例有成熟的 select 选项点击写法，照搬该写法点选「后端开发」后断言 payload，优先于上面的 vm 内部赋值。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/console && pnpm vitest run test/employee-detail.test.ts`
Expected: FAIL（字段/下拉不存在）

- [ ] **Step 3: 实现**

**`api.ts`**：

1. `EmployeeRecordView` 的 `capabilities: string[];` 之后加：

```ts
  /** Skill 分类关联（2026-09-06）：该分类下已批准 skill 对该员工生效；空 = 不挂 */
  skillCategories: string[];
```

2. `MessageView` 的 type 联合改：`type: 'review_required' | 'task_failed' | 'task_done' | 'skill_pending';`

3. 全局搜（`grep -rn "'task_done'" apps/console/src/`）MessageView.type 的 switch/映射处，补 `skill_pending` 分支（如消息中心列表的类型徽标映射）：新增 `{ key: 'skill_pending', label: 'Skill 待审查', tag: 'warning' }` 样式的条目（以该文件既有条目形状为准）。

**`EmployeeEditDialog.vue`**：

1. `EmployeeDraft` 类型沿用 `Omit<EmployeeRecordView, 'busy' | 'runningTasks'>`（自动带上新字段）。
2. import 区把 `api` 的类型导入补 `type SkillCategoryView`；`const categories = ref<CapabilityDef[]>([]);` 附近加：

```ts
const skillCats = ref<SkillCategoryView[]>([]);
```

3. `emptyDraft()` 返回对象加 `skillCategories: [],`；编辑分支回填对象加 `skillCategories: [...(props.employee.skillCategories ?? [])],`
4. watch 打开分支拉数据处，`api.listCapabilities()` 的 try 里并行拉分类：

```ts
    try {
      const [caps, cats] = await Promise.all([api.listCapabilities(), api.listSkillCategories()]);
      capabilities.value = caps;
      skillCats.value = cats;
    } catch {
      capabilities.value = [];
      skillCats.value = [];
    }
```

（保持既有 catch 语义：失败不阻塞。）

5. 模板「能力绑定」`el-form-item` 之前加：

```html
      <el-form-item label="技能分类">
        <el-select
          data-test="d-skill-cats"
          v-model="form.skillCategories"
          multiple
          :teleported="false"
          placeholder="不选 = 不挂 Skill"
          class="!w-full"
        >
          <el-option v-for="c in skillCats" :key="c.id" :label="c.name" :value="c.id" />
        </el-select>
      </el-form-item>
```

**`EmployeeDetail.vue`**：「能力绑定」`el-descriptions-item`（约 114 行）之前加：

```html
        <el-descriptions-item label="技能分类" :span="3">
          <template v-if="employee.skillCategories?.length">
            <el-tag v-for="c in employee.skillCategories" :key="c" type="success" class="mr-1.5">{{ c }}</el-tag>
          </template>
          <span v-else class="text-gray-400">未挂分类（无 Skill 注入）</span>
        </el-descriptions-item>
```

（`employee` 变量名以文件实际为准。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/console && pnpm vitest run`
Expected: 全量 PASS（若有既有用例构造 EmployeeRecordView 字面量缺 skillCategories 导致 TS 报错，逐处补 `skillCategories: []`）

- [ ] **Step 5: Commit**

```bash
git add apps/console/src/api.ts apps/console/src/components/EmployeeEditDialog.vue apps/console/src/views/EmployeeDetail.vue apps/console/test/employee-detail.test.ts
git commit -m "feat(console): 员工编辑对话框技能分类多选 + 详情展示 + skill_pending 消息类型

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## 收尾清单（全任务完成后）

- [ ] 全量回归：`cd packages/console-api && pnpm vitest run && cd ../../apps/console && pnpm vitest run`
- [ ] `grep -rn "TBD\|TODO\|待定" packages/console-api/src/team/skill-*.ts apps/console/src/views/SkillLibrary.vue` 为空
- [ ] 对照 spec 八节测试清单逐项核对（store/handlers/distiller/注入/前端）
- [ ] 更新 `docs/阶段成果总账.md` 登记 Skill 管理模块（feat 列表 + 双提交链）
- [ ] 提醒用户：**重启 pnpm dev** 后 skills.json 才会创建、蒸馏钩子才生效
