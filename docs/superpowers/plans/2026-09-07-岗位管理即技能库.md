# 岗位管理即技能库 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 岗位成为受管实体（复用 SkillCategory），员工单选岗位，调度与 Skill 注入全部按岗位，员工 skills/skillCategories 两字段退役。

**Architecture:** SkillCategory 升级为岗位（name 作业务键加唯一约束）；`validateEmployeeRecord` 的白名单参数从分类 id 改为岗位名清单并严格校验 role；`ManagedRoster.acquire` 从 `skills.includes(role)` 改为 `role === role` 精确匹配；装配层 `skillsFor` 改为「员工 role → 按 name 查分类 → 该分类 approved skills」；前端员工编辑岗位改下拉、任务创建岗位下拉数据源改岗位清单、文案改「岗位管理」。

**Tech Stack:** TypeScript / Node（pnpm workspace）、vitest、Vue3 + element-plus + Tailwind v4。

## Global Constraints

- 全程简体中文注释与文案；conventional commits 中文描述
- 旧字段兼容：`skills` / `skillCategories` 读入不炸、忽略不报错；`EmployeeRecord.skills` 保持必填 `string[]`（seedFrom 与 EmployeeProfile 结构兼容），前端提交可以不带
- 调度点名路径（assignee → acquireById）语义不变
- 测试命令：`pnpm --filter console-api test`、`pnpm --filter console test`；全部任务完成后 `pnpm -r typecheck` 零错误
- runtime 包零改动（EmployeeRoster 旧语义仅在 yaml 回退路径，本次不动）
- 每任务结束提交一次 git commit

---

### Task 1: skill-store 分类 name 唯一性（岗位业务键）

**Files:**
- Modify: `packages/console-api/src/team/skill-store.ts:143-153`（upsertCategory）
- Test: `packages/console-api/test/skill-store.test.ts`

**Interfaces:**
- Produces: `FileSkillStore.upsertCategory(cat): Promise<'upserted' | 'name-conflict'>`——返回值语义，调用方（Task 4 handlers）映射 409

- [ ] **Step 1: 写失败测试**（追加到 `skill-store.test.ts` 既有 describe 内，复用该文件现有临时目录模式）

```ts
it('upsertCategory name 唯一（岗位业务键）：同名且非自身 → name-conflict 不落盘', async () => {
  const store = new FileSkillStore(join(dir, 'skills.json'));
  await store.upsertCategory({ id: 'a', name: '后端开发' });
  await store.upsertCategory({ id: 'b', name: '前端开发' });
  // 同名不同 id → 冲突
  const r1 = await store.upsertCategory({ id: 'c', name: '后端开发' });
  expect(r1).toBe('name-conflict');
  expect((await store.listCategories()).map((c) => c.id)).toEqual(['a', 'b']);
  // 同 id 更新自身 name → 放行
  const r2 = await store.upsertCategory({ id: 'a', name: '后端研发' });
  expect(r2).toBe('upserted');
  // 同名同 id（幂等重复提交）→ 放行
  const r3 = await store.upsertCategory({ id: 'a', name: '后端研发' });
  expect(r3).toBe('upserted');
});
```

- [ ] **Step 2: 跑测试确认失败**：`pnpm --filter console-api exec vitest run test/skill-store.test.ts`——期望 FAIL（返回值是 undefined 而非 'upserted'）

- [ ] **Step 3: 实现**——`upsertCategory` 改为带返回值 + 查重：

```ts
  /** 岗位 upsert（2026-09-07 岗位即分类）：name 为业务键必须唯一；返回冲突信号由 HTTP 层映射 409 */
  upsertCategory(cat: SkillCategory): Promise<'upserted' | 'name-conflict'> {
    const run = this.queue.then(async (): Promise<'upserted' | 'name-conflict'> => {
      const shape = await this.load();
      // 同名且 id 不同 → 冲突（同 id 重复提交 / 自身改名放行）
      if (shape.categories.some((c) => c.name === cat.name && c.id !== cat.id)) return 'name-conflict';
      const idx = shape.categories.findIndex((c) => c.id === cat.id);
      if (idx >= 0) shape.categories[idx] = { ...cat };
      else shape.categories.push({ ...cat });
      await this.flush(shape);
      return 'upserted';
    });
    this.queue = run.catch(() => undefined);
    return run;
  }
```

- [ ] **Step 4: 跑测试确认通过**（同 Step 2 命令）

- [ ] **Step 5: 提交**

```bash
git add packages/console-api/src/team/skill-store.ts packages/console-api/test/skill-store.test.ts
git commit -m "feat(team): 分类 upsert name 唯一校验——岗位业务键，冲突返回 name-conflict"
```

---

### Task 2: employee-store role 严格校验 + 旧字段退役

**Files:**
- Modify: `packages/console-api/src/team/employee-store.ts:47-91`（validateEmployeeRecord）、`14-30`（注释）、`57-62`（profileOf 侧在 Task 3 文件，此处只改 store）
- Test: `packages/console-api/test/employee-store.test.ts`

**Interfaces:**
- Produces: `validateEmployeeRecord(rec, capabilityKinds, roleNames: string[] = [])`——第三参语义从「skill 分类 id 白名单」改为「岗位 name 白名单」；调用方（Task 4 handlers）传分类 name 清单
- Produces: `EmployeeRecord.skillCategories?: string[]` 改为纯兼容字段（不再校验）

- [ ] **Step 1: 写失败测试**（追加到 `employee-store.test.ts`）

```ts
describe('岗位即分类（2026-09-07）：role 严格校验', () => {
  const base = { id: 'e1', name: '小数', role: '后端开发', capabilities: [], enabled: true, createdAt: 1 };

  it('role 不在岗位名清单 → 400 文案列出可选岗位', () => {
    expect(() => validateEmployeeRecord({ ...base, role: 'backend', skills: [] } as EmployeeRecord, [], ['后端开发', '前端开发']))
      .toThrow(/role 必须是已注册岗位.*后端开发 \/ 前端开发/);
  });

  it('role 在清单 → 通过；skills/skillCategories 旧字段忽略不报错（任意形状）', () => {
    expect(() => validateEmployeeRecord({ ...base, skills: ['whatever'] } as EmployeeRecord, [], ['后端开发'])).not.toThrow();
    expect(() => validateEmployeeRecord({ ...base, skillCategories: 'not-an-array' } as unknown as EmployeeRecord, [], ['后端开发'])).not.toThrow();
  });

  it('岗位清单为空（未装配 Skill 库）→ role 仅做非空校验，不设限', () => {
    expect(() => validateEmployeeRecord({ ...base, role: '自由岗位' } as EmployeeRecord, [], [])).not.toThrow();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**：`pnpm --filter console-api exec vitest run test/employee-store.test.ts`——期望 FAIL（现第三参校验的是 skillCategories，role 自由文本通过）

- [ ] **Step 3: 实现**——`validateEmployeeRecord` 签名与相关段改为：

```ts
/** upsert 前的形状与取值校验（错误信息可读，HTTP 层原样 400）；白名单由调用方传入。
 *  第三参 roleNames（2026-09-07 岗位即分类）：已注册岗位 name 清单，role 必须引用其一；
 *  skills / skillCategories 已退役——不再参与调度与注入，字段原样忽略（旧数据读入不炸）。 */
export function validateEmployeeRecord(rec: EmployeeRecord, capabilityKinds: string[], roleNames: string[] = []): void {
  if (typeof rec.id !== 'string' || !rec.id.trim()) throw new Error('员工 id 必须是非空字符串');
  if (typeof rec.name !== 'string' || !rec.name.trim()) throw new Error('员工 name 必须是非空字符串');
  if (typeof rec.role !== 'string' || !rec.role.trim()) throw new Error('员工 role 必须是非空字符串');
  if (!Array.isArray(rec.capabilities) || !rec.capabilities.every((x) => typeof x === 'string')) {
    throw new Error('员工 capabilities 必须是字符串数组');
  }
  // 白名单仅在注册表已配置时校验（capabilityKinds 空 = 未注入能力注册表，不设限）
  if (capabilityKinds.length > 0) {
    const unknown = rec.capabilities.filter((k) => !capabilityKinds.includes(k));
    if (unknown.length > 0) {
      throw new Error(`员工 capabilities 含未注册的能力: ${unknown.join(', ')}（允许: ${capabilityKinds.join(', ') || '无'}）`);
    }
  }
  // 岗位白名单仅在分类表已配置时校验（roleNames 空 = 未装配 Skill 库/无岗位，不设限）
  if (roleNames.length > 0 && !roleNames.includes(rec.role)) {
    throw new Error(`员工 role 必须是已注册岗位: ${roleNames.join(' / ')}（在管理台「Skill 库 → 岗位管理」维护）`);
  }
  if (rec.supervision !== undefined && (rec.supervision === null || typeof rec.supervision !== 'object' || !LEVELS.has(rec.supervision.level))) {
    throw new Error('员工 supervision.level 必须是 shadow | assisted | trusted');
  }
  if (typeof rec.enabled !== 'boolean') throw new Error('员工 enabled 必须是布尔值');
  if (rec.model !== undefined) {
    const m = rec.model;
    if (typeof m.baseUrl !== 'string' || !m.baseUrl.trim()) throw new Error('员工 model.baseUrl 必须是非空字符串');
    if (typeof m.apiKey !== 'string' || !m.apiKey.trim()) throw new Error('员工 model.apiKey 必须是非空字符串');
    if (typeof m.model !== 'string' || !m.model.trim()) throw new Error('员工 model.model 必须是非空字符串');
    if (m.api !== undefined && m.api !== 'openai-completions' && m.api !== 'anthropic-messages') {
      throw new Error('员工 model.api 必须是 openai-completions | anthropic-messages');
    }
  }
}
```

同时 `EmployeeRecord` 接口注释更新（第 18-23 行区域）：

```ts
  /** 【退役 2026-09-07】可接任务岗位集合——调度已改按 role 精确匹配岗位；字段保留仅为 EmployeeProfile 结构兼容 */
  skills: string[];
  /** 【退役 2026-09-07】原 Skill 分类关联——注入已改按岗位；旧数据读入忽略 */
  skillCategories?: string[];
```

- [ ] **Step 4: 跑测试确认通过**——注意：同文件既有用例若依赖「skillCategories 白名单校验」或「skills 形状校验」会转红，按退役语义删除/改写这些用例（如 `skillCategories 含未注册分类` 用例整体删除）
- [ ] **Step 5: 全量跑 console-api 测试**：`pnpm --filter console-api exec vitest run`——handlers.test.ts / e2e-* 若因第三参语义转红，其夹具改为传 `[]` 或岗位名清单（本任务只修测试夹具使其语义对齐，功能接线在 Task 4）
- [ ] **Step 6: 提交**

```bash
git add packages/console-api/src/team/employee-store.ts packages/console-api/test/employee-store.test.ts packages/console-api/test/handlers.test.ts
git commit -m "feat(team): 员工 role 严格校验岗位名白名单，skills/skillCategories 字段退役忽略"
```

---

### Task 3: 调度匹配改岗位精确匹配

**Files:**
- Modify: `packages/console-api/src/team/managed-roster.ts:57-74`（profileOf / acquire）
- Test: `packages/console-api/test/managed-roster.test.ts`

**Interfaces:**
- Consumes: Task 2 的 `EmployeeRecord`（role 语义 = 岗位名）
- Produces: `acquire(role)` 语义变更：`r.role === role` 精确匹配（scheduler 零改动）

- [ ] **Step 1: 改测试夹具与断言**——`managed-roster.test.ts` 现夹具员工用 `skills: ['backend']` 参与 acquire('backend') 匹配，全部改为岗位语义：夹具记录 `role: 'backend'`（skills 保留为退役字段随意值），断言不变（acquire('backend') 命中逻辑等价，但匹配来源从 skills 变 role）。另外**新增用例**：

```ts
it('岗位匹配为 role 精确相等（skills 退役）：skills 含岗位名但 role 不同 → 不命中', async () => {
  await store.upsert({ id: 'emp-x', name: '错岗', role: '前端开发', skills: ['backend'], capabilities: [], enabled: true, createdAt: 1 });
  await roster.refresh();
  expect(roster.acquire('backend')).toBeNull();
});
```

- [ ] **Step 2: 跑测试确认失败**：`pnpm --filter console-api exec vitest run test/managed-roster.test.ts`——新增用例 FAIL（现按 skills 匹配会命中）

- [ ] **Step 3: 实现**：

```ts
  private profileOf(r: EmployeeRecord): EmployeeProfile {
    return {
      id: r.id, name: r.name, role: r.role, skills: [...(r.skills ?? [])],
      ...(r.supervision ? { supervision: r.supervision } : {}),
    };
  }

  acquire(role?: string, extra?: (p: EmployeeProfile) => boolean): EmployeeProfile | null {
    const hit = this.records.find((r) => {
      if (!this.free.has(r.id)) return false;
      // 岗位精确匹配（2026-09-07 岗位即分类）：role 即岗位名，skills 集合匹配退役
      if (role && r.role !== role) return false;
      if (extra && !extra(this.profileOf(r))) return false;
      return true;
    });
    if (!hit) return null;
    this.free.delete(hit.id);
    return this.profileOf(hit);
  }
```

- [ ] **Step 4: 跑测试确认通过**（含 team-scheduler.test.ts：`pnpm --filter console-api exec vitest run test/managed-roster.test.ts test/team-scheduler.test.ts`——scheduler 夹具若用 skills 匹配，同步改夹具 role）
- [ ] **Step 5: 提交**

```bash
git add packages/console-api/src/team/managed-roster.ts packages/console-api/test/managed-roster.test.ts packages/console-api/test/team-scheduler.test.ts
git commit -m "feat(team): 调度岗位匹配改 role 精确相等，skills 集合匹配退役"
```

---

### Task 4: HTTP 层与装配层接线

**Files:**
- Modify: `packages/console-api/src/http/handlers.ts:268-276`（分类 POST 409）、`501-502 / 522-523`（员工校验传岗位名）
- Modify: `packages/console-api/src/http/server.ts:138-141`（skillsFor 按岗位）
- Test: `packages/console-api/test/handlers.test.ts`、`packages/console-api/test/server-assembly-skills.test.ts`

**Interfaces:**
- Consumes: Task 1 `upsertCategory` 返回值、Task 2 `validateEmployeeRecord` 第三参（岗位名清单）
- Produces: `skillsFor(id)` 新语义——员工 role → 按 name 查分类 → `skillsForCategories([cat.id])`

- [ ] **Step 1: 写失败测试**——`handlers.test.ts` 追加（复用该文件现有 deps 构造模式）：

```ts
it('POST skill-categories 重名 → 409（name 业务键）', async () => {
  await api('POST', '/api/skill-categories', { id: 'a', name: '后端开发' });
  const res = await api('POST', '/api/skill-categories', { id: 'b', name: '后端开发' });
  expect(res.status).toBe(409);
  expect(res.body.message).toMatch(/已存在/);
});

it('POST employees role 非注册岗位 → 400 且文案含岗位清单', async () => {
  const res = await api('POST', '/api/employees', {
    id: 'e9', name: '小九', role: '不存在的岗位', capabilities: [], enabled: true,
  });
  expect(res.status).toBe(400);
  expect(res.body.message).toMatch(/必须是已注册岗位/);
  expect(res.body.message).toMatch(/后端开发/); // seed 分类名在清单中
});
```

`server-assembly-skills.test.ts` 追加（该测试走真实 startConsoleServer + 真实 store）：

```ts
it('skillsFor 按岗位注入：员工 role=岗位名 → 该分类 approved skill 生效', async () => {
  // 既有装配模式：建分类 → 建 approved skill（categoryId=分类 id）→ 建员工 role=分类 name
  // 断言：plan 执行链收到的 skills 含该 skill；再把员工 role 改成无 skill 的岗位 → 注入空清单
});
```

（按该文件既有断言手段展开；核心断言 = 注入清单随员工岗位变化，且岗位无对应分类时返回 `[]` 不抛错。）

- [ ] **Step 2: 跑测试确认失败**：`pnpm --filter console-api exec vitest run test/handlers.test.ts test/server-assembly-skills.test.ts`

- [ ] **Step 3: 实现**——handlers.ts 分类 POST 段：

```ts
        if (method === 'POST' && seg.length === 2) {
          const cat = body as SkillCategory;
          try { validateSkillCategory(cat); } catch (e) { return err(400, e instanceof Error ? e.message : String(e)); }
          // 岗位 name 业务键（2026-09-07）：重名 → 409
          if ((await deps.skills.upsertCategory(cat)) === 'name-conflict') {
            return err(409, `岗位「${cat.name}」已存在（name 唯一）`);
          }
          return ok({ id: cat.id }, 201);
        }
```

handlers.ts 员工两处校验（501-502 / 522-523 一致改）：

```ts
            const kinds = ...; // 原有 capabilityKinds 取值不动
            // 岗位名白名单（2026-09-07）：分类 name 即岗位
            const roleNames = deps.skills ? (await deps.skills.listCategories()).map((c) => c.name) : [];
            try { validateEmployeeRecord(rec, kinds, roleNames); } catch (e) { return err(400, e instanceof Error ? e.message : String(e)); }
```

server.ts skillsFor：

```ts
        // 岗位即分类（2026-09-07）：员工 role（岗位名）→ 按 name 查分类 → 该分类 approved skills；
        // 岗位无对应分类（被删/未建）→ 空清单不抛错
        skillsFor: async (id: string) => {
          const rec = await employeeStore.get(id);
          if (!rec) return [];
          const cats = await skillStore.listCategories();
          const cat = cats.find((c) => c.name === rec.role);
          return cat ? skillStore.skillsForCategories([cat.id]) : [];
        },
```

- [ ] **Step 4: 跑测试确认通过**（Step 2 命令 + `pnpm --filter console-api exec vitest run` 全量）
- [ ] **Step 5: 提交**

```bash
git add packages/console-api/src/http/handlers.ts packages/console-api/src/http/server.ts packages/console-api/test/handlers.test.ts packages/console-api/test/server-assembly-skills.test.ts
git commit -m "feat(console-api): HTTP/装配层按岗位接线——重名 409、role 白名单、skillsFor 按岗位注入"
```

---

### Task 5: 前端员工编辑改岗位下拉（旧字段移除）

**Files:**
- Modify: `apps/console/src/api.ts`（EmployeeRecordView 两字段改可选）
- Modify: `apps/console/src/components/EmployeeEditDialog.vue`
- Modify: `apps/console/src/views/Employees.vue:75-77`（启用链路 PUT payload）
- Test: `apps/console/test/employee-edit-dialog.test.ts`

**Interfaces:**
- Consumes: `GET /api/skill-categories`（岗位清单，`api.listSkillCategories()` 已存在）

- [ ] **Step 1: 改类型**——api.ts `EmployeeRecordView`：

```ts
export interface EmployeeRecordView {
  id: string;
  name: string;
  role: string;
  /** 【退役 2026-09-07】调度改按岗位；旧数据可能有值，不再提交 */
  skills?: string[];
  /** 能力 kind 绑定：任务项 kind ∈ 此集合才可执行；空数组 = 全部可用 */
  capabilities: string[];
  /** 【退役 2026-09-07】注入改按岗位；旧数据可能有值，不再提交 */
  skillCategories?: string[];
  // ……其余字段不动
```

- [ ] **Step 2: 写失败测试**——`employee-edit-dialog.test.ts` 追加/改写：

```ts
it('岗位为下拉单选（数据源岗位清单），不再有技能标签与技能分类输入', async () => {
  // 既有挂载模式；岗位下拉选项来自 mock 的 skill-categories（名称为选项文本）
  // 断言1：data-test="d-role" 是 select 且选项含「后端开发」
  // 断言2：document.querySelector('[data-test="d-skills"]') 为 null（技能标签输入移除）
  // 断言3：document.querySelector('[data-test="d-skill-cats"]') 为 null（技能分类多选移除）
  // 断言4：选「后端开发」保存 → createEmployee 收到的 payload.role === '后端开发'，且不含 skills/skillCategories 键
});
```

- [ ] **Step 3: 跑测试确认失败**：`pnpm --filter console exec vitest run test/employee-edit-dialog.test.ts`

- [ ] **Step 4: 实现 EmployeeEditDialog.vue**：
  - `skillCats` 重命名 `roleOptions`（注释：岗位清单 = 分类表，2026-09-07）
  - 模板岗位表单项改为下拉（替换原文本 input）：

```vue
      <el-form-item label="岗位">
        <el-select
          data-test="d-role"
          v-model="form.role"
          filterable
          :teleported="false"
          placeholder="选择岗位"
          class="!w-full"
        >
          <el-option v-for="c in roleOptions" :key="c.id" :label="c.name" :value="c.name" />
        </el-select>
      </el-form-item>
```

  - 整体删除「技能」el-form-item（`d-skills`）与「技能分类」el-form-item（`d-skill-cats`）
  - `emptyDraft()` / watch 回填 / save payload 中删除 `skills` / `skillCategories` 键（类型已可选）
- [ ] **Step 5: Employees.vue 启用链路**——PUT payload（75-77 行附近）删除 `skills: row.skills` 与 `skillCategories: row.skillCategories` 两行；表格「技能」列（123-124 行附近）删除或改为展示 `row.role`（保留岗位列则与既有岗位列去重——若已有 role 列，直接删技能列）
- [ ] **Step 6: 跑测试确认通过**：`pnpm --filter console exec vitest run`（全量——Employees.test 等夹具同步改）
- [ ] **Step 7: 提交**

```bash
git add apps/console/src/api.ts apps/console/src/components/EmployeeEditDialog.vue apps/console/src/views/Employees.vue apps/console/test/
git commit -m "feat(console): 员工编辑岗位改下拉单选，技能标签/技能分类字段退役"
```

---

### Task 6: 任务创建岗位下拉 + 文案与详情页收尾

**Files:**
- Modify: `apps/console/src/components/TaskCreateDialog.vue:20-44,83,211-216`
- Modify: `apps/console/src/views/SkillLibrary.vue`（分类管理文案改「岗位管理」）
- Modify: `apps/console/src/views/EmployeeDetail.vue:110-114`
- Test: `apps/console/test/task-create-dialog.test.ts`、`apps/console/test/skill-library.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// task-create-dialog.test.ts 追加：
it('岗位下拉数据源为岗位清单（不再取员工 skills 并集），且不可自由创建', async () => {
  // mock api.listSkillCategories → [{ id:'backend', name:'后端开发' }, ...]
  // 断言1：岗位下拉选项 = 岗位名（不出现员工 skills 独有值）
  // 断言2：f-role 输入框不可输入任意文本（allow-create 已移除——EP select 无 create 行为）
});

// skill-library.test.ts 改写：
it('分类管理入口文案为「岗位管理」', () => { /* data-test="manage-cats" 按钮文本含「岗位管理」；对话框标题含「岗位管理」 */ });
```

- [ ] **Step 2: 跑测试确认失败**：`pnpm --filter console exec vitest run test/task-create-dialog.test.ts test/skill-library.test.ts`

- [ ] **Step 3: 实现 TaskCreateDialog.vue**：
  - 44 行 `skillOptions` 改为：

```ts
// 岗位下拉选项（2026-09-07 岗位即分类）：来自岗位管理清单（分类表 name），受管不可自由创建
const roleOptions = ref<string[]>([]);
```

  - 83 行附近的并行拉取处追加 `api.listSkillCategories()` → `roleOptions.value = cats.map((c) => c.name)`（失败不阻塞，保持空数组）
  - 模板 211-216 行岗位 el-select：`:options` 数据源换 `roleOptions`，删除 `allow-create default-first-option`，placeholder 改「选择岗位（可空 = 不限）」
- [ ] **Step 4: 实现 SkillLibrary.vue**——「管理分类」按钮与对话框标题、分类表格表头等文案中「分类」改「岗位」（检索「分类」逐一改：管理岗位 / 岗位名称 / 新建岗位 / 删除岗位等；skill 记录表格里的「分类」列改名「岗位」列）；`api.listSkillCategories` 调用不变
- [ ] **Step 5: 实现 EmployeeDetail.vue**——110 行「技能」el-descriptions-item 与 114 行「技能分类」el-descriptions-item 整体删除（岗位展示已有 role 行则不动）
- [ ] **Step 6: 跑测试确认通过**：`pnpm --filter console exec vitest run` 全量
- [ ] **Step 7: 提交**

```bash
git add apps/console/src/components/TaskCreateDialog.vue apps/console/src/views/SkillLibrary.vue apps/console/src/views/EmployeeDetail.vue apps/console/test/
git commit -m "feat(console): 任务创建岗位下拉取岗位清单，管理页文案改岗位管理，详情页退役字段移除"
```

---

### Task 7: 全量回归 + 收尾登记

- [ ] **Step 1: 全仓测试与类型检查**

```bash
pnpm -r typecheck   # 期望零错误
pnpm --filter console-api exec vitest run   # 期望全绿
pnpm --filter console exec vitest run       # 期望全绿
```

夹具遗留转红逐个修（常见：员工夹具 role 自由文本 → 改用岗位名；旧 skillCategories 断言 → 删除）。

- [ ] **Step 2: data 目录演示数据说明**——不写迁移脚本；确认 `packages/console-api/data/employees.json` 现有员工的 role 是否为岗位名（如「后端开发」），不是则提醒用户在管理台改档案或重置 data
- [ ] **Step 3: 总账登记**——`docs/阶段成果总账.md` P16 补充段追加「岗位管理即技能库」小节（改动点 + 测试数字 + 存量数据手工迁移提醒）
- [ ] **Step 4: 提交**

```bash
git add docs/阶段成果总账.md
git commit -m "docs(总账): 岗位管理即技能库收官登记"
```
